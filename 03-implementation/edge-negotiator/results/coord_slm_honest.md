# Honest coordination effect with the REAL Phi-4-mini SLM (throughput-controlled, 2x2 grid)

Generated by `src/run_slm_metrics.py`. Both arms use the **real SLMAgent** (`Phi-4-mini-instruct-generic-gpu:5` via Foundry Local); the ONLY difference is whether coordination information reaches the model. `uncoordinated` runs the event-gated hybrid (SLM proposes, MaxPressure shield disposes) with NO neighbour note; `coordinated` (coord_weight=1.0) additionally injects the per-phase neighbour-note into the SLM prompt (**Channel A**) and makes the coordination-adjusted deterministic reference live as the shield fallback (**Channel B**).

WHY THE REAL SLM IS REQUIRED: coordination enters chiefly via Channel A, the neighbour-note in the SLM prompt. The deterministic StubAgent ignores that note (proven in `results/coord_throughput.md`), so the Channel-A effect can ONLY be measured with the real model -- which is exactly this run.

Every run is scored with the throughput-controlled metrics in `src/metrics.py`, parsed from a tripinfo emitted WITH `--tripinfo-output.write-unfinished --tripinfo-output.write-undeparted` (unique path per run under `results/tripinfo_slm/`), so the whole vehicle population (completed + stranded-at-horizon + never-departed) is counted and the Milestone-2 survivorship bias cannot recur.

- Seeds: n=4 ([0, 1, 2, 3])
- Horizon: end=500 steps (per run, capped)
- SLM model: `Phi-4-mini-instruct-generic-gpu:5` (real, serialised ~0.5s/call)
- Coordinated mode: coord_weight=1 (Channel A note + Channel B adjusted reference both live)

## Headline verdict

**With the REAL SLM and throughput-controlled metrics, Channel-A/B coordination shows: NO SIGNIFICANT EFFECT (NEUTRAL)** (coordinated vs uncoordinated, paired on seed).

- mean_network_delay: coordinated improved by diff=-1.723 vs uncoordinated, perm p=0.7542 (CI includes 0; n.s.)
- completion_rate: coordinated improved by diff=+0.007 vs uncoordinated, perm p=0.3859 (CI includes 0; n.s.)
- throughput: coordinated improved by diff=+2.250 vs uncoordinated, perm p=0.5107 (CI includes 0; n.s.)
- matched_diff (same-vehicle travel time, coord - uncoord): -1.02s, perm p=0.7454 (CI includes 0; n.s.)

> **POWER CAVEAT (honest):** n=4 seeds is small. With so few paired observations the permutation test has very low power and the BCa CIs are wide, so a 'NO SIGNIFICANT EFFECT' result is **likely underpowered** -- it means *not detected at n=4*, NOT *proven absent*. A larger seed count would be needed to resolve a small true effect.

## Coordination pathway provenance (proves the channels were live)

These confirm coordination was actually exercised in the coordinated arm; without them a null result could merely mean 'coordination never fired'.

- Channel-A neighbour-notes injected into SLM prompts: **634** (across 4 coordinated runs)
- Verified neighbour messages received: **1306**
- `coord_adjusted_decisions` (Channel-B adjusted choice differed from plain MaxPressure): **100**
- `coord_changed_events` (per-event cross-check of the above): **100**

## Per-mode honest metrics (mean over seeds)

`slm_decisions` = event-gated SLM consultations; `slm_differed_from_shield` = times the SLM's phase differed from the MaxPressure shield's (proves the real model was in the loop and active).

| mode | n | throughput | completion_rate | mean_network_delay | total_network_delay | avg_tt_completed (biased) | slm_decisions | slm_differed_from_shield |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| uncoordinated | 4 | 97.5 | 0.277 | 231.4 | 81249 | 118.5 | 164 | 15.5 |
| coordinated | 4 | 99.8 | 0.284 | 229.6 | 80502 | 117.0 | 165 | 18.0 |

## Inferential comparison: coordinated vs uncoordinated (BCa 95% CI + paired permutation)

Paired on seed. The family is a single comparison per metric, so Holm reduces to the raw permutation p.

**Metric: mean_network_delay (lower=better)**

| group | n | mean | 95% BCa CI | diff vs uncoord | diff 95% BCa CI | excl 0 | perm p | Holm thr | Holm reject |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| uncoordinated (baseline) | 4 | 231.355 | [227.073, 234.511] | - | - | - | - | - | - |
| coordinated | 4 | 229.632 | [224.659, 236.601] | -1.723 | [-7.538, 3.897] | no | 0.7542 | 0.0500 | fail-to-reject |

**Metric: completion_rate (higher=better)**

| group | n | mean | 95% BCa CI | diff vs uncoord | diff 95% BCa CI | excl 0 | perm p | Holm thr | Holm reject |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| uncoordinated (baseline) | 4 | 0.277 | [0.271, 0.284] | - | - | - | - | - | - |
| coordinated | 4 | 0.284 | [0.265, 0.295] | 0.007 | [-0.007, 0.015] | no | 0.3859 | 0.0500 | fail-to-reject |

**Metric: throughput (higher=better)**

| group | n | mean | 95% BCa CI | diff vs uncoord | diff 95% BCa CI | excl 0 | perm p | Holm thr | Holm reject |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| uncoordinated (baseline) | 4 | 97.500 | [93.500, 101.000] | - | - | - | - | - | - |
| coordinated | 4 | 99.750 | [90.750, 104.750] | 2.250 | [-4.500, 7.250] | no | 0.5107 | 0.0500 | fail-to-reject |

## Matched-set (same-vehicle) travel-time comparison

`matched_diff` averages travel time over ONLY the vehicles that completed in BOTH arms (apples-to-apples; the survivorship-bias antidote for travel time). diff = coordinated - uncoordinated; negative => coordination is faster on shared trips. BCa CI + paired-vs-zero permutation p over seeds.

| comparison | seeds | mean diff (coord - uncoord) | 95% BCa CI | excl 0 | perm p | total matched veh | only coord | only uncoord |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| coordinated vs uncoordinated | 4 | -1.02 | [-4.72, 3.76] | no | 0.7454 | 367 | 32 | 23 |

Per-seed matched diffs: seed 0: +6.16s (n=95), seed 1: -3.42s (n=97), seed 2: -6.02s (n=88), seed 3: -0.78s (n=87)

## Method notes

- BOTH arms use the SAME real SLMAgent; the only manipulated variable is whether coordination info reaches the model. A difference is therefore attributable to coordination (chiefly Channel A).
- `throughput` is a count and cannot be gamed by stranding slow vehicles. `mean_network_delay` divides total time-in-network by ALL departed vehicles (stranded ones keep their accrued time), so a 'completes fewer but faster' controller earns no spurious win.
- The biased `avg_tt_completed` column is shown only for contrast; it is NOT the headline.
- Comparisons are paired on seed; permutation p uses the +1 small-sample correction (so p is never exactly 0).
- n=4 seeds. Significance is reported honestly: at this seed count the test is likely UNDERPOWERED.
