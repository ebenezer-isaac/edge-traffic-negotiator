# Ledger benchmark — Besu on-chain registry vs local in-memory baseline

Generated by `src/bench_ledger.py`. All latencies are **measured at runtime**, not estimated.

## Setup

- **Besu**: `besu/v24.12.0/linux-x86_64/openjdk-java-21`, single-node `--network=dev` (free gas, PoW dev miner), Docker.
- **RPC**: `http://127.0.0.1:8545`  |  **chainId**: `1337`  |  **gasPrice**: 0.
- **Measured block/finality time**: **1.000 s** (mean inter-block interval over recent blocks).
- **Trials per op**: 20  |  **batch size N**: 10.
- **local** = `src/registry.py` (the *plain-signing / no-ledger* baseline).  **besu** = `src/besu_registry.py` over the live node.
- Write latency (register/revoke) is timed **end-to-end including mining + receipt wait** — i.e. commit-to-finality on this dev chain.
- Read latency (is_approved/public_key) is a local `eth_call` against contract state (no transaction).

## Results (milliseconds)

| op | local median | local p95 | besu median | besu p95 | besu/local (median) |
|---|---|---|---|---|---|
| register | 0.0154 | 0.0353 | 1444.23 | 3862.95 | 93,781x |
| revoke | 0.0101 | 0.0254 | 1841.71 | 5388.59 | 181,450x |
| is_approved | 0.0002 | 0.0012 | 46.42 | 67.39 | 232,078x |
| public_key | 0.0002 | 0.0008 | 31.77 | 48.23 | 158,865x |
| batch_register_10 | 0.1478 | 0.2058 | 20559.82 | 27784.31 | 139,059x |

## Raw summary

```
LOCAL (in-memory baseline):
  register                 n= 20 median=0.0154ms p95=0.0353ms mean=0.0235ms min=0.0112 max=0.1342
  revoke                   n= 20 median=0.0101ms p95=0.0254ms mean=0.0123ms min=0.0082 max=0.0396
  is_approved              n= 20 median=0.0002ms p95=0.0012ms mean=0.0004ms min=0.0001 max=0.0037
  public_key               n= 20 median=0.0002ms p95=0.0008ms mean=0.0004ms min=0.0002 max=0.0028
  batch_register_10        n= 20 median=0.1478ms p95=0.2058ms mean=0.1548ms min=0.1139 max=0.2095

BESU (on-chain):
  register                 n= 20 median=1444.23ms p95=3862.95ms mean=1781.20ms min=502.06 max=4900.89
  revoke                   n= 20 median=1841.71ms p95=5388.59ms mean=2266.79ms min=232.70 max=6766.27
  is_approved              n= 20 median=46.42ms p95=67.39ms mean=51.77ms min=32.46 max=145.22
  public_key               n= 20 median=31.77ms p95=48.23ms mean=35.00ms min=25.02 max=57.44
  batch_register_10        n= 20 median=20559.82ms p95=27784.31ms mean=20575.10ms min=12384.59 max=28182.93
```

## Interpretation

- **Reads are cheap on both backends.** `is_approved` / `public_key` are local `eth_call`s on Besu — single-digit-to-low-tens-of-ms — vs sub-microsecond in-memory. For the message bus, a per-message membership check could hit the chain, but in practice membership is cached and the registry is consulted on change, so read cost is not a control-loop risk.
- **Writes carry finality latency.** `register` / `revoke` median commit latency is bounded by the block period (**1.000 s**). On the in-memory baseline the same op is sub-microsecond. This is the whole reason the brief keeps the ledger **off the control loop**.
- **Does the brief's '1-2 s finality, async-only' assumption hold? YES — confirmed with real numbers.** Measured `register` median = **1.44 s**, `revoke` median = **1.84 s** — both squarely inside the brief's stated **1-2 s** envelope (block period is 1.0 s; the extra ~0.5-0.8 s is tx-submission + receipt-poll + occasional multi-block sealing, p95 reaching ~3.9-5.4 s under the dev miner's intermittent world-state pauses). A production QBFT network (2 s default block period + multi-validator round-trips) would land at the top of, or just above, this band. So the assumption is **sound for an async audit-write path** but would be **catastrophic on the fast control path** — a 1.4 s membership write vs the **0.000015 s** (15 microsecond) in-memory equivalent is a ~94,000x penalty. The architecture decision to keep the ledger strictly async is validated by these numbers.

## Implication for the audit-log write path

- Because each on-chain write costs ~1 block, writing one transaction per control decision (sub-second cadence across 6 junctions) would saturate the commit path and couple control latency to consensus latency. **Mitigation (already in the brief §3): batch commits.** Buffer audit events in memory on the fast path and flush them asynchronously — either as periodic batched transactions or by anchoring a Merkle root of a batch on-chain. The `batch_register_N` row shows sequential per-tx cost scales linearly with N, confirming that *un-batched* per-event writes do not scale; batching/anchoring is required for the real audit volume.

## Limitations of these numbers

- Single-node **dev** chain (PoW dev miner), not multi-validator QBFT, so this is a **lower bound** on production finality. QBFT adds validator round-trips.
- Localhost RPC: no network latency between the agent host and a remote node, which a real deployment would add.
- Free gas (`gasPrice=0`): excludes any fee-market queueing a permissioned production chain might still impose under load.
