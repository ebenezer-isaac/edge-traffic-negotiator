\subsection{Experiment inventory}\label{sec:met-inventory}
Table~\ref{tab:inventory} (Appendix~\ref{app:tables}) maps every
experiment family in \S\ref{ch:results} to its design and artifact trail;
all raw cells, per-seed records and aggregation scripts are retained in the
repository, so every number is recomputable. The campaign spanned three
machines and roughly six weeks of wall-clock compute.
