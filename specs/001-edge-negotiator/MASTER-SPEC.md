# The Edge Negotiator: Master Build & Experiment Specification (v5)

**Status**: CANONICAL, frozen for direction. Consolidates five adversarial teardowns (rounds 2-6). Single document Fable builds from.

**v5 CHANGELOG (2026-07-21, round-6 no-regret fixes; NO framing change — all within the 2026-07-21 honest conditional-coupling + characterisation decision):**
- **The FINDING is now a measured quantity, not a confirmatory table** (novelty F1/F2/F3): the headline object is the **critical coverage-density `d_crit(footprint)`** — the honest-independent-sighting coverage a deviation's physical footprint needs to be caught-or-traced — measured as a function of the *real Euston turning-count geometry*, with a PRE-REGISTERED falsifiable hypothesis that the real geometry moves `d_crit` (or creates spatial heterogeneity / a collusion×density interaction) away from the well-mixed FDI/CT prediction.
- **The conditional coupling is stated as a theorem with EXPLICIT SUFFICIENT hypotheses** (novelty F2, methods, security F7): `[coverage > d_crit(footprint)] ∧ [corroboration from honest independent keys only] ∧ [non-equivocating quorum anchor] ∧ [no operator creation-time omission]` ⇒ every deviation caught-live-or-traced. Sufficiency proven analytically; necessity shown empirically by the sweep. (v4's `witness-density > 0` was a false sufficient condition; the operator-omission premise was missing.)
- **The hard measured object is a proven provenance-INDISTINGUISHABILITY result** (methods F1/F2), not a veracity-triad accuracy: prove the physical-veracity triad {authentic-deviation, honest-sensor-error, detector-error} is provenance-indistinguishable (this IS the characterisation), force those cells to `unknown`, and score accuracy ONLY over the origins provenance CAN decide.
- **Two in-scope free-deviation classes added to the honest boundary** (security F1/F2): piggyback-inflation on a legitimately-corroborated event (→ `driving_input_seqs` is now a SET, and preemption magnitude is bound to corroborated need), and keyless physical sensor-spoof by one non-operator actor. The boundary is now honestly complete.
- Build (reach the real producer), legal (citation accuracy), and consistency (the surviving false "is LANDED" done-claim) fixes folded in. Full ledger in `_hardening-log.md`. Round-6: 13 fatal / 36 major, all no-regret; fatals halved vs round 5.

**BUILD-TARGET NOTICE (read first):** this spec describes the INTENDED built system. A claim is *not settled* until its §12 acceptance gate passes over a real run. Sections describing unbuilt behaviour (§1(b)/(c), §3, §6, §8, §9) are targets. The current code is pre-teardown; §5 + §11 are the path to conform it. Do not quote any number or capability as achieved until its gate is green.

**Golden rule:** fail loud, never degrade silently. Any gate that cannot prove its precondition (real model answered, corpus retrieved, edges resolved, signatures verified, quorum-anchored + cross-audited) must SKIP-with-a-recorded-skip or ABORT that step, never emit a green result.

---

## 0. Locked constraints (user decisions; do NOT overturn without consulting the user)

- SLM is a REQUIRED component (load-bearing + honestly measured; a pre-committed kill-criterion (§8) may demote its CLAIM, never its presence).
- Substrate: real **Euston Road (A501)**, 3-4 signal stretch. Pre-built + committed (§9). Synthetic corridor = unit-test fixture only.
- Fault rules grounded in cited UK law (`01-research/uk-traffic-law.md`).
- **Fault output = SPLIT** (2026-07-16): a mechanically-verifiable PROVENANCE evidence pack (closed forensic channel, NO machine verdict/confidence/accusation, mechanical origin-independent citation) + a firewalled, NON-EVIDENTIAL, counsel-gated internal AI triage note.
- **Novel object = the DUALITY as an HONEST CONDITIONAL COUPLING + a measured CHARACTERISATION** (2026-07-21). Characterise the bounded class of stealthy conservation-consistent DEVIATIONS the real-time gate provably cannot detect (physical-impact axis), and prove the CONDITIONAL coupling under explicit hypotheses (§8), whose contribution is the **measured map of its boundary** — the critical coverage-density `d_crit`, and exactly where the coupling fails (colluding keys; keyless sensor-spoof; piggyback-inflation; operator creation-time omission; witness-density → 0) — on a real London corridor. The mechanism is CONCEDED to be Certificate-Transparency-style accountability (cited); the increment is the conditional theorem with explicit assumptions + the first quantified measurement of `d_crit`/the boundary on real corridor geometry.
- **A QUORUM / gossip anchor with a specified cross-audit is MANDATORY for any non-equivocation claim** (2026-07-21): ≥2 independent external witnesses + a named auditor that reads both and surfaces disagreement. A single-node, un-cross-audited, or mock anchor CANNOT back the coupling.
- Deadline August 2026; scope phased, not cut.

---

## 1. The one thesis (frozen)

> An on-device, trust-preserving coordination layer for signalised junctions, and a **measured characterisation** of exactly which stealthy insider deviations it can and cannot hold accountable. Contribution: (a) a deterministic real-time gate that refuses signed-but-uncorroborated emergency preemption from a compromised insider and clears physically-corroborated real emergencies; (b) a Certificate-Transparency-style, quorum-anchored, cross-audited, signature-verified accountability log (mechanism credited to prior art, cited); and (c) the novel object — a **conditional coupling** proven under explicit hypotheses (adequate honest-sighting coverage of the deviation footprint, honest independent corroboration, a non-equivocating quorum, and no operator creation-time omission), together with the quantified measurement of its **boundary**: the critical coverage-density `d_crit` as a function of the real Euston turning-count geometry, and the free-deviation classes that survive (colluding keys; keyless physical sensor-spoof; piggyback-inflation on a corroborated event; operator omission). The honest boundary — not an unconditional guarantee — is the finding. A frozen on-device SLM is the mandatory **reasoner-of-record** that produces the map's per-incident origin labels from the verified, quorum-complete audit window + retrieved statute, evaluated for citation-faithfulness and for a measured frozen-vs-adaptive robustness/adaptivity tradeoff.

**Lead with (the novel object):** the **measured boundary** — `d_crit` vs the well-mixed prediction, the free-deviation classes, and the provenance-indistinguishability result — on real Euston geometry. NOT "a novel security mechanism," NOT a detectability ROC, NOT "the SLM beats a rule," NOT "coordination optimises traffic."

**Position vs prior art (concede the mechanism, claim the measurement):** the accountability layer IS Certificate Transparency / tamper-evident-log / accountability — RFC 6962 (Laurie 2013), CONIKS (Melara 2015), A2M (Chun 2007), TrInc (Levin 2009), PeerReview (Haeberlen 2007), Haber-Stornetta 1991, Schneier-Kelsey 1999, Küsters CCS 2010; CT auditing/gossip needs multiple logs + auditors (Chuat CNS 2015). The stealthy class is FDI — Liu-Ning-Reiter 2011, Teixeira-Sandberg 2015, Mo-Sinopoli 2010; the topology-dependence of unobservability is owned — Kosut-Jia-Thomas-Tong (IEEE TSG 2011), Hendrickx-Johansson-Jungers-Sandberg-Vetterli (IEEE TAC 2014), Cárdenas-Amin-Sastry (HotSec 2008); collusion vs TSC — Qu-Tang-Ma (arXiv 2111.02845); non-repudiation ≠ truthfulness — Li-Nejad-Zhang (arXiv 1906.02628). **Our increment (do not overstate — drop "first" as load-bearing):** these abstract security-index / unobservable-subspace results assume a WELL-MIXED measurement model; on a real signalised corridor the sighting-independence geometry is HETEROGENEOUS, and we measure how that changes `d_crit` / the free-region shape — extending named prior work, not re-running it. Confront Traffic-R1 (differentiator = the accountability ROLE + the measured boundary).

**SLM (mandatory) role:** the reasoner-of-record producing the characterisation's per-incident origin labels (Job A), the honestly-characterised disambiguation classifier (Job B), and a measured robustness/adaptivity tradeoff (Job C: frozen vs a hardened continual-learner under poisoning). A pre-committed kill-criterion (§8) may demote the SLM's CLAIM; it stays in the system regardless.

---

## 2. Single Source of Truth (freeze; every live doc conforms)

| Item | Frozen value |
|---|---|
| Headline / novel object | The measured CHARACTERISATION: critical coverage-density `d_crit` vs the well-mixed prediction + the free-deviation boundary + the provenance-indistinguishability result, on Euston A501 |
| Mechanism basis | Certificate-Transparency-style quorum-anchored + cross-audited accountability log — CONCEDED to prior art (RFC 6962 et al), cited, NOT claimed novel |
| Fault output | SPLIT: mechanically-verifiable provenance pack (no verdict/confidence/accusation; mechanical origin-independent citation; closed forensic channel) + firewalled, non-evidential, counsel-gated internal AI note |
| Real-time defence | Deterministic Ed25519 auth + conservation/CUSUM + corroboration gate; preemption MAGNITUDE bound to corroborated need |
| Model | Phi-4-mini, 3.8B, frozen, Foundry Local |
| Deploy target | Foundry Local in SUMO; Jetson/INT4 = future hardware-in-loop only |
| Ledger | Signed hash-chained AuditLog + a QUORUM external anchor (≥2 independent witnesses) + a named cross-auditor + first-party signed per-signer emission counts (out-of-scope-operator instrument only). Completeness ≠ integrity (§6.7). |
| **Substrate** | **Euston Road (A501) 3-4 signal stretch, pre-built & committed; synthetic corridor = fixture only; Lambeth dropped** |
| Fault attribution ontology | Origin over the audit log: `attacker-key` / `system-classifier` / `system-detector` / `sensor-fed-spoof` / `colluding-keys` / `legitimate` / `unknown`. Lives in the INTERNAL note, never the pack. Where the origin is a signing party it names a KEY, never a person; attribute-to-key is a ~100% verification CHECK, NOT the contribution. |
| Provenance-decidable origins (scored) | ONLY: `key-attributed-corroboration`, `uncorroborated-signed` (→`attacker-key`), `keyless-lone-sighting` (→`sensor-fed-spoof`), `colluding-keys` (≥2 keys, mechanical), `legitimate`. The physical-veracity triad {authentic-deviation, honest-sensor-error, detector-error} is PROVEN provenance-indistinguishable → forced to `unknown`, never scored (§3/§8). |
| CoT / reasoning | Internal note contains reasoning; VALIDATED against cited statute, never trusted; raise `max_tokens`. Non-evidential, firewalled, counsel-gated. |
| RAG | policy text from `ground_rules.yaml` + `uk-traffic-law.md`; small curated corpus; fail-loud if empty; not the Qdrant experience-store |
| Coordination effect | a STRUCTURAL ZERO (inert term); reported as such, NOT an experiment (§8) |
| EV benefit (StubAgent demo, deterministic gate, n=30) | 31.1 s [25.4, 36.5] (preemption on vs off; NOT coordination, NOT SLM) |
| Gate cost (StubAgent demo) | 8.0 s [3.7, 12.5] · Worst-case harm avoided 16.05 s [11.51, 20.44] · Mean harm avoided 4.63 s [3.25, 5.99] |
| Seeds / determinism | n≥30 for any inferential claim (exp1 n=1 / exp2 n=3 = pilots); determinism N=100 across restarts |
| STSD / superseded docs | in-body stale claims to be NEUTRALISED or D1-allowlisted (target-state, §5) |

---

## 3. The SLM (mandatory): jobs, all non-circular

**Job A — reasoner-of-record for the characterisation's per-incident origin labels** (novelty F4: this is the SLM's headline justification, the INSTRUMENT of the map, not a sidecar). Given the *cryptographically-verified AND quorum-complete* audit window + retrieved statute, it writes the **internal, non-evidential, counsel-gated** note:
```
{ candidate_origin: attacker-key|system-classifier|system-detector|sensor-fed-spoof|colluding-keys|legitimate|unknown,
  cited_rules: ["RTA1988-s36", ...], reasoning: "<=120 words linking VERIFIED facts to cited rules",
  fault_weight_note: "MUST-rule (statute) | should-rule (advisory, RTA s.38(7))" }
```
**Primary metric = origin-classification accuracy vs the injected latent cause, as a CONFUSION MATRIX, scored ONLY over the provenance-decidable origins** (§2). The scoring label is SEVERED from any field the engine/SLM reads (`ground_truth` scores ONLY). **The hard finding is the theorem (§8) that the physical-veracity triad is provenance-indistinguishable** — those cells are pre-registered as forced-to-`unknown`, and D6 FAILS if the pre-registered provenance-SEPARABLE fraction of incidents falls below a stated floor (so a matrix dominated by trivial classes cannot pass; novelty F5). Pin the injected class PRIOR; report per-class precision/recall + a scored-origins accuracy with its own MDE + a failing threshold. Also: citation-correctness vs annotator-blind held-out labels (gated on 2-rater kappa ≥ 0.6 else NOT_ASSESSED); faithfulness graded against STATUTE TEXT by qualified humans (no LLM-as-judge); the SLM false-citation rate on novel combos — build the annotator-blind novel-combo gold so BOTH faithfulness and FCR are reported there (methods M9), or drop "primary" for that region.

**Job A baseline = a deterministic rule-to-text template with a graceful fallback**; coverage scored by citation CORRECTNESS; pin the numeric FCR ceiling + the exact both-metrics dominance rule pre-run (methods M10). A citation whose supporting fact lacks verified provenance is a validation FAILURE (anti prompt-injection §6.9).

**Job B — disambiguation classifier**, characterised under §8. Win-or-a-powered-null.

**Job C — robustness/adaptivity tradeoff.** Frozen SLM vs a HARDENED continual-learner. Pin the baseline concretely (methods M6): the continual model class + online update rule; a defense demonstrably applicable to THAT class (do not cite TRIM/Steinhardt-Koh-Liang, which are convex/regression defenses that do not compose with LLM fine-tuning — name an applicable LLM-continual defense or a plausible instruction-tuning sanitiser and justify applicability); the poison channel + budget as a fraction of the stream; a measurable attacker objective. Report robustness gain AND adaptivity loss, BOTH converted to the same external operational-harm currency (sec EV delay / FP%) with PRE-REGISTERED poisoning-vs-shift mixing weights; Exp1-grade n/MDE/CI/pre-registration.

**The single load-bearing SLM job (methods M5):** the headline SLM claim survives ONLY by winning **Job A** (origin-label citation-correctness dominating the template at the pre-registered MDE + FCR ceiling on novel combos). Job B and Job C are reported but are NOT sufficient for claim-survival. The kill-criterion is an AND on the load-bearing job, not an OR across arms.

**The E2 rule + tie-break:** a validly-signed, uncorroborated claim that caused harm → `attacker-key` (wins the tie against catchable-phantom). `system-classifier`/`system-detector` trigger ONLY via a PURELY PROVENANCE-DERIVED predicate with zero `ground_truth` reference (e.g. "a corroboration check the detector logged as failed is contradicted by an independently-signed sighting record present in the verified window"); if no such provenance-only predicate exists, the class is pre-declared non-separable → `unknown` (methods F2). A frozen read-only worked-example fixture pins the expected verdict. Verdict inputs come ONLY from verified provenance; corroboration is RECOMPUTED from independently-signed sighting records (§6.6), never a self-asserted scalar.

---

## 4. Architecture

```
default -> MaxPressure (+ coordination term, INERT structural zero; not an experiment)     safety floor
   trigger (EV sensed | advance claim | conservation anomaly | incident | conflicting-green)
      -> deterministic gate: Ed25519 auth + registry + conservation/CUSUM + corroboration  <- real-time defence
           (corroboration key-bound + >=2 keys; preemption MAGNITUDE bound to corroborated need; cold-start fail-closed)
           advance-claim ambiguous middle -> disambiguator (rule or SLM Job B, withhold-only)
      -> shield: min/max green, clearance, ANTI-STARVATION (MaxPressureController.step)      safety invariants
   every message + signed sighting + decision(driving_input_seqs:[...]) -> AuditLog (sender-signature + hash-chain)
   batch -> monotonic completeness commitment + first-party per-signer counts
         -> QUORUM external anchor (>=2 witnesses) + named CROSS-AUDITOR (split-view detection)
   incident/stealthy-deviation post-hoc -> verify chain+signatures+quorum-completeness -> EVIDENCE PACK (provenance)
                                        -> SLM Job A internal note (origin + cited law), firewalled + counsel-gated
```

---

## 5. Consistency remediation (D1; gates everything)

1. Full forbidden-term manifest via `grep -rl` (blockchain, liability, verdict, decentralis(z)ed, optimization, Iroha, "EU AI Act", "detectability envelope"/"detectability ROC"). Conform ALL. Prior-work quotations allowed.
2. Pin ONE canonical banner. **Neutralise STSD in-body claims.** A banner ALONE does NOT satisfy D1; the D1 grep runs LAST; a quarantined file must be neutralised OR in the §12 verbatim allowlist.
3. Fix the v1 contradictions (resolved): C2 `colluding-keys` kept; C3 anchor is CT-style + cited; C4 SSOT complete. (C1 policy-exemption is TARGET-STATE, see §5.7 — NOT claimed landed.)
4. Propagate the §6.1 record into `TEST-AUDIT §1.1` (delete `sig_valid:bool`) + conform TEST-AUDIT Part 2's ORIGIN enum to §2 (add `sensor-fed-spoof` + `legitimate`, the E2 direction).
5. **Rewrite the thesis sentence across ALL live docs** to the §1 characterisation framing — `spec.md` §1 overview + SC-001 + SC-005; `TEST-AUDIT §3.3(c)` (→ physical-impact axis); the 05-supervision layer (`PROJECT-STATUS-AND-PLAN.md`, `AKIN-research-survey-and-direction.md`, `FEEDBACK-RESPONSE-MATRIX.md`, `lee-progress-update.html`, **`LEE-business-specification.md`** [carries "detectability envelope":39], **`PIVOT-SIGNOFF-BRIEF.md`** [banner-only over "blockchain"/old thesis]) and `05-supervision/report/edge-negotiator-report.tex`; **`PROJECT-PROPOSAL.md`** ("detectability envelope" own-claim at :57/:82/:160/:186 + old thesis at :19/:33/:45, AND demote its self-declared "canonical/SSOT-for-novelty" designation — MASTER §2 is the SSOT) — OR add dated superseded snapshots to the §12 D1 allowlist with a stated reason. DEMO-REPORT: StubAgent + powered means only. Remove the deleted `PROJECT-DECISION-BRIEF` ref + no-CoT + MaxPressure-selection prompt from `slm_agent.py`.
6. **Rewrite `spec.md` scenario 1 / FR-003/004 / SC-002**: "local sensing is strong but SPOOFABLE; a lone keyless local reading cannot force sustained preemption."
7. **Land the GROUND-RULES §F + TEST-AUDIT amendment** so "a decision citing no policy is a defect" EXEMPTS the SLM Job-A note (non-evidential, carries `cited_rules` not P1-P8): amend GROUND-RULES-POLICY.md:92 + TEST-AUDIT:39/:127 in the overnight doc subset. Until amended, §12 states this as TARGET, NOT "landed" (consistency B-1, the surviving false done-claim).

---

## 6. Security (accountability layer: cryptographic, quorum-anchored + cross-audited, wired, honestly bounded)

1. **Store the signable payload verbatim (NO minimisation of the signed bytes; build FATAL-1).** Message records: `{kind:"message", seq, sender, sender_pubkey_fpr, sender_pubkey_der, signed_payload:{sender,t,payload}, sender_signature(hex)}`. The bus signs `canonical_bytes(sender,t,payload)`; the record stores that EXACT payload + the DER key captured from the registry AT CONSUMPTION (re-registration mutates it; verify against the record's own DER), so `identity.verify(sender_pubkey_der, canonical_bytes(sp["sender"],sp["t"],sp["payload"]), bytes.fromhex(sender_signature))` is recomputable. **GDPR minimisation is handled at the SOURCE** — the signed coordination payload carries ONLY non-personal fields (no raw EV/vehicle identifiers or fine location); personal data never enters the signed message (§7.7). A separate non-signed display field may be minimised further.
2. **Wire from the REAL producer** (build FATAL-2/3/4/5). Inject the `AuditLog` into `CoordinatedController`/`EmergencyController` ctors; lift the no-edit rule for `decide()` and BOTH inbox seams (`CoordinatedController.decide` inbox loop AND `EmergencyController._ingest_ev_inbox`, which currently DISCARDS `.signature`): at each `bus.inbox`/`ev_bus.inbox` consumption append `kind:"message"` (with `sender_signature=m.signature.hex()`, `sender_pubkey_der=registry.public_key(sender)`), capture the returned envelope `seq`, then append `kind:"decision"` emitting `driving_input_seqs:[...]` (the SET of all materially-influencing inputs; build FATAL-4 + security FATAL-1), `junction`, `t`, `executed`, `classification`, `policies_applied` DIRECTLY (not via a lossy remap). DELETE the `_drain_audit` decision-mirroring (keep the registry mirror) so there is ONE decision producer. Wire `EmergencyController` + `ev_bus` + adjacency + a scripted phantom-claim AND real-EV incident into `IntegratedSystem.run()` behind a config flag, so the EV attribution path actually executes. One CLOCK (sim time) for decision.t / message.t / outcome.t (build MAJOR-4).
3. **Verify crypto + quorum-completeness before attributing.** `fault_report(AuditLog, registry_der_dict)` runs `verify_chain()` once + `identity.verify(...)` per causal record + the §6.7 quorum-completeness + cross-audit proof, refusing (`UNKNOWN`/`INCOMPLETE_DISCLOSURE`) on any failure. Name the TWO signature layers distinctly (build MAJOR-5): `_audit_bundle` → `verify_signatures(registry_der_dict)` (ISSUER integrity over the entry hash); `fault_report` → `identity.verify` per causal record's SENDER signature. The tamper test isolates `identity.verify`: rebuild a new `AuditLog` by re-appending `IntegratedSystem.run()`'s entries with ONE `sender_signature` replaced by a valid-form signature from a DIFFERENT `JunctionIdentity` so `verify_chain()==True`; only `identity.verify` rejects (build FATAL-4/ MAJOR-3).
4. **Sensor-spoof vs detector, evidence-derived + honest about the keyless free region** (build MAJOR-2, security FATAL-2). The gate HARD-REFUSES any keyless-forced sustained preemption (a lone keyless local reading may NOT force sustained green); `sensor-fed-spoof` then fires only where a keyless reading nonetheless drove a *transient* effect, computed from records (`ground_truth` SCORING-ONLY). STATE plainly: a keyless physical sensor-spoof that induces a *transient* deviation before refusal is undetectable-live + untraceable (no key) = an IN-SCOPE free-deviation class, listed in §6.11/§13. Drop "local sensing cannot be spoofed."
5. **External identity root + counter-signed rotation** (security FATAL-2 R5). An identity root EXTERNAL to the operator (manufacturer attestation / CA / physically-witnessed ceremony) signs the genesis key→entity binding; rotation requires the incumbent key's counter-signature; recovery from a lost key requires an EXTERNAL quorum, NOT a unilateral operator override. The pack carries enrolment provenance. STATE plainly: this RELOCATES trust to an external party whose mis-enrolment is undetectable in-band; name who certifies/rotates the root's own key; list identity-root compromise in §6.11 AND §13.
6. **Corroboration = key-bound (≥2 keys), coverage-checked, cold-start fail-closed** (security F1/F6). Signed `kind:"sighting" {sighter_key, ev_id, approach, t, position, signature}`; corroboration RECOMPUTED by walking these. Independence checked against a MEASURED, non-attacker-authored position source; absent it, route-consistency is a plausibility filter only (adds no security vs a colluder) — stated. Preemption MAGNITUDE/DURATION bound to the corroborated need (a corroborated EV cannot justify unbounded sustained green; security FATAL-1). Cold-start FAIL-CLOSED: refuse sustained preemption until the corroboration set is warm (persist the replay HWM AND define post-restart warmup for the corroboration gate, not just the detector); TTL affects only the live working set, never the anchored record. Per-claim nonce on `(ev_id, approach, time-window, claimer)`.
7. **Completeness via a QUORUM anchor + a named CROSS-AUDITOR; completeness ≠ integrity** (security F3/F4, novelty M1). Signed monotonic append-only commitment; write `(seq_high, merkle_root)` per batch to ≥2 independent witnesses (e.g. a Rekor transparency-log entry AND a named single-node ledger RPC); a NAMED cross-auditor reads BOTH and surfaces disagreement (split-view detection) — without it, non-equivocation is ASSUMED not enforced, so the cross-audit is a D-anchor acceptance condition (§9 `anchor.md`). Per-signer first-party emission counts instrument the OUT-OF-SCOPE operator boundary ONLY (in scope the operator is honest, so counts add no security); define each mismatch DIRECTION (a lone signer's `claimed>disclosed` cannot by itself attribute suppression to the operator — else a single key frames the honest operator). STATE plainly, inline: this catches operator rollback/selective-disclosure IF cross-audited, but **operator creation-time omission of its OWN never-emitted record, and a k-of-n colluding-key coalition, remain out of reach** — MEASURED as the boundary (§8), not defended. Mock/single-node/un-cross-audited anchor CANNOT back the claim.
8. **Ship the flagged build items:** consecutive-spillback cap forcing a CUSUM window; payload size/field/depth bound at the bus; no preemption during detector warmup (in addition to the corroboration cold-start of §6.6).
9. **Prompt-injection guard, content-vs-provenance** (security S7). Untrusted content enters the SLM prompt only as delimited NON-instruction context; the oracle distinguishes "authentically logged" from "independently corroborated"; attacker free-text may not be a citation's sole support. Honestly bounded: under collusion a corroborating sighting can be an attacker key — stated.
10. **Origin-independent citation selection** (security S4): the pack's failed-checks set + cited-rule selection are a mechanical origin-INDEPENDENT function; a test proves the citation set is not a function of the injected origin.
11. **Honest bound, stated inline in §1/§13** (security F8/ MAJOR-7). The conditional coupling holds against a NON-operator adversary controlling one corridor key, under a non-equivocating cross-audited quorum, adequate honest-sighting coverage, and no operator omission. The IN-SCOPE free-deviation set is essentially {piggyback-inflation on a real corroborated event; keyless-transient sensor-spoof}; the substantive resisted content (corroborated stealthy deviations) requires ≥2 keys = OUT of scope. The measured boundary (colluding keys; keyless sensor-spoof; piggyback; operator omission; identity-root/quorum compromise; witness-density → 0) is the finding — reported, not claimed defended. State the in-scope guarantee's near-vacuity honestly.

---

## 7. Legal framing (split; pack mechanically-verifiable, note counsel-gated; residual limits documented)

1. The EVIDENCE PACK emits only mechanically-true, reproducible facts (verified sender key + enrolment provenance, chain/signature/quorum-completeness/cross-audit results, which corroboration checks failed [mechanical, origin-independent], neutral cited-rule text [mechanical selection]). NO `candidate_origin`, confidence, `fault_weight`, or "attacker"/"lie" label. Header: "Provenance record; not a determination of legal fault." CLOSED forensic channel.
2. `technical_causal_chain` separated from `legal_causation: NOT_ASSESSED`.
3. Every cited rule carries `binding_statute` vs `advisory_highway_code` (+ RTA s.38(7)), source-provenance, an as-at date, the KB caveat verbatim; secondary-sourced/low-confidence rules degraded to "reference only, verify against primary" and excluded from emission. A pinned legal-currency review date + fail-loud staleness gate. Disambiguate "misfeasance" (Stovin positive-act sense) from the tort of misfeasance in public office (bad faith, *Three Rivers No 3* [2003] 2 AC 1).
4. The SLM note is NON-EVIDENTIAL, restricted, firewalled, counsel-gated (the always-on auto-note is NOT privileged — *Waugh v BRB* [1980] AC 521 dominant-purpose; *Three Rivers No 6* [2004] UKHL 48; *SFO v ENRC* [2018] EWCA Civ 2006). No consequential action on the note alone; meaningful human review per Art 22 / SCHUFA C-634/21 (a retained-EU PERSUASIVE authority, not binding post-EU(W)A 2018; cross-check EDPB/ICO guidance). The `attacker-key`/`colluding-keys` enum prejudges as hard as "lie" did — defended only by the note being non-evidential + counsel-gated + on a synthetic substrate; acknowledged in §7.8.
5. Conflicting-green reported as the PHYSICAL fact only ("simultaneous green on conflicting controlled links per `getControlledLinks`"); `legal_causation: NOT_ASSESSED` (*Bird v Pearce* vs *Gorringe*/*Stovin*/*Poole BC v GN* [2019] UKSC 25 is unsettled). **Controller determined FIRST** (UK GDPR Art 4(7)/(8)): TfL is the controller for A501 (TLRN; DPA 2018 s.8 public task); a private edge operator processing on TfL's instructions is a PROCESSOR (Art 28) under TfL's basis — it needs its own Art 6(1)(f) + LIA only if it determines purposes/means (legal M4).
6. Criminal-implying language never appears in an auto-emitted field; the pack DOES process criminal-offence data via RTA s.36 text — reconciled by origin-independent selection, requiring the DPA 2018 Sch 1 condition + an Appropriate Policy Document (Defamation Act 2013 s.1; UK GDPR Art 10). A governmental body cannot sue in defamation (*Derbyshire CC v Times* [1993] AC 534).
7. GDPR: personal data + salt strictly OFF-CHAIN under a bounded retention clock (Art 6(1)(e) is necessity + a legal basis per DPA 2018 s.8, NOT an LIA balancing test); the quorum anchor stores ONLY a Merkle root provably non-re-linkable without the off-chain secret; Art 17(3)(b)/(e); DPIA records residual re-linkability during retention.
8. **Documented residual legal LIMITATIONS (honest, not solved):** curation-of-applicable-rules is an adjudicative act (*Bates v Post Office Ltd (No 6: Horizon Issues)* [2019] EWHC 3408 (QB); criminal side *Hamilton v Post Office* [2021] EWCA Crim 577; reliability presumption *DPP v McKeown* [1997] 1 WLR 295) — mitigated by mechanical origin-independent citation + human assembly, not eliminated; the closed-channel pack attracts only QUALIFIED privilege, defeasible by malice/excess and evaporating on DSAR/litigation disclosure, so the innuendo sting is CONTAINED not eliminated (*Charleston v News Group* [1995] 2 AC 65; *Adam v Ward* [1917] AC 309); the note's privilege is deployment-dependent; the preimage retention-vs-erasure tension is bounded, not resolved. **The dissertation's OWN artifacts (worked-example fixture, confusion matrix) name keys + criminal statutes in the thesis text but concern a SYNTHETIC corridor with no data subject and no defamable claimant — so Art 10 / defamation bite only on the hypothetical deployment, not the thesis** (legal M5).

---

## 8. Experiments (the ONE valid protocol)

**Experiment D (the headline, the measured boundary):**
- Construct a family of **conservation-consistent** deviations (residual below the INDEPENDENTLY-CALIBRATED threshold — set from the honest CUSUM operating point at a NUMERICALLY-PINNED FP budget, WITHOUT reference to the attack set, bound to the same real-calibration gate as inferential claims; report the residual distribution vs that threshold). Parameterise by physical impact (green-time stolen, EV delay). Do NOT claim "recall collapses." Cite + beat FDI prior art; detectability is SETUP.
- **The conditional coupling as a THEOREM with explicit SUFFICIENT hypotheses:** `[coverage > d_crit(footprint)] ∧ [honest independent corroboration keys only] ∧ [non-equivocating cross-audited quorum] ∧ [no operator creation-time omission]` ⇒ every deviation caught live or leaves a quorum+cross-audit-detectable trace. Prove SUFFICIENCY analytically; show NECESSITY empirically by the sweep (not an implicit iff). Independence has a testable predicate over the measured position source; where absent it is ASSUMED not verified — stated.
- **The measured finding = `d_crit`** (novelty F1/F2/F3): measure the critical honest-sighting COVERAGE-density at which the free-region fraction crosses a fixed level, as a function of the real Euston approach-weighted sighting-independence geometry, and report the GAP between measured `d_crit` and the well-mixed prediction; report spatial HETEROGENEITY (local pockets failing at high average density) and the collusion×density INTERACTION. PRE-REGISTER the falsifiable hypothesis (real geometry moves `d_crit` / creates heterogeneity) before any run. Report the free-region as a per-cell SURFACE over a PRE-REGISTERED grid whose axes INCLUDE collusion-degree (k, numerically bounded by the corridor key count + the single-key threat model) and witness-density, with NO aggregate scalar and the impact denominator = the real-geometry physical max (methods M3). Specify which component each deviation defeats.
- **Provenance-indistinguishability (the hard object; methods F1/F2):** PROVE the physical-veracity triad is provenance-indistinguishable (no §6.4 provenance feature discriminates the pair — the proof committed under the pre-registration seal, methods M10); force those cells to `unknown`; score the CONFUSION MATRIX only over the provenance-decidable origins (§2), scoring label severed from engine input, with a pre-registered separable-fraction floor that FAILS D6 if unmet (novelty F5).

**Experiment 1 (Job B classifier), de-circularised:** SUMO-injection ground truth with genuine class overlap; pre-register the noise params. §9 must define an EXPLICIT PARAMETRIC generative `p(x|y)` with closed-form or bounded-error-integrable likelihood (methods M2), so the nonlinear Bayes ceiling is computed ANALYTICALLY where injector density > a numerically-pinned floor; on the low-density/novel tail NO optimal reference exists, so the SLM's novel-split performance is reported WITHOUT a ceiling and cannot be claimed near-optimal there (methods M1). Judge the SLM vs the ceiling in-distribution; pre-register an MDE for the ceiling-minus-SLM gap. Novel/OOD split by a PRE-REGISTERED classifier-independent distance-from-dev-support metric (exact formula + dev-frozen standardisation + fixed-percentile threshold) AND a continuous accuracy-vs-distance curve (no threshold DOF); do not call low-density-in-distribution "OOD". Disjoint dev/test in code; git-hash-SEAL the ENTIRE analysis bundle (noise params, OOD metric+threshold, prompt, MDE) under one hash before any test eval; global eval-counter capped at 1. NO threshold-DEFINING constant in the prompt (asserted across the WHOLE assembled prompt incl `SYSTEM_DISAMBIG` + `_case_evidence`, not just few-shots; a BEHAVIORAL leak test: SLM implied dev boundary coinciding with the rule's box boundary = leak). `real_model=true` hash/quant/version asserted; parse-failures COUNT AS failures + reported jointly with recall + decompose reasoning-on-parseable vs end-to-end; memoise only at 100% agreement. Stats: n≥30; paired deltas; BCa + paired permutation + Holm; a pre-registered numeric MDE justified from an EXTERNAL operational-harm threshold + a pinned conservative SD rule (upper 90% CI of a pilot at n≥20); TOST; null only when CI half-width < MDE. Determinism N≥100.

**Experiment C (frozen-vs-adaptive):** as §3 Job C.

**Pre-registration seal (methods M7):** the one-hash bundle seal + eval-counter cap applies to Exp D and Exp C too (free-region grid bounds + sampling measure, FP budget, separable-fraction floor, `d_crit` level, Job-C mixing weights + threat-model budget, every "numerically-pinned" threshold) — every number is BOUND under the seal before any run; none back-fitted (methods M8).

**Coordination:** NOT an experiment (inert structural zero); state no run can move it; removed from experimental claims.

**Substrate:** Euston (§9); demand calibrated to a NAMED, TIME-RESOLVED source (TfL/DfT hourly profile + turning counts + vehicle mix, not a bare AADT); a hard startup gate for any inferential claim. Every number labelled relative-in-SUMO.

---

## 9. Euston substrate (pre-built, committed, never regenerated at build time)

Literal build parameters in a committed `euston-build.md`: exact bbox lat/lon; the full literal `netconvert` command incl `--tls.guess/--tls.join`; expected TLS + in-edge counts (asserted). Pre-build + COMMIT `euston.net.xml` + `edge_ids.json`; the overnight builder MUST NOT call `osmGet`. `edge_map_from_net(mode="chain")` hard-errors on the non-chain topology; derive from `net.getEdges()`/connections + assert coverage of every TLS-controlled in-edge. Conflicting-green via `getControlledLinks()` + phase state; fixture net with a known conflicting + safe phase. Pin sumocfg collision flags; forced-collision + forced-hard-brake fixtures asserting detection fires at the known tick and nowhere else.

**`ground_rules.yaml` schema:** `{id, source: primary|secondary, statute_ref, binding: MUST|should, text, applies_when, fault_weight}`, `statute_ref` cross-linked to a pinned `uk-traffic-law.md` id format. **Exp-1 injector spec:** an EXPLICIT parametric generative model with a closed-form/bounded-error likelihood per class, feature-vector schema, noise params, label format, target Bayes-error floor, numerically-pinned density floor + integration method. **`anchor.md`:** the concrete ≥2 witnesses (e.g. Rekor + a named single-node ledger RPC), the NAMED cross-auditor + its disagreement-surfacing behaviour, submit/verify calls, fail-loud offline behaviour.

---

## 10. Fail-loud gates (each SCOPE-LOCAL; SKIP-with-record when its precondition is absent)

- Real-model / Corpus / Net gates (SKIP-and-mark).
- **Quorum-anchor gate:** any non-equivocation / conditional-coupling claim requires a real write to ≥2 independent witnesses + a reconciled per-signer count + a passing CROSS-AUDIT; a single-node/un-cross-audited/mock anchor is labelled "not externally anchored" and CANNOT back the claim.
- **Prerequisite-fixture gate:** the E2 worked-example fixture, the forced-skip fixture, and `ground_rules.yaml` must exist AND be git-committed at startup, else the dependent items SKIP-with-record (never self-author — self-authored expected-verdict is tautological; build FATAL-6).
- Per-run: `slm_calls_ok/total` above threshold; the decider on SLM-measured rows is actually `slm`; every attributed record passed signature + chain + quorum-completeness + cross-audit; an incomplete window returns `INCOMPLETE_DISCLOSURE`.
- Doc + corridor work gated on NONE of Foundry/Euston/corpus/anchor.

---

## 11. Build order

**Prerequisite artifacts (pre-built during hardening, NOT overnight-blind):** `euston.net.xml` + `edge_ids.json` + `euston-build.md`; `ground_rules.yaml` + citation oracle; the SUMO-injection labelled dataset + the explicit parametric injector model; the frozen E2 worked-example fixture; the forced-skip traffic fixture; `anchor.md` + a reachable ≥2-witness quorum + cross-auditor.

**Pinned schemas the overnight build MUST use verbatim (supersedes TEST-AUDIT §1.1):**
- message: `{kind:"message", seq, sender, sender_pubkey_fpr, sender_pubkey_der, signed_payload:{sender,t,payload}, sender_signature(hex)}`
- sighting: `{kind:"sighting", seq, sighter_key, ev_id, approach, t, position, signature(hex)}`
- decision: `{kind:"decision", seq, driving_input_seqs:[...], junction, t, executed, classification, policies_applied}`
- registry: `{kind:"registry", seq, entity_id, key_fingerprint, key_der, action:enrol|rotate|revoke, external_root_sig, self_sig, incumbent_countersig?, admin_sig}`
- ORIGIN enum (exact strings): as §2; each with an EVIDENCE-DERIVED (zero-`ground_truth`) trigger predicate + the `ground_truth` field used for SCORING ONLY.

**Honest overnight subset (no Foundry/Euston/OSM/anchor dependence):**
1. Doc remediation over the full §5 manifest (incl §5.5 supervision + LEE-business-spec + PIVOT-brief + PROJECT-PROPOSAL + report.tex, §5.6 spec.md sensing reframe, §5.7 GROUND-RULES §F + TEST-AUDIT amendment); D1 grep runs LAST with the verbatim allowlist.
2. Wire from the real producer: inject `AuditLog` into controller ctors, capture `NeighborMessage` + signature + DER at BOTH inbox seams, emit message/sighting/decision(`driving_input_seqs`) inline, DELETE `_drain_audit` decision-mirror, one clock, wire `EmergencyController` + a phantom+real-EV incident into `IntegratedSystem.run()` behind a flag (build FATAL-2..5).
3. Rewire `fault_attribution`→`assessment`/ORIGIN with pinned enum + evidence-derived (zero-`ground_truth`) predicates; `flatten` lifts `event.*` + preserves `seq` (no field guessing, producer emits the fields); enforce `identity.verify` + `verify_chain` + quorum-completeness before attributing; two named signature layers; the E2 direction + tie-break vs the frozen fixture; the tamper test over `IntegratedSystem.run()`'s rebuilt log. Atomically replace `sig_valid` across module + tests + doc.
4. Fix `slm_agent.py` STATIC edit ONLY: strip the "1.0 = the limit"/"plausible limit of 1.0" phrasing from `SYSTEM_DISAMBIG` AND `_case_evidence` (not just few-shots), qualitativise 0.40/1.80, raise `max_tokens`, drop no-CoT + the deleted-brief ref; add the Job-A output SHAPE (incl `legitimate`). Job-A CONTENT deferred (corpus-gated).
5. Anti-starvation on `MaxPressureController.step` (NO `ShieldController` exists): pin a per-approach skip counter that must REACH `max_skip` (>0, proves the trigger) AND a distinct `anti_starvation_violations` counter that must stay 0; pin the force-serve through the yellow/min_green machine; a COMMITTED forced-skip fixture where the starved approach provably never wins `_pressure` argmax, asserting it IS served within `max_skip` AND `anti_starvation_violations==0` EMERGES (never set by the test) (build FATAL-5/ MAJOR-1).
6. Foundry determinism probe as SKIP-not-abort.

**OUT of the overnight subset:** Euston experiments; Job A/citation-CONTENT runs; SUMO-injection Experiment 1; Experiment D full run + quorum-anchored + cross-audited runs; Experiment C; the 30-seed powered runs; the quorum-anchor + cross-auditor integration. T10/T11 documented-xfail.

---

## 12. Acceptance tests (all binary; all required for distinction)

- D1: full §5 manifest clean; excluded set ⊆ a VERBATIM allowlist (incl LEE-business-spec + PIVOT-brief with a dated reason, or neutralised) AND every non-excluded live doc greps clean; one thesis sentence across live docs. Grep runs last.
- D2: fault language = split; the pack has no verdict/confidence/accusation/"lie"/"attacker"; citation selection origin-INDEPENDENT (proven not a function of injected origin); the note non-evidential + firewalled + counsel-gated.
- D3: `real_model=true` guard ABORTS/SKIPS-with-record, never a null.
- D4: Experiment 1 — CIs + numeric MDE (external-harm-justified) + TOST; sealed analysis bundle + eval-counter=1; classifier-independent OOD metric + continuous accuracy-vs-distance curve; axis-aligned-conjunction ceiling AND analytic nonlinear Bayes ceiling (in-distribution only) with tail-no-ceiling stated; no threshold-defining constant across the whole prompt + behavioral leak test; parse-failures as failures.
- D5: non-circular labels + SLM vs the nonlinear ceiling in-distribution + Job-A vs the graceful-fallback template on citation CORRECTNESS on novel combos + FCR + citation-gold kappa≥0.6.
- **D6 Experiment D (the measured boundary):** the stealthy class on the physical-impact axis (residual vs the numerically-pinned FP-budget threshold); the conditional coupling proven under the FOUR explicit hypotheses; the MEASURED `d_crit` + its gap vs the well-mixed prediction + spatial heterogeneity + the collusion×density interaction over the pre-registered grid (the boundary is the result); the provenance-indistinguishability theorem proven + the confusion matrix scored ONLY over provenance-decidable origins with the separable-fraction floor that can FAIL D6.
- **D-anchor:** completeness written to ≥2 witnesses + a reconciled per-signer count + a PASSING cross-audit; a single-node/un-cross-audited/mock-anchored run labelled "not externally anchored", its coupling claim refused.
- **D-SLM-kill:** the pre-committed kill/demote criterion evaluated + reported; survival is an AND on the load-bearing job (Job A at the pre-registered MDE + FCR ceiling); Job C's arm beats a HARDENED baseline net of adaptivity loss in a common harm currency; the SLM stays in the system regardless.
- D7: `anti_starvation_violations == 0` across the attacked n=30 matrix; the committed forced-skip fixture triggers the override; the skip counter reaches `max_skip` and violations EMERGE 0 (never set by the test).
- D8: honest demand sweep + fairness + worst-case, all modes, one CI method; coordination reported as a structural zero, not an experiment.
- D-sec: fault engine verifies signatures + chain + quorum-completeness + cross-audit before attributing; the framing attack (operator re-registration) defeated by the external identity root + counter-signed rotation; a keyless-local-sensing incident → refused-sustained + `sensor-fed-spoof`-on-transient via the evidence-derived predicate (not `ground_truth`); the tie-break matches the frozen fixture; the pack cites real UK statutes with source-tag + as-at date + the not-a-determination header; **the four measured free-deviation classes (colluding keys, keyless sensor-spoof, piggyback-inflation, operator omission) are demonstrated and reported as the boundary, not defended.**
- **Policy-KPI rule:** the empty-`policies_applied` abort applies to DETERMINISTIC decision records only; the SLM Job-A note is exempt. This exemption is TARGET-STATE until GROUND-RULES §F + TEST-AUDIT are amended (§5.7) — §12 does NOT claim it is already landed (consistency B-1).
- Tests: T1-T9 pass-required; T10/T11 documented-xfail.

---

## 13. The viva sentence this spec must make true

"My contribution is a measured characterisation. I build an on-device trust-preserving emergency-coordination layer — a deterministic gate refusing a compromised insider's uncorroborated preemption, over a Certificate-Transparency-style, quorum-anchored, cross-audited accountability log whose mechanism I credit entirely to prior work. My novel object is the measured map of what that layer can and cannot hold accountable. I prove a conditional coupling: for the bounded class of stealthy conservation-consistent deviations the gate cannot catch live, no deviation is both invisible live and untraceable afterwards — provided honest independent sightings adequately cover its physical footprint, corroboration comes only from honest keys, the anchor is a cross-audited non-equivocating quorum, and the operator does not omit its own record at creation time. Then I measure exactly where those provisos fail: the critical coverage-density d_crit at which the guarantee decays, how the real Euston turning-count geometry moves d_crit away from the well-mixed prediction, and the deviations that escape in scope — piggyback-inflation on a genuinely corroborated emergency, and a keyless physical sensor-spoof — alongside the out-of-scope colluding-key and operator-omission cases. That boundary, quantified on the real corridor against cited UK law, is the finding; I claim no novel primitive and no unconditional guarantee, and I prove that the physical-veracity of a corroborated claim is provenance-indistinguishable so the system never adjudicates it. The evidence pack states only cryptographically-verified, cross-audited facts through a closed forensic channel, selects citations mechanically, and does not adjudicate fault. A frozen on-device SLM is the reasoner-of-record for the map's per-incident origin labels, characterised for citation-faithfulness against statute with a pre-committed kill criterion tied to that one load-bearing job, and I separately measure the robustness-versus-adaptivity tradeoff of freezing it against a hardened continual learner. Coordination and traffic optimisation are not my claims; the measured accountability boundary is." Every clause backed by a run that passed its §12 gate, or it is not settled.
