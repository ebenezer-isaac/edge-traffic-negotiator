# Master-spec hardening log (working file; findings accumulate here, synthesised into MASTER-SPEC each round)

## Round 3 (adversarial battery vs MASTER-SPEC.md)

### Novelty agent (a76324cb) — verdict: NOT distinction-grade as written; recoverable
Ranked findings + fixes:
1. **The "detectability envelope" is a re-labelled detector ROC.** Prior art owns it: Neyman-Pearson (1933) likelihood-ratio OC; Page (1954) CUSUM average-run-length; CPS stealthy-attack characterisation (Teixeira/Sandberg/Johansson Automatica 2015; Mo-Sinopoli 2010; Liu-Ning-Reiter 2011 undetectable FDI; Cardenas-Amin-Sastry 2008). Our own ADVERSARIAL-FAILURE-SYNTHESIS S.4 (Keijzer) names the stealthy-residual limit. Plotting recall vs lie-magnitude = tracing our own threshold. NOT novel as-is.
2. **Envelope is tautological if lie-magnitude is on the residual axis** (residual monotone in m -> "recall rises with m" is by construction; the knee is just where we set the threshold). FIX: define lie-magnitude on the PHYSICAL-IMPACT axis (seconds of green stolen, EV delay induced), and construct conservation-consistent lies that are physically large yet residual-invisible. Promote today's "bounding zeros" (collusion/spillback) to the MAIN measured object.
3. **Commodity composition** = exactly the synthesis's own Stage 1-5 recommended default (Ed25519 + Haber-Stornetta/Schneier-Kelsey hash-chain + conservation if-statement + summariser). Each leg individually unpublishable. FIX: make the thesis the DUALITY (emergent property): real-time-undetectable BUT forensically-attributable. Characterise the stealthy class the gate misses in real time, then prove the log attributes exactly that class post-hoc to a key with cited law. This is the one non-obvious claim the composition owns.
4. **SLM Job A is decorative unless it beats a deterministic rule-to-text TEMPLATE on held-out NOVEL fact-combinations** (open-world statute mapping a finite rule table can't enumerate), scored on faithfulness + coverage. FIX: add the template baseline + novel-combination held-out set; SLM win condition = strictly dominate the template there. Elevate the faithfulness "rule oracle" into the contribution: a formal ENTAILMENT CHECKER over a codified UK-law fragment that REFUSES to ship any evidence pack whose cited rules don't entail its attribution (the verifier + shipping guarantee is the novelty, not the generation). Prior art to cite/beat: Lewis RAG 2020; Dahl/Magesh/Suzgun/Ho "Large Legal Fictions" Stanford 2024; Winfield-Jirotka ethical black box.
5. **Traffic-R1 (arXiv:2508.02344) guts the edge/small/frozen angle** (3B production TSC model, beats GPT-4o). "Frozen = no poisoning surface" is true of any non-continual deployment = not a contribution. FIX: either drop edge/small/frozen as novelty and reposition SLM as a TRANSFER question (can a general SLM given only cited policy match a bespoke production model on narrow disambiguation with zero task-specific training?), OR MEASURE "frozen is safer" by running an actual data-poisoning attack vs a continual-learning baseline and reporting the attack-surface delta.

**Single highest-value change (agent's pick):** redefine the envelope around conservation-consistent stealthy collusive lies on the physical-impact axis, paired with the forensic-attributability duality. Makes the envelope non-tautological, turns composition into an emergent property, gives the log+SLM a unique job, reuses everything built.

DECISION FLAG for user: the duality reframe repositions the novelty claim (additive, consistent with locked constraints). Surface at synthesis.

### Methods agent (a40c1c0d) — §8 still not a valid experiment
- K1 circularity INTACT in design: generate_dataset labels are monotone-threshold functions of the same features the rule reads; the "novel" split has NO region a threshold fails. FIX: label must be non-recoverable from observable features by any monotone threshold; documented class-overlap region, Bayes-error floor >0, report label<->feature mutual information; SUMO injector sets the latent real/phantom flag, a noisy sensing process generates overlapping observables.
- K2 prompt STILL leaks "1.0 = the limit" (slm_agent.py) = the novel-split boundary. FIX: strip it; add an assertion that fails the run if the prompt carries any numeric evidence threshold.
- K3 tuned AND tested on the same 90 cases (no dev/test split in code). FIX: disjoint dev/test seeds in code; evaluate once on test; forbid evaluate over tuning cases.
- K4 real_model=true does not exist; parse-failure -> REJECT so a dead model scores maximally safe (fake-green via the error path). FIX: pin+assert model hash/quant/Foundry version, abort on mismatch; parse_failures a separate exclusion, never a safe default; abort if > threshold.
- K5 evidence-pack faithfulness circular (graded vs a rule = rule-mimicry); citation-correctness is an echo test (RAG injects the same KB); Job A not built (max_tokens=16, no-CoT stance). FIX: grade faithfulness vs STATUTE TEXT by an independent adjudicator; citation vs held-out applicable-rule labels produced without showing the annotator the retrieved set.
- K6 human gold standard hand-waved/infeasible (no annotator, qualification, frame; kappa needs >=2 raters). FIX: name qualified raters + sampling frame + 2-rater protocol + adjudication + report kappa BEFORE comparing the SLM; else drop the kappa claim.
- K7 MDE is words not a computation. FIX: real power calc (alpha, power=0.8, piloted SD, Holm-adjusted alpha, derived n) + a TOST equivalence test + "CI half-width < MDE" as the acceptance gate for declaring a null (else = underpowered, not null).
- K8 coordination contrast confounded (defended - maxpressure_preempt mixes gate+SLM+shield+coord). FIX: toggle ONLY the coordination term with all else fixed; rename the current delta "combined defended-stack effect."
- K9 "demand calibrated where possible" = unfalsifiable escape hatch. FIX: name the count source (TfL/DfT AADT), make calibration a startup gate that aborts or flags "uncalibrated synthetic"; delete "where possible."

### Legal agent (a25128598) — §7 rename is cosmetic; core defect survives
- A1 confidence-scored candidate_attribution + cited statutes = machine adjudication of the ultimate issue; post-Bates(Horizon) unreliable-presumption + no admissibility route; the disclaimer is a label not a control. FIX: REMOVE candidate_attribution / confidence / fault_weight from the emitted EVIDENTIAL object.
- A2 auto "attacker_key" = machine-published criminal accusation -> Defamation Act 2013 + UK GDPR Art 10 (criminal-offence data) + Art 22 (automated decision, SCHUFA C-634/21). FIX: ban attacker/culprit language; neutral provenance only ("claim from key X (entity E) failed corroboration checks P3/P5; uncorroborated"); add Art 10 condition + Art 22 handling; restricted/privileged distribution.
- A3 GDPR Art 17 overclaimed (salted hash re-linkable, Breyer). FIX: anchor the Merkle ROOT ONLY (no per-record personal-data hash on chain); per-record secret salt destroyed on erasure; retain under Art 17(3)(b)/(e) exemption + documented balancing; name the Art 6(1)(e) public-task basis.
- A4 SLM legal prose inadmissible + disclosable weapon + induces automation bias. FIX: class the SLM trace non-evidential/internal, firewalled from the pack AND from the reviewer's FIRST look.
- A5 conflicting-green=misfeasance baked as settled + confidence on unsettled law. FIX: emit factual detection only; legal characterisation NOT_ASSESSED (Bird vs Gorringe/Stovin/Poole unsettled); confidence attaches to detection, never to law.
- A6 HITL does not discharge vs rubber-stamping. FIX: define meaningful review (competent+empowered, sees raw provenance FIRST, records independent reasons, logged override-rate KPI).
- A7 KB mixes primary/secondary + SI-numbering caveats; "hard_law" strips the s.38(7) evidential-only nuance + goes stale. FIX: every emitted citation carries source-provenance (primary/secondary) + as-at date + the KB caveat verbatim; never assert hard_law as a conclusion ("MUST-rule; evidential weight only per RTA s.38(7)"); pinned legal-currency review + fail-loud staleness gate.
- VERDICT: single biggest = STOP emitting machine attribution. Split: (1) evidential asset = reproducible provenance record (which key drove which decision, sig/chain verification, neutral rule text; NO attacker/fault/confidence/legal-conclusion); (2) SLM prose = non-evidential, restricted, firewalled triage note.

### Security agent (a13300f7) — §6/§7 fixes are LARGELY ABSENT in the code, plus a design inversion
- E1 fault_report reads a sig_valid bool and verifies NO crypto (no verify_chain/verify anywhere); _audit_bundle still "comment then return" (never verifies). TEST-AUDIT §1.1 STILL defines sig_valid:bool (spec never propagated its own §6.1 fix). FIX: fault_report takes the AuditLog + registry pubkeys, verify_chain once + identity.verify per causal record, hard-refuse (UNKNOWN + abort) on any failure.
- E2 DESIGN INVERSION: an authentic-but-lying insider is attributed to the AI (SYSTEM_CLASSIFIER), NOT the key, contradicting the spec's own worked example T3 (ATTACKER(J1)). Crypto-verify does NOT fix it (message is genuinely signed; the content is the lie). FIX: a validly-signed uncorroborated claim that caused harm -> ATTACKER(sender_key); SYSTEM_CLASSIFIER only with proof the classifier erred on a WELL-EVIDENCED case vs ground truth. THIS ties to the novelty duality: the authentic lie the gate cannot catch in real time is exactly what the log must attribute to the key.
- E3 attacker picks the verdict branch via self-asserted content (msg=None -> AI; in-band+corr>0 -> collusion; out-of-band -> AI); no profitable branch names the attacker. FIX: verdict inputs ONLY from cryptographically-verified provenance; corroboration RECOMPUTED by the engine from independently-signed sighting records, never read from a scalar the attacker set.
- E4 SENSOR_FED_SPOOF split does not exist; ORIGIN rename never happened (HUMAN_VS_AI still hardcoded); local sensing admitted unconditionally and declared "cannot be spoofed", contradicting §6.4. FIX: add ORIGIN incl SENSOR_FED_SPOOF; require a 2nd independent signal before a lone local sighting forces sustained green; STATE the sensor-spoof-vs-detector-error label is a post-hoc, ground-truth-dependent forensic call, not a runtime attribution.
- E5 no conflicting_green outcome type -> authority/AI fault can NEVER be attributed (falls to UNKNOWN); §11 scopes conflicting-green to log-only. FIX: add a conflicting_green outcome + SYSTEM_MISFEASANCE verdict (low-confidence, Bird-v-Pearce, advisory), wired to the live detector, anchored BEFORE actionable so the authority cannot delete its own liability record.
- E6 indirect prompt injection via attacker-authored audit fields (decision reason, ev_id, payload) steers the evidence pack (a real rule id passes citation-correctness). FIX: segregate untrusted audit/message content as clearly-delimited non-instruction context; add a provenance check to the faithfulness oracle (every asserted fact must trace to a verified audit field).
- E7 single-domain operator can rewrite/rollback/selectively-disclose; verify_chain only proves self-consistency; inclusion proof proves presence NOT completeness; §11 uses a MOCK anchor so current runs have ZERO external binding; §1/§13 say "externally-anchored" without the caveat inline. FIX: scope every headline/viva sentence inline ("tamper-evident vs third parties, NOT the operator/admin-key tier"); gate any "externally-anchored" claim on a REAL anchor write (abort/label if mock); add a signed monotonic append-only completeness commitment (anchored (seq_high, root) per batch) so omission/rollback is externally detectable.
- E8 ROOT: trust root is an UNSIGNED, operator-forgeable registry; replay guard in-memory (cross-restart replay works); no nonce/TTL/freshness/HWM/payload-bound; corroboration = a single signed assertion. FIX: sign+anchor registry events; persist per-sender replay HWM; bound payload size/field/depth at publish; TTL-expire sightings; per-CLAIM nonce; require route/time plausibility before a sighting counts as corroboration.
- SINGLE BIGGEST HOLE: non-repudiation rests on the unsigned-registry + mocked-anchor = exactly the out-of-scope operator/admin-key tier; until the registry is signed-and-anchored and the anchor is real, "non-repudiation to a key" is scoped to an adversary strictly WEAKER than the operator the log is meant to hold accountable.

### CROSS-CUTTING (all four so far)
1. The spec is a BUILD TARGET whose fixes are NOT in the code yet; it must say so explicitly and the acceptance gates must be the mechanism that proves each fix landed (not prose claims).
2. CONVERGENT DESIGN DECISION for the user: stop emitting a machine fault verdict/attribution; SPLIT into (a) a pure, reproducible, cryptographically-verified PROVENANCE record (the evidential asset) and (b) a firewalled, non-evidential SLM triage note. Attribution, where made at all, is to a VERIFIED KEY, computed only from verified provenance, and a validly-signed uncorroborated harmful claim goes to the KEY not the AI.
3. The novelty DUALITY (undetectable-in-real-time but attributable-post-hoc) + E2 (attribute the authentic lie to the key) + the split artifact all point the SAME way = the coherent, defensible thesis.

### Consistency agent (a14287e1) — 4 FATAL contradictions INSIDE the master spec (conforming other docs cannot fix these)
- C1 §5.4 "No SLM decision is a defect for lacking policies" vs §10/§12 "zero decision records with empty policies_applied -> ABORT". Direct self-contradiction. FIX: either every decision (SLM incl.) carries the rule policies that fired, OR exempt SLM Job-A records from the empty-policy gate explicitly + amend GROUND-RULES §F + TEST-SPEC.
- C2 §2 ORIGIN enum {attacker-key/system-classifier/system-detector/sensor-fed-spoof/legitimate/unknown} DROPS OUT_OF_SCOPE_COLLUSION which T10 (TEST-AUDIT §2.4) + §7.4 + fault_attribution.py REQUIRE. FIX: add colluding-keys origin OR map collusion to unknown+key-list and amend T10.
- C3 §1 "externally-anchored" retracted by §6.8 (operator controls anchor), §11 (mock anchor), §13 (silently drops it). FIX: strike "externally-anchored" from §1; "hash-chained, optional operator-controlled anchor (single trust domain)".
- C4 §2 "Single Source of Truth" is INCOMPLETE: no Substrate/map row (Euston cutover unlisted) and omits the 4.63s mean-harm figure it calls canonical in §5.2. FIX: add Substrate row (Euston A501; synthetic=fixture; Lambeth dropped) + Harm-avoided-mean row 4.63 [3.25,5.99].
- Quarantine is a LANDMINE: D1 grep passes stale STSD "blockchain/EU-AI-Act-mandate/60ms/optimization" claims merely because a banner sits above them. FIX: excise/neutralise the in-body claims OR exclude the quarantined file from the D1 grep scope; a banner alone must NOT satisfy D1.
- §6.1 sig_valid ban NOT propagated: TEST-AUDIT §1.1 still defines sig_valid:bool. Aspirational-as-settled: §1(b)/(c),§3,§8 read settled but are unbuilt (§13 admits "not yet met"). Two "novel object" defs (§1 pair vs §8 Exp3-alone). slm_agent still cites the DELETED PROJECT-DECISION-BRIEF + keeps no-CoT + MaxPressure prompt. SC-001 still says "coordinated".
- VERDICT: 4 fatal internal inconsistencies survive even if all other docs conform. The rewrite tightened prose but froze a thesis it admits is unbuilt, on a substrate that does not exist, governed by a table that omits the substrate.

### Build agent (a7681af7) — NOT buildable overnight as written
Missing on disk: euston.net.xml, edge_ids.json, ground_rules.yaml, run_emergency.py, attacks_live.py, run_exploit_defend.py, the real_model pin. foundry probe may hang (~120s).
- F1 (fatal) Exp1 label circularity + its ONLY non-circular source (SUMO injection) is phased out -> no honest green tonight. FIX: declare Exp1-with-real-labels OUT of overnight subset; overnight ship only the rule harness + a Foundry determinism probe, labelled "pilot, synthetic, non-circular labels pending".
- F2 (fatal) §10 global startup gates abort to NOTHING when Euston/Foundry/corpus absent. FIX: make each gate SCOPE-LOCAL to the artifact it protects; add a sanctioned SKIP-not-abort contract (doc/corridor work must not be gated on Foundry/Euston).
- F3 (fatal) Euston OSM recipe under-specified + needs live network + edge_map(mode=chain) HARD-ERRORS on real topology. FIX: pre-build and COMMIT euston.net.xml + edge_ids.json by hand (exact bbox, full netconvert flags, expected TLS/in-edge count); the overnight builder must NEVER call osmGet; else stay on corridor.
- F4 (severe) fault_attribution/AuditLog schema mismatch: AuditLog signature = ISSUER's over the entry hash, NOT the SENDER's message signature §6.1 wants; no kind="message" record is ever emitted; _audit_bundle cross-check is a comment then return. FIX: pin the message-record JSON schema + ORIGIN enum + flattening-adapter signature VERBATIM; add a test running fault_report over a REAL AuditLog that refuses attribution on verify failure.
- F5 (severe) slm_agent contradicts spec both ways (no-CoT + max_tokens=16 + threshold-leaking prompt); Job A evidence-pack + ground_rules KB unbuilt. FIX: build+commit ground_rules.yaml (P1-P8 index cross-linked to uk-traffic-law statute ids) + a citation oracle BEFORE the run; else Job A / Exp4 OUT of subset.
- F6 (severe) consistency remediation is 30-40 files not the ~12 §5 names (blockchain 37, liability 34, verdict 29, decentralis(z)ed ~20, optimization 18, Iroha 18, EU-AI-Act 17). FIX: generate the full `grep -rl` manifest + one canonical banner string; run D1 grep LAST as the gate; allow terms inside quotations of prior work.
- F7 (moderate) T10/T11 both REQUIRED (§11 step7 "A1-A5 + T1-T11") and phased-out (documented-xfail). FIX: state T1-T9 pass-required, T10/T11 documented-xfail, in one place.
- F8 (moderate) exploit-defend runner + NaiveCooperativeController don't exist -> D8 + exploit-defend OUT of overnight subset.
- CORRECTED overnight subset (buildable, no Foundry/Euston/OSM dep): (1) doc remediation over the full manifest, D1 grep last; (2) message-record schema + kind="message" emission in the live seam; (3) rewire fault_attribution to the real AuditLog: rename assessment/ORIGIN, add SENSOR_FED_SPOOF, flattening adapter, enforce identity.verify + verify_chain before attributing, wire the dead _audit_bundle cross-check; (4) fix slm_agent prompt as a STATIC edit (strip threshold + few-shots, raise max_tokens, drop no-CoT); (5) anti-starvation shield invariant + forced-skip unit test; (6) Foundry determinism probe as SKIP-not-abort. OUT: Euston, ground_rules citations, Job A/Exp4, SUMO-injection-labelled Exp1, exploit-defend/D8, 30-seed powered runs.
- Single biggest feasibility risk: F1 (Exp1 label circularity) is a DESIGN dependency, not a missing file; the SLM's only scorable task needs a non-circular labelled set whose sole valid source (SUMO injections) is out of the overnight subset, so no honest headline green tonight.

## ROUND 3 SYNTHESIS -> v2 spec changes (pending user decision on attribution model)
Mechanical v2 fixes (no user decision needed): fix C1-C4 internal contradictions; propagate sig_valid ban to TEST-AUDIT §1.1; add SENSOR_FED_SPOOF + keep collusion origin; mark spec explicitly BUILD-TARGET-not-settled (per-section) and gate "settled" on the acceptance test; make §10 gates scope-local + SKIP-not-abort; replace §11 overnight subset with the build agent's realistic 6-item core + a "prerequisite artifacts to pre-build, never osmGet-at-night" phase; T1-T9 required / T10-T11 xfail; full D1 grep manifest + canonical banner, run last, neutralise STSD in-body claims; pin the message-record schema + adapter verbatim; strip the leaked threshold from slm_agent + methods §8 (Bayes-error-floor>0 label decorrelation, dev/test split in code, real power-calc + TOST + CI-half-width<MDE null gate, single-term coordination toggle, statute-text faithfulness oracle, 2-rater kappa protocol, named demand source); adopt the NOVELTY DUALITY as the lead novel object (physical-impact axis, characterise the stealthy conservation-consistent class the gate cannot catch in real time, prove the log attributes it post-hoc to a verified key).
USER DECISION REQUIRED (restructures §1/§3/§7): the fault-output model (machine attribution/verdict vs pure-provenance-evidence-pack split). Asked -> RESOLVED: user chose SPLIT (pure evidence pack + firewalled note). Also RESOLVED: user chose to adopt the DUALITY as the lead novel object.

## Round 4 (adversarial battery vs MASTER-SPEC v2)

### Legal agent (aee40882) — split helped (A5,A7 cured; A3 mostly; A1 mitigated) but 3 FATAL remain
- FATAL-1 defamation-by-innuendo + UK GDPR Art 10: the "pure" pack juxtaposes "corroboration checks P3/P5 FAILED" + curated criminal-statute text (RTA s.36 = criminal) + a key the registry resolves to an entity -> a reasonable reader takes "entity E committed a criminal offence"; a key is a pseudonym = still personal data (Breyer), so "attribution to a key not a person" is legally hollow; the header disclaimer does not change the meaning taken. FIX: strip ALL curated law + the "which-checks-failed" characterisation from the SHAREABLE pack; ship only independently re-verifiable RAW crypto (signed bytes, pubkeys, tooling), assert nothing; OR confine the pack to a closed forensic/legal channel (qualified privilege).
- FATAL-2 the internal note is DISCLOSABLE anyway: an auto-generated operational triage note produced every incident fails the litigation-privilege dominant-purpose test (Waugh, SFO v ENRC) and is not legal-advice privileged; disclosable in litigation + on a DSAR (Art 15). The "firewall" controls internal sequencing, not discoverability. FIX: do NOT persist an auto attribution conclusion at all, or generate it only under genuine litigation instruction via counsel; drop the criminal-implying attacker-key enum from the note.
- FATAL-3 (TOUCHES LOCKED DECISIONS - CONSULT USER) the DUALITY relocates adjudication under a new name: §1/§13 claim the log "attributes the RESPONSIBLE signing key" and E2 hardcodes signed-uncorroborated-harm -> attacker-key = a machine causal-fault determination (SCHUFA: a preparatory automated step IS the Art 22 decision). FIX proposed: change the thesis verb from "attributes the responsible key" to "produces a tamper-evident, re-verifiable record FROM WHICH a human inquiry MAY attribute"; the system links inputs to decisions, it does not name a culprit. NOTE: this softens the attribution the user just locked (duality + split); the security agent pulls the OPPOSITE way (attribute-to-key). RECONCILE at synthesis + put to the user.
- MAJOR A1-residual: machine "verification result / which checks failed" asserted as reliable fact with no admissibility route (post-Bates). FIX: ship raw bytes + pubkeys + tooling so any party re-computes; the pack asserts nothing (provenance = reproducibility, not conclusion).
- MAJOR A6: meaningful-review is aspirational + the reviewer works from a machine-selected record set / machine-chosen checks / machine-curated law = decision frame pre-set. FIX: reviewer assembles their own record set; note withheld unless requested; override-rate externally audited, no target; no consequential action traceable to the note.
- MAJOR "curation IS adjudication": choosing which rules are applicable + reporting which checks failed ARE the adjudicative acts the split claims to remove; RTA s.36 text in a "pure" pack is the tell. FIX: separate a neutral UNSELECTED rule library from the record; the human picks applicable law.
- MINOR A3-residual: Art 6(1)(e) public-task basis fits TfL but NOT a private operator (needs Art 6(1)(f)+balancing); Merkle-leaf erasure vs committed root needs high-entropy secret salt truly destroyed. MINOR A5-label: keep detection physical ("simultaneous green on conflicting links per getControlledLinks"), not "conflicting-green/misfeasance".
- COUNTS: FATAL 3, MAJOR 3, MINOR 2. Verdict: NOT legally survivable as written for an auto-emitted shareable/court-facing artifact. Becomes survivable with: (a) strip curated law + characterisation from the pack -> raw re-verifiable crypto only; (b) don't persist an auto attribution conclusion (or only under privilege), closed channel; (c) reframe thesis "attributes" -> "produces a re-verifiable record enabling a human inquiry".

### Methods agent (ab0ed3a0) — §8/§3 v2 NOT yet valid; the DUALITY reintroduces the tautology (FATAL 5, MAJOR 10)
- FATAL-1 (TOUCHES LOCKED DUALITY - CONSULT) Experiment D "gate recall COLLAPSES on the physical-impact axis" is tautological: the gate detects on conservation residual; you DEFINE the lie family as residual~0; so "recall floored at residual~0" is by construction, no collapse to measure. It is also the prior-art-owned stealthy-detectability half (Teixeira/Sandberg 2015, Mo-Sinopoli 2010, Liu-Ning-Reiter FDI; our own SYNTHESIS S.4). FIX: stop claiming "recall collapses"; state "there exists a lie family whose residual stays below the INDEPENDENTLY-CALIBRATED detection threshold (report residual distribution vs that threshold + the FP budget that sets it) yet whose physical impact is unbounded"; LEAD with the attribution half; detectability half = setup, cite+beat the prior art.
- FATAL-2 (TOUCHES LOCKED DUALITY - CONSULT) D6 "attribution rate to the correct KEY" measures the TRIVIAL part (signature lookup ~100% by construction, not reasoning). FIX: D6 primary metric = ORIGIN-classification accuracy vs the injected latent cause, FULL CONFUSION MATRIX over the ontology, foreground the hard confusions (authentic-lie vs honest sensor/detector error); report crypto attribute-to-key separately as a ~100% verification check, explicitly NOT the contribution. [Converges with the legal agent: the real object is the origin classification enabling a human inquiry, not a machine key-verdict.]
- FATAL-3 Experiment 1 de-circularisation rigs generator+split for the SLM: (a) "label non-recoverable by any MONOTONE threshold" + a non-monotone SLM pre-ordains the SLM win; Bayes-floor/MI do not bound the monotone-vs-nonlinear gap; (b) the novel split is authored "where a reasoner could plausibly generalise" = SLM-favourable, assigned by the experimenter's own rule; (c) "monotone threshold" is the WRONG baseline class - rule_disambiguator is an axis-aligned CONJUNCTION (box), strictly stronger. FIX: report the accuracy ceiling of (i) the baseline's exact class (axis-aligned conjunctions, dev-tuned) and (ii) the unrestricted Bayes-optimal NONLINEAR classifier; judge the SLM vs the nonlinear ceiling (margin is then visibly a fraction of a built-in ceiling); define the novel split by a classifier-INDEPENDENT OOD criterion (distance from dev support), pre-registered; pre-register noise params before any SLM result.
- FATAL-4 UNFALSIFIABLE: §7/§12 make every outcome a contribution (SLM wins -> SLM headline; null -> coordination headline); no result retires the SLM. FIX: pre-commit an SLM KILL criterion (e.g. if the SLM neither beats the template on Job-A faithfulness+coverage on settled-law novel cases NOR shows below-MDE equivalence at an acceptable false-citation rate, the SLM is cut). State before the run. [NOTE: "cut the SLM" collides with SLM-mandatory lock; the kill criterion instead demotes the SLM's ROLE/claim, not its presence - CONSULT user on wording.]
- FATAL-5 Job-A template comparison guaranteed by construction (table template has zero coverage on truly-novel combos -> SLM "dominates" for free) + coverage-as-presence lets a confident WRONG citation win (Dahl et al 2024 legal hallucination). FIX: score coverage by citation CORRECTNESS not presence; give the template a graceful "nearest rule + no exact match" fallback; report the SLM false-citation rate on novel combos as a PRIMARY number (a distinction result may be "honest template abstention beats confident SLM hallucination").
- MAJOR (10): M1 no-threshold assertion catches numerals only, labelled few-shots + comparative bands still leak the boundary -> ban labelled few-shots, independent reviewer confirms no leak. M2 "evaluate once" unenforceable -> git-hash-seal test labels, log+flag every test eval, cap dev prompt revisions. M3 parse-failure exclusion biases up + always-reject games false-preemption -> count parse-failure as classification failure (primary), report FP jointly with recall. M4 kappa/faithfulness infeasible as written + ban LLM-as-judge for legal faithfulness (circular) -> name qualified human raters + frame + adjudication, restrict to settled-law, gate on kappa>=0.6 else NOT_ASSESSED. M5 no numeric MDE/TOST margin pre-registered + pilot SD from n<=3 -> pre-register numeric MDE (seconds EV delay / FP%) + conservative SD. M6 coordination toggle on an INERT term = structural zero, not an empirical null -> say "no active term to measure", measuring it needs a live term = a different system than 31/16/8. M7 AADT is a daily total not a time-resolved profile -> name the hourly/turning/mix sources + method; hard gate for inferential claims. M8 Exp-D region size is a free knob (threshold + impact-axis max are experimenter-set) -> fix threshold at its calibrated FP budget, bound impact axis by physical max, report region as a FRACTION of the physically-realizable attack space. M9 Exp-D conflates the conservation detector with the FULL gate (conservation+CUSUM+corroboration) -> a conservation-consistent lie may still fail corroboration; specify which component each lie defeats, prove the UNION of all live checks has recall below a stated bound (a lie caught by corroboration is NOT in the undetectable class). M10 memoisation launders nondeterminism -> memoise only at 100% agreement; below that report accuracy as a distribution.
- COUNTS: FATAL 5, MAJOR 10. Verdict: §8/§3 NOT valid as written; v2 retired the crudest K1-K9 forms but Exp D reintroduces the tautology + leads with the prior-art-owned/trivial halves, and Exp1/Job-A embed generator+split rigging that pre-ordain the SLM. Fix the 5 FATALs (reframe Exp D around origin-classification with a calibrated threshold; report the nonlinear Bayes ceiling + a classifier-independent novel split; score Job-A on citation correctness with a pre-committed SLM-kill/demote criterion) -> defensible.

### Novelty/examiner agent (a1e59b4f) — the DUALITY is not distinction-grade as written (FATAL 3, MAJOR 3)
- FATAL-1 (TOUCHES LOCKED DUALITY - CONSULT) The attribution half is TRIVIAL non-repudiation dressed as forensics. Attributing a signed message to its key is what a signature IS; every logged message is signed, so D6 attribution-rate ~1.0 by construction. The non-trivial act (establishing the signed claim was a LIE) is done by external ground truth (§6.4/E4 concedes this). The two halves share no mechanism (lie invisible because residual~0; attribution works because Ed25519 signed it) = orthogonal facts stapled, not an emergent property. Prior art: ISO 13888 non-repudiation; accountability formalised by Kusters-Truderung-Vogt CCS 2010, PeerReview (Haeberlen SOSP 2007). FIX: reframe half-B as a NON-EQUIVOCATION / COMPLETENESS guarantee, and make the emergent claim the COUPLING: the same corroborating sightings the operator must SUPPRESS to hide the lie are the physical signals that could have caught it LIVE. That coupling is the one non-obvious link the composition can own.
- FATAL-2 (TOUCHES LOCKED DUALITY - CONSULT) "real-time-undetectable but post-hoc-attributable" is textbook accountability + FDI, not a new object. Prior art: Haber-Stornetta 1991 hash-chain; Schneier-Kelsey 1999 audit logs surviving compromise; Kusters accountability; PeerReview; stealthy half = Liu-Ning-Reiter 2011, Teixeira-Sandberg 2015, Mo-Sinopoli 2010 (our own S.3/S.4 already name these). The ONE defensible increment: not the mechanism but the DOMAIN INSTANTIATION + quantified map = first characterisation/measurement of the physically-large-but-conservation-invisible insider-preemption class on a real signalised corridor tied to cited UK statute. Genuine wrinkle vs PeerReview: the lie is protocol-VALID (conservation-consistent) so accountability-by-replay returns "no fault" (which is exactly why attribution needs external ground truth). FIX: demote "duality" from novel MECHANISM to novel DOMAIN-MAP; stop calling it "the lead novel object", call it "first measured instance of X in domain Y".
- FATAL-3 THREAT-MODEL MISMATCH: the attribution guarantee EXCLUDES the very adversary the gap names. Synthesis motivates an authenticated-but-lying INSIDER / trusted coordination peer (S.1/S.3), but §2/§6.10/E7 scope tamper-evidence to third parties only, NOT the operator/admin-key tier, and §11 keeps the anchor MOCK for all runs. So every actual run has zero external binding; the guarantee holds only against an adversary strictly weaker than the operator. FIX: ship a REAL anchor (not mock) + bind the operator via non-equivocation, OR restrict the threat model to a non-operator insider stated inline in §1/§13.
- MAJOR: (M4) mandatory SLM is NOT load-bearing to the headline (gate does real-time defence; attribution recomputed by engine; pack has no SLM output; Job-A firewalled/non-evidential) -> give it an evidential role that survives the firewall, OR run data-poisoning vs a continual-learning baseline to MEASURE "frozen is safer" (currently asserted, absent from §8), OR honestly downgrade. Prior art: Traffic-R1 arXiv 2508.02344, Dahl et al Stanford 2024. (M5) the split heals the legal defect by SEVERING the value chain: pack = consequential but standard primitives; note = novel-ish but non-evidential; the firewall that saves the legal case prevents the two halves combining -> locate ONE property both novel AND evidential, put it in the pack (the non-equivocation coupling is the only candidate). (M6) nothing distinction-critical is built/green: Exp D, Exp 1, Exp 2, Job-A citation all OUT of overnight subset; a spec is not a result.
- SINGLE CHANGE THAT MOST RAISES THE CEILING (examiner's own words): replace "attributes to a verified signing key" with a REAL-ANCHORED NON-EQUIVOCATION / COMPLETENESS guarantee that BINDS THE OPERATOR, and make the emergent claim the coupling (suppress-to-hide == could-have-caught-live). This one move (a) converts the trivial attribution half into the genuinely hard property, (b) closes the biggest security hole E7/E8, (c) aligns the guarantee with the operator-adjacent insider the synthesis motivates, (d) gives a non-obvious emergent link, (e) puts a novel-AND-evidential property into the pure pack (heals the split). Requires a REAL external anchor instead of the mock. That scope decision is the true gate on distinction. COUNTS: FATAL 3, MAJOR 3.

### Security agent (a3548fd7) — attribution-to-key does NOT survive as the headline (FATAL 2, MAJOR 5, MINOR 1)
- FATAL-1 (TOUCHES LOCKED DUALITY - CONSULT) SELF-CORROBORATING sensor-spoof defeats key-attribution of the headline stealthy lie: attacker signs an in-band conservation-consistent claim AND spoofs the local sensor the gate treats as "independent" corroboration (§6.4 only needs "a second independent signal"); gate passes; post-hoc the corroboration source is a KEYLESS sensor reading so it attributes to no key. §6.4 itself concedes sensor-fed-spoof is a post-hoc ground-truth call. So the corroborating half of the very class the duality headlines has no key. FIX: restrict the attributability claim to lies whose CORROBORATION is entirely key-bound; require any signal that forces sustained preemption to be an attributable signed record; mark keyless-corroborated preemptions "corroboration not attributable".
- FATAL-2 (wholly UNADDRESSED in v2 - CONSULT) FRAME AN HONEST KEY via operator-controlled re-registration: registry.register permits re-registration after revoke with a NEW key (registry.py:72-73) and registry events are admin-key-signed only (single trust domain). An operator-adjacent attacker binds an attacker-held key under honest junction J's id, signs the lie; post-hoc attribution + the pack's key->entity map (§7.2) resolve to honest J while verify_chain/verify_signatures/completeness all stay green. Conflates key-that-signed with entity-that-controls-key. FIX: enrolment ceremony where each junction self-signs its key; rotation/re-registration requires the incumbent key's counter-signature (or a quorum external to the operator); the pack carries enrolment provenance not just current registry state.
- MAJOR: (S3) headline §1/§13 "attributes the responsible key" lacks the §6.10 operator/selective-disclosure caveat inline -> propagate verbatim. (S4) evidence-pack citation-SELECTION is a covert origin label: failed-checks set + cited-rule selection is a near-deterministic function of candidate_origin, so a reviewer reconstructs origin from the "pure" pack, defeating the automation-bias control (D2) -> cite mechanically & origin-independently (every rule touching any present field) or prove citation set is not a function of origin. (S5) E1 fix NOT self-verifiable: §6.1 stores signed_bytes_HASH but §6.3 verify needs the signed PREIMAGE; record lists neither t nor payload -> store canonical signable content (or t+payload) so signed_bytes is recomputable. (S6) corroboration-recompute (§6.6) has no schema to walk: §6.2 pins only kind:message/decision, not a signed per-sighting record with its origin key -> add signed kind:"sighting" record. (S7) prompt-injection oracle checks PROVENANCE not TRUTH: an attacker's own authentically-signed free-text reason/ev_id IS a "verified field" whose content is a lie -> distinguish "field authentically logged" from "content independently corroborated"; forbid attacker-authored free-text as a citation's sole support.
- MINOR: (S8) piggyback corroboration on a real EV's ev_id -> bind nonce to (ev_id, approach, time-window, claimer) + route-consistency. Verdict: "attributed post-hoc to a verified key" survives ONLY inside the explicitly-scoped adversary-weaker-than-operator sub-model AND only for lies whose corroboration is entirely key-bound (spec does not restrict to that). Two things break it within intent: keyless self-corroboration + operator-forgeable key->entity binding. Biggest residual hole = the TRUST ROOT (admin-key-signed registry permitting unilateral re-registration; anchor still mock).

### Build-feasibility agent (a76be160) — full §11 subset NOT unattended-safe tonight (FATAL 3, MAJOR 7)
- FATAL-1 message/decision record schema NOT pinned & self-contradictory: §6.1 says {sender, sender_pubkey_fpr, signed_bytes_hash, sender_signature}; TEST-AUDIT §1.1 (still canonical, line 18) says {channel, payload_hash, sig_valid:bool, ...} (banned bool + different names); §5.4 admits propagation is TODO. FIX: freeze ONE verbatim JSON schema (names/types/enums), delete sig_valid from §1.1 in the same edit, reconcile sender_pubkey_fpr vs key_fingerprint.
- FATAL-2 flattening-adapter signature + real-log link semantics undefined; adapter absent & dead-tested: real AuditLog nests payload under `event`, but fault_attribution reads flat top-level e["kind"]/e["seq"] -> over a real log every read is None -> everything degrades to UNKNOWN; tests use FLAT hand dicts so the real-log path is never exercised (silent fake-green in the accountability layer). FIX: pin flatten(entries)->list[dict] mapping event.* up + preserving envelope seq; define driving_input_seq over a real log; require the item-3 test to run fault_report over a CONSTRUCTED real AuditLog and refuse on a tampered signature.
- FATAL-3 §3 E2 rule CONTRADICTS §6.4/_catchable_phantom for the zero-corroboration case: §3 says signed+uncorroborated+harmful -> attacker-key; §6.4 + code say signed+zero-corroboration+in-band -> system-classifier (catchable phantom). Same input, two origins. FIX: state which wins (recommend signed+uncorroborated+harmful -> attacker-key; system-classifier only when AI erred on a WELL-EVIDENCED case vs ground truth) and rewrite _catchable_phantom so D-sec and the T3 worked example agree.
- MAJOR: (B1) ORIGIN enum string form ambiguous + sensor-fed-spoof has no constant/trigger/test -> pin exact string values + a sensor-fed-spoof predicate + the ground_truth field. (B2) item-2 live seam cannot populate driving_input_seq without editing the forbidden controller -> log each bus msg as kind:"message" in _drain_audit + stamp driving seq, or lift the no-edit rule for this hook. (B3) anti-starvation invariant/counter/test unpinned + no mechanism exists (only min_green) yet D7 asserts anti_starvation_violations==0 -> pin the counter, max-skip bound, hook point, forced-skip test. (B4) item-4 conflates safe static prompt edit with Job-A output needing the absent corpus + "no numeric threshold" assertion will false-fire on legit evidence numbers + few-shot residuals (0.40/1.80) still leak a soft threshold -> split the item, assert "no threshold-DEFINING constant" not "no numeric", strip/qualitativise few-shot numbers. (B5) D-sec statute citations need ground_rules.yaml (out of subset) + §6.7 completeness commitment is a NEW crypto scheme not a wire-up -> mark citation half deferred, scope §6.7 separately. (B6) prerequisite-artifacts phase under-specified: §9 says "pin bbox/netconvert flags/TLS count" but gives NONE; ground_rules.yaml schema never written; injector noise model/feature schema/label format/Bayes target absent -> fill in literal values before the phase runs. (B7) sig_valid ban not yet landed in code/tests/doc -> item 3 must rewrite module+tests+doc atomically. Single biggest risk: record + flattening shapes specified in conflicting prose and never pinned as one verbatim schema -> unattended model emits records fault_report cannot read over a real AuditLog and proves them green on synthetic dicts.

### Consistency agent (a643444c) — round-3 C1-C4 all fixed; v2 introduced N1 (FATAL 1, MAJOR 3, MINOR 2)
- All four round-3 FATALs C1-C4 RESOLVED in v2 text; remaining round-3 items (sig_valid, PROJECT-DECISION-BRIEF, SC-001) honestly reframed as BUILD-TARGET remediation, not settled.
- N1 FATAL: §3 Job-A candidate_origin enum lists only 6 values - `legitimate` is MISSING - while SSOT (:46) says the 7-value ontology "lives in the internal note". Mirror of the fatal C2. Without it the note cannot express "flagged event was actually legitimate" and mis-routes to unknown. FIX: add `legitimate` to the §3 enum.
- N2 MAJOR: §12 Policy-KPI rule states the Job-A exemption is "Stated in GROUND-RULES §F + TEST-SPEC" (present tense) but GROUND-RULES-POLICY.md:92 still says flatly a decision citing no policy is a defect "(rule or SLM)" and §5 never schedules the amendment -> add the amendment to §5, or reword §12 to "must be stated in" (target not fact).
- N3 MAJOR: the detectability->physical-impact reframe is NOT propagated: spec.md:85 SC-005 still says "detectability envelope"; TEST-AUDIT §3.3(c) still sweeps "lie size as multiples of the band, the detectability envelope" (the tautological residual axis). Neither term is in the D1 forbidden-term manifest, no gate catches it -> add SC-005 + TEST-AUDIT §3.3 to §5 (physical-impact axis) or add "detectability envelope/ROC" to the D1 manifest.
- N4 MAJOR: spec.md:20 still leads "the headline value is coordinated emergency response", contradicting §1/§13 "coordination is not my claim; the duality is"; the D1 one-thesis-sentence test would fail but §5 gives it no target edit -> add a §5 item rewriting spec.md §1 overview + SC-005 to the duality thesis.
- N5/N6 MINOR: TEST-AUDIT Part 2 still uses old enum constants + no sensor-fed-spoof (extend §5.4); STSD SSOT row over-states "neutralised" as done while coursework.md still carries live 60ms/blockchain/EU-AI-Act/decentralised-optimisation text -> reword SSOT cell to target-state.

## ROUND 4 TOTAL: FATAL 17, MAJOR 31, MINOR 5. NOT bulletproof.
## CONVERGENCE (4 of 6 agents - novelty, methods, legal, security - independently hit the SAME wound):
The duality's second half "attributes to a verified KEY" is (novelty) trivial 1980s non-repudiation / prior-art accountability, (methods) a trivial signature lookup ~1.0 by construction not the hard origin classification, (legal) machine adjudication that should soften to "a re-verifiable record a human may attribute from", (security) broken by keyless self-corroboration AND operator key-re-registration against the exact operator-adjacent insider the threat model names. ALL converge on ONE keystone reframe: replace "attribute to a key" with a REAL-ANCHORED NON-EQUIVOCATION / COMPLETENESS guarantee that BINDS THE OPERATOR, whose emergent claim is the COUPLING (the corroborating sightings the operator must suppress to hide the lie are the same physical signals that could have caught it live). This ONE move collapses novelty-F1/F2/F3 + methods-F1/F2 + legal-F3 + security-F1(partial) at once and is the true gate on distinction-vs-merit. It REQUIRES a real external anchor (not the mock Besu) = a scope decision, and it repositions the locked duality wording = both are USER-LOCKED decisions -> CONSULT before applying. Everything else in round 4 is a no-regret correctness fix (schema pinning, enum values, tie-break rules, citation-correctness scoring, nonlinear ceiling + OOD split, SLM kill-criterion-as-demote (SLM stays mandatory, only its CLAIM is gated), sighting-record schema, signature preimage, operator caveat propagation, detectability-envelope purge from spec.md/TEST-AUDIT) -> apply autonomously in v3.

---

# ROUND 5 (vs MASTER-SPEC v3, 2026-07-21) — battery attacking the keystone reframe

### Legal agent (aed84484) — v3 NOT legally survivable as an auto-emitted court-facing artifact (FATAL 3, MAJOR 5, MINOR 2)
- FATAL-1 (PRODUCT TENSION - CONSULT USER) the "pure" pack STILL accuses by INNUENDO: naming a verified sender key + enrolment provenance (key->entity) + WHICH corroboration checks failed + curated criminal-statute text (RTA s.36) lets a reasonable reader assemble "this entity's key lied/offended". Removing the `attacker` label + origin verdict does not remove the STING; origin-independent citation (S4) defeats reconstruction but not the defamatory meaning; a header disclaimer does not cure under bane-and-antidote. Authority: Defamation Act 2013 s.1(1)/(2); Tolley v Fry [1931] AC 333 (innuendo); Charleston v News Group [1995] 2 AC 65 (disclaimer inadequacy). FIX (agent's): shareable pack ships ONLY independently re-verifiable raw crypto (signed preimage bytes, pubkeys, verification tooling) + an UNSELECTED rule library; it asserts provenance by reproducibility, states NO negative finding about any key. [TENSION: this strips the law-CITATION the user/Lee/Seb explicitly wanted from the shareable pack; law-selection moves to the human/counsel-gated stage. Genuine deliverable question -> ask user how minimal the auto-emitted pack should be.]
- FATAL-2 (TOUCHES SLM OPERATION - CONSULT) the internal note is NOT privileged: an always-on auto-generated note fails BOTH litigation-privilege limbs (litigation not in reasonable contemplation at generation; dominant purpose is operational triage not litigation) and legal-advice privilege (no lawyer-client advice). So the machine `candidate_origin: attacker-key` verdict is DISCOVERABLE + answerable on a DSAR; the firewall controls viewing order + automation bias, NOT discoverability. Authority: Waugh v BRB [1980] AC 521 (dominant purpose); Three Rivers No 6 [2005] UKHL 48; SFO v ENRC [2018] EWCA Civ 2006. FIX (agent's): do NOT auto-persist an attribution conclusion; generate candidate_origin only under genuine counsel instruction once adversarial litigation really contemplated; drop the criminal-implying `attacker-key` enum from any AUTO-generated artifact. [TENSION: changes SLM Job A from run-every-incident-and-persist to counsel-gated; SLM stays mandatory + still does Job A, only WHEN changes -> confirm with user.]
- FATAL-3 (CONSULT) machine-emitted + machine-selected criminal-law record = Art 22 automated decision + unlawful Art 10 processing. Even origin-independent, the machine-curated record set + machine-chosen checks + machine-selected law leaves the human a pre-set frame, so "meaningful human review" not made out (SCHUFA C-634/21: a preparatory automated step relied on downstream IS the Art 22(1) decision). Emitting RTA s.36 against a key that enrolment provenance re-links to an entity = criminal-offence data (Breyer C-582/14: pseudonymous-but-re-linkable = personal). Spec cites Art 6(1)(e) but is silent on the DPA 2018 Sch 1 condition + Appropriate Policy Document that Art 10 requires. FIX: confine the pack to a CLOSED forensic channel, no auto criminal-law selection; add DPA 2018 Sch 1 condition + APD; document Art 22 safeguards with the human assembling their own record set.
- MAJOR: (M1) CURATION IS ADJUDICATION - choosing which rules are applicable + which checks failed ARE the adjudicative acts the split claims to remove; origin-independence cures automation-bias, not adjudication-by-selection -> ship a full UNSELECTED neutral rule library, human picks applicable law. Authority: R v Bates (Post Office Horizon) reliability line. (M2) immutable Merkle-root anchor is in TENSION with Art 17 DURING the retention window: root + retained salt/preimage is re-linkable = personal + Art-10 data committed to a store the operator cannot rewrite; salt-destruction erasure only bites at end-of-retention -> keep ALL personal data + salt strictly off-chain with a bounded retention clock, anchor a root provably non-re-linkable without the off-chain secret, verify the preimage never enters the committed leaf identifiably. (M3) storing preimage {sender,t,payload} PERSISTS new personal data the digest-only design did not (payloads may carry EV routing/location -> maybe Art 9) -> bounded retention + encrypt at rest + Art 9 assessment in the DPIA. (M4) legal basis + controller under-specified for anyone but TfL: Art 6(1)(e) fits TfL (public authority; A501 is TLRN so TfL is the correct signal authority + misfeasance defendant per Bird v Pearce) but NOT a private edge-layer vendor (needs 6(1)(f) + LIA; a public authority cannot use 6(1)(f) for its tasks) -> name the controller explicitly. (M5) (TOUCHES HEADLINE WORDING - CONSULT) the "no-free-LIE" headline PRE-LABELS the traced act a LIE, but the project's own 7-way confusion matrix exists BECAUSE authentic-lie vs honest-sensor-error vs detector-error are confusable; the coupling proves traceability of SUPPRESSION not dishonest intent; calling it a "lie" prejudges the dishonesty/ultimate issue -> reword the headline object from "lie" to "conservation-consistent deviation" / "unattributed-but-traced deviation"; reserve "lie"/dishonesty for a human inquiry's finding. Authority: Defamation Act 2013 s.1; malicious falsehood (1952 s.3).
- MINOR: (m1) rename court-facing "conflicting-green detector" to the physical fact only ("simultaneous green on conflicting controlled links per getControlledLinks"); add Poole BC v GN [2019] UKSC 25 to uk-traffic-law.md. (m2) auto-emitted "neutral cited-rule text" asserts law the KB itself flags uncertain (secondary sources, SI-numbering caveats, authority-liability least-settled) -> degrade any secondary-sourced/low-confidence rule to "reference only, verify against primary" + exclude from shareable emission.
- VERDICT: NOT survivable as an auto-emitted court-facing artifact. Becomes survivable only if: shareable pack = raw re-verifiable crypto + UNSELECTED rule library asserting nothing; NO auto-persisted attribution conclusion (counsel-gated); closed forensic/privileged channel not "shareable"; controller fixed as TfL + DPA Sch 1 + APD (or 6(1)(f)+LIA for a private operator).
- NOTE FOR SYNTHESIS: F1/F2 + M5 touch USER-LOCKED decisions (SPLIT pack purpose = law-cited & shareable; SLM Job A cadence; "no-free-lie" headline wording). The MINIMAL-PACK direction strips the law-citation the user/Lee/Seb wanted from the SHAREABLE artifact. -> put a single scope question to the user at R5 synthesis; do NOT unilaterally gut the pack. M2/M3/M4/m1/m2 are no-regret (apply in v4).

### Novelty agent (acac9346) — v3 caps at MERIT not distinction (FATAL 4, MAJOR 5)
- FATAL-1 the COUPLING is a definitional TAUTOLOGY not an emergent property: §6.6 defines BOTH live-corroboration and post-hoc-corroboration as a function of the SAME signed-sighting object, so "the evidence you must suppress to hide it is the evidence that caught it live" = reading one variable twice, not a property of traffic physics. Prior art shape: PeerReview (Haeberlen SOSP 2007). FIX: either PROVE the live physical corroboration signal and the anchored audit record are physically INDEPENDENT channels (so their coincidence is a theorem not a definition) + measure the residual where they diverge, OR drop "emergent coupling / non-obvious link" (which removes the distinction claim).
- FATAL-2 no-free-lie COLLAPSES against a colluding second key, and the ontology already concedes it: the stealthy class is corroboration-consistent by construction; if the corroborating sightings are signed by a 2nd colluding key (attacker holds 2 keys), the lie passes live, needs ZERO suppression, leaves NO anchored trace = the "third state". §2 lists `colluding-keys` while §8 targets "free-lie region empty" -> contradiction. Prior art: Qu/Tang/Ma colluding-vehicle TSC attack (arXiv 2111.02845); Li/Nejad/Zhang (arXiv 1906.02628) "non-repudiation not truthfulness". FIX: restrict no-free-lie to "lies corroborated only by honest independent keys"; state inline a k-of-n colluding coalition defeats it (external ground truth required).
- FATAL-3 the keystone §6.7 mechanism IS Certificate Transparency relabelled: signed monotonic append-only commitment + (seq_high, merkle_root) to an external witness + suppression externally detectable = Signed Tree Head + consistency proof + split-view detection. Prior art head-on: RFC 6962 (Laurie et al 2013); Crosby-Wallach USENIX Sec 2009; CONIKS (Melara USENIX Sec 2015); A2M (Chun SOSP 2007), TrInc (Levin NSDI 2009); Haber-Stornetta 1991; Schneier-Kelsey TISSEC 1999; Kusters CCS 2010. FIX: CONCEDE §6.7 = CT applied to sightings, cite RFC 6962, relocate novelty off "the mechanism".
- FATAL-4 "no third state" is asserted+gated, never PROVEN: §8 admits a "bounded residual"; the disjunction is exhaustive only IF (corroboration-live == corroboration-post-hoc, i.e. FATAL-1) AND (anchor uncompromised, an assumption). FIX: state as a CONDITIONAL THEOREM (holds iff single-sourced-honest-keys AND non-equivocating-anchor), report measured collusion residual as the honest bound = defensible MERIT result; unconditional headline is false.
- MAJOR: (1) threat-model mismatch persists - single-node anchor at demo scale is exactly what an operator-adjacent adversary can reach; CT needed MULTIPLE logs + auditors + gossip (Chuat CNS 2015) -> require quorum/gossip (>=2 witnesses) or restrict thesis threat model to non-operator insider inline. (2) confusion matrix scores the SLM vs INJECTED ground truth; the hard confusion (real EV vs collusively-fabricated) is SET by the injector -> state it measures SLM reasoning CONDITIONAL on externally-supplied ground truth, separate from any claim the SYSTEM discriminates real from fabricated (it does not). (3) frozen-is-safer is a strawman (a no-learning model can't be poisoned = true by construction; Biggio-Roli 2018, Goldblum TPAMI 2022) -> compare vs a HARDENED continual-learner (TRIM Jagielski S&P 2018; Steinhardt-Koh-Liang NeurIPS 2017) or measure the ADAPTIVITY COST of freezing. (4) mandatory SLM still not load-bearing (headline coupling is pure crypto/log, SLM firewalled out) -> give it an evidential role or honestly demote. (5) fallback "first quantified map" is an application/measurement contribution + the target result merely CONFIRMS a known guarantee -> lead with a SURPRISING measured finding (collusion residual larger than FDI lit predicts on real turning-count geometry, or anchored-detection-latency phase transition vs demand).
- VERDICT: MERIT not distinction as written. Distinction reachable ONLY via (1) no-free-lie as explicitly conditional theorem bounded by measured collusion residual, (2) concede §6.7 is CT + move novelty to a surprising empirical finding on the real corridor, (3) make SLM load-bearing or demote honestly.

### Methods agent (a91184c5) — §8/§3 still NOT valid (FATAL 6, MAJOR 10, MINOR 1)
- F1 the 7-way confusion matrix is scored against a `ground_truth` field §11 says "the fault engine READS" -> either the classifier reads the answer key (100% by construction) or the hard classes are analytically unresolvable from provenance (§6.4 concedes) and undefined. FIX: SEVER the scoring label from any field the engine/SLM sees; forbid ground_truth from the prompt+engine input; enumerate the verified-provenance-only feature set each origin decision may use; pre-declare provably-non-separable pairs -> force to `unknown` not scored as wins.
- F2 easy classes (attacker-key, legitimate, unknown, colluding-keys) dominate; aggregate accuracy set by the experimenter-chosen unpinned class PRIOR; D6 has no failing condition. FIX: pin the injected class prior; pre-register per-class P/R + a SEPARATE accuracy on the {authentic-lie, honest-sensor-error, detector-error} triad each with its own MDE + a minimum that can FAIL D6.
- F3 the coupling premise ("every stealthy lie needs suppression of an honest sighting") is FALSE on (a) colluding-keys (ADD a sighting, no suppression) and (b) low-impact/no-witness lies (nothing to suppress); region (c) is populated exactly by these and they're EXCLUDED from the declared attack axes; the union-recall "bound" is an asserted knob (depends on empirical witness density). FIX: state the coupling as a THEOREM with explicit assumptions (witness-density>0, no colluding sighter key) then EMPIRICALLY vary collusion-degree + witness-density as first-class attack axes.
- F4 the free-lie "fraction of attack space" has no pinned sampling MEASURE + the 2-D axes exclude collusion-degree/witness-density -> the fraction is whatever the sampling makes it. FIX: pre-register the full attack-parameter grid incl those two dims + a stated sampling distribution; report region (c) per cell.
- F5 the SLM kill-criterion is unfalsifiable: a 3-way OR whose arm (iii) frozen-is-safer is guaranteed true by construction -> criterion never fires (reintroduces round-4 methods-F4). FIX: arm (iii) must beat a FAIR defended continual baseline net of adaptivity loss; multiplicity correction; CLAIM survives only by winning the SPECIFIC job the thesis leans on.
- F6 the frozen-is-safer experiment (the load-bearing arm per F5) has NO protocol (no threat model/channel/budget/objective/baseline-defenses/n/MDE/CI/pre-registration) + strawman baseline. FIX: fully specify threat model, same model/task both arms, defended continual baseline, measure robustness gain AND adaptivity loss, same rigor as Exp1.
- MAJOR (10): M1 "distance from dev support" is a family not a criterion (metric/standardization/support-def/threshold all unpinned; Mahalanobis still favors the SLM) -> pre-register exact formula + report accuracy as a CONTINUOUS curve vs distance so no threshold DOF. M2 nonlinear Bayes ceiling estimable ONLY from the known injector model + least estimable on the low-density novel tail + SLM can't beat it by construction -> compute analytically from the injector model, restrict to density>floor, propagate MC error, pre-register an MDE for the ceiling-minus-SLM GAP, stop calling low-density-in-distribution "OOD". M3 parse-failure-as-failure conflates reasoning with format (template never parse-fails) -> decompose (reasoning acc on parseable + end-to-end incl parse-failure as a separate cost). M4 "no threshold-defining constant" policed by a human reviewer -> add a BEHAVIORAL leak test (SLM implied dev boundary coincides with the rule's box boundary = leak). M5 git-hash seals labels not the whole protocol -> pre-register the ENTIRE analysis bundle under one hash + global test-eval counter cap=1. M6 TOST margin == experimenter-chosen MDE + a Job-B equivalence null is a SUCCESS (kill-arm ii) = incentive to inflate -> justify MDE from an external operational-harm threshold + pin the SD rule. M7 the CUSUM threshold rests on an unpinned FP budget over possibly-uncalibrated synthetic honest traffic -> pin the FP budget numerically + bind Exp-D threshold to the same real-calibration gate. M8 Job-A citation-correctness gold labels have NO kappa gate (only faithfulness does) -> apply 2-rater kappa>=0.6-or-NOT_ASSESSED to citation gold too. M9 faithfulness measured only on settled-law, excluding the novel combos where hallucination risk is highest -> report a faithfulness proxy on novel too or NOT_ASSESSED-with-reason. M10 "strictly dominates" + "acceptable false-citation rate" unpinned (kill-arm i) -> pin the numeric FCR ceiling + exact dominance rule before the run.
- MINOR: m1 coordination toggle is honestly a structural zero but still framed as an experiment -> remove from experimental claims.
- VERDICT: §8/§3 NOT valid; v3 closed the crudest round-4 forms but 3 headline defects survive under new names: confusion matrix scored vs a ground_truth field the engine reads (F1), no-free-lie residual is a knob because the premise is asserted-not-proven + the attack-space measure excludes the dims that populate the residual (F3/F4), SLM kill-criterion unfalsifiable via a construction-guaranteed arm (F5/F6).

### Security agent (a7c795b0) — no-free-lie does NOT survive as headline (FATAL 5, MAJOR 7)
- HEADLINE: v3 is a build target; code implements ~NONE of the repairs (grep: zero signed_payload/sender_pubkey_fpr/kind:sighting/completeness/INCOMPLETE_DISCLOSURE/self_sig/incumbent_countersig/enrol in src/). Per the fail-loud golden rule every no-free-lie claim must currently report RED. AND even if built as written the design still leaks:
- FATAL-1 root-per-batch anchor does NOT bind completeness; operator equivocates WITHOUT touching the anchor: the incriminating decision record is simply NEVER appended (append is operator-side), the chain over what WAS appended is contiguous, verify_chain=True, operator anchors merkle_root over the sanitized set. Inclusion proofs prove PRESENCE never ABSENCE/completeness. FIX: completeness requires the witness to receive a first-party per-signer COUNT ("junction J asserts it emitted N msgs", signed by J, gossiped outside the operator) reconciled against disclosed records, OR every record independently mirrored to the witness. State plainly: intra-batch + creation-time omission survive.
- FATAL-2 enrolment bootstrap is CIRCULAR; operator forges the genesis key->junction binding with NO anchor compromise: "self-sign" proves key-controls-key never key-belongs-to-physical-J; operator enrols an attacker key AS J (self-signed, trivially valid), anchors it, all subsequent counter-signed rotations inherit the forged genesis. FIX: an identity root EXTERNAL to the operator (manufacturer attestation key / CA / physically-witnessed ceremony) must sign the first key->entity binding, else "attribution to a key" conflates key-that-signed with entity-that-controls-key (round-4 finding UNFIXED).
- FATAL-3 colluding keys defeat key-bound corroboration for the exact headline class (multi-junction corridor = operator holds all keys): J2 signs the claim, J3 signs a sighting for the same ev_id from a self-asserted plausible position, _corroborated returns True (junction != claimer/tl), lie passes live, corroboration attributes to the attacker's OWN 2nd key = colluding-keys, runtime-undetectable. F1 relabels the hole (1 key -> 2 keys), does not close it. FIX: none at demo scale under a single admin trust domain; honest framing = coupling holds only vs an adversary controlling ONE corridor key, stated inline in §1/§13 (contradicts the current headline).
- FATAL-4 no external anchor exists (merkle_root is pure in-memory; Besu anchors registry events not audit batches) -> per §10 + D-anchor the claim must be labelled "not externally anchored", not green.
- FATAL-5 the repaired fault engine is entirely absent: fault_report does NO verify_chain, NO identity.verify, NO completeness check; reads msg.get("sig_valid") (banned bool the author controls) + a self-asserted corroboration_count scalar; E2 tie-break not implemented; old HUMAN_VS_AI/ATTACKER enum. Attacker shapes the verdict by choosing content. -> item-3 rewire is unbuilt; no attribution sound until it lands + a test runs over a constructed real AuditLog refusing on a tampered signature.
- MAJOR: (1) counter-signed rotation BRICKS a lost key (availability) or its unspecified "external quorum" escape hatch reopens F2 -> specify the quorum concretely + prove external to operator, or accept the availability cost. (2) pre-anchor rollback/fork window (operator chooses WHEN to anchor) -> bound max unanchored window, treat gaps as INCOMPLETE_DISCLOSURE (still can't catch creation-time omission). (3) route-consistency is a plausibility filter not an independence proof (position self-asserted, never read) -> needs a measured non-attacker-authored position source. (4) S5 preimage persistence vs GDPR Art 17 (recomputable-forever XOR erase-on-request; the real personal data is the off-chain preimage) -> minimise payload to non-personal fields before signing, or defend indefinite retention under Art 17(3) for a NAMED controller (TfL public-task; private operator lacks 6(1)(e)). (5) S7 oracle defeated by colluding keys (provenance-to-a-key != truth). (6) no TTL/nonce/persisted replay HWM (unbuilt). (7) the stated residual adversary (§6.11 "admin key AND anchor") UNDERSTATES the true out-of-scope set: FATAL-1/2/3 each reach a free-lie region WITHOUT that conjunction -> widen the stated residual to include operator-alone omission, genesis forgery, any multi-key insider.
- VERDICT: no-free-lie/operator-binding does NOT survive as the §1/§13 headline. FATAL-1 (completeness != integrity) + FATAL-2 (circular enrolment) each break operator-binding without the anchor compromise the spec names as the sole residual; FATAL-3 leaves the headline class's corroboration in the attacker's own 2nd key.

### Build-feasibility agent (a32eedab) — Fable CANNOT build the §11 subset unattended-safe tonight (FATAL 5, MAJOR 6) [ALL NO-REGRET build-target pins]
- FATAL-1 pinned decision schema matches NO real producer: coordinated_controller emits {tls,tick,slm_phase,...} not {junction,t,classification,policies_applied,driving_input_seq}; the false_preemption/missed_ev decisions live in EmergencyController.ev_events (a different list on a class IntegratedSystem never instantiates + _drain_audit never mirrors); flatten "map event.* up" lifts wrong keys (tls!=junction, tick!=t) -> every causal read None -> UNKNOWN. FIX: pin the schema to what a producer will ACTUALLY emit + schedule the producer edit; require the item-3 test to consume IntegratedSystem.run()'s log not a hand-built dict; pin the field remap in flatten.
- FATAL-2 driving_input_seq cannot be populated from the _drain_audit seam: drain runs AFTER decide() consumed+marked-delivered the messages; only digested event["received"] remains (signature/pubkey/payload discarded). FIX: LIFT the no-edit rule for CoordinatedController.decide/EmergencyController.decide specifically (append the message record + capture its seq at consumption, stamp into the decision), or add a public ctrl.last_driving_seq. Pin which.
- FATAL-3 signed messages never reach the log with a verifiable sender_signature (NeighborMessage.signature dies at inbox; received has none) -> Fable synthesizes a message record with no real signature OR re-signs with the issuer (conflating issuer-sig layer with sender-sig layer = round-3 F4 error). FIX: capture NeighborMessage at consumption, store sender_signature=msg.signature.hex() + signed_payload; assert it verifies under the registry DER key in the same test.
- FATAL-4 "refuse on a tampered signature" is passable by verify_chain ALONE (sender_signature is inside the event body which the chain hashes) -> Fable never calls identity.verify + ships green. FIX: pin a test that swaps sender_signature for a valid-form signature from the WRONG key while re-chaining so verify_chain passes; only identity.verify can reject.
- FATAL-5 anti-starvation: named hook `ShieldController` does NOT exist (base is MaxPressureController); no mechanism, no counter; D7 anti_starvation_violations==0 passes trivially on a never-incremented counter; forced-skip test unpinned = tautological. FIX: correct hook to MaxPressureController.step; pin per-approach skip count + force-serve at max_skip + increment site; pin a committed traffic fixture where one approach genuinely never wins argmax, asserting it IS served within max_skip AND the counter EMERGES 0 (never set by the test).
- MAJOR: (6) sender_pubkey_fpr is a FINGERPRINT, verify needs the 44-byte DER key (only in the live Registry, and re-registration changes it); persisted jsonl has no DER -> store the DER pubkey (or enrolment epoch) in the record + land §6.5 enrolment self-sig. (7) E2 tie-break has no committed ground-truth fixture (code+test can agree either way; T3 is about the UNDEFENDED system so pins nothing) -> commit a frozen worked-example vector (input records -> expected origin string) read-only, authored pre-overnight. (8) sensor-fed-spoof vs system-detector is a ground_truth ECHO (engine reads the same field the test sets) = tautological -> pin the EVIDENCE-DERIVED trigger predicate (lone keyless local sighting, no 2nd independent signed sighting), ground_truth SCORING-ONLY. (9) D1 grep gameable by unverified prose scope-exclusion -> pin an exact exclude ALLOWLIST + assert excluded subset of allowlist + all live docs grep clean. (10) canonical_bytes(signed_payload) recipe not executable (takes 3 positional args not a dict; hex-vs-base64 unpinned) -> pin canonical_bytes(sp["sender"],sp["t"],sp["payload"]) + sender_signature=lowercase hex. (11) real-anchor has no concrete acceptance (endpoint/protocol/write/verify/offline all unpinned) -> pin a concrete witness (Sigstore/Rekor or a named single-node ledger RPC) + submit/verify calls + fail-loud offline, in a prerequisite doc like euston-build.md.
- 13 THINGS THE SPEC MUST PIN (see agent output): decision-record PRODUCER; flatten field remap; which list feeds preemption outcomes; the decision->driving-message link + explicit no-edit-rule lift; sender_signature provenance + DER storage; a tamper test isolating identity.verify; item-3 gate binds to the LIVE-produced log; frozen worked-example fixture for E2; evidence-derived sensor-spoof predicate; anti-starvation hook/mechanism/counter/fixture; D1 exclude-allowlist; literal recompute call; real-anchor contract.
- FATAL 1-3 are ONE wound: v3 pinned the record SHAPE + adapter but not the PRODUCER, so the fake-green vector round-4 named survives intact.

### Consistency agent (a07d1aee) — v3 NOT only-minor; the reframe is coherent INSIDE the master but a fresh FATAL + 3 unscheduled doc contradictions (FATAL 1, MAJOR 3, MINOR 2) [ALL NO-REGRET]
- GOOD: the coupling + real-anchor reframe is coherently propagated inside MASTER §0/§1/§2/§8/§13 (no surviving "attribute-to-key as the novel object"); N1 + N6 genuinely fixed; the anchor golden-rule trap is HANDLED (mandatory-in-design + unbuilt + D-anchor-gated is consistent).
- B1 FATAL: MASTER:226 falsely claims the N2 exemption "is LANDED in GROUND-RULES §F + TEST-SPEC" but GROUND-RULES:92 + TEST-AUDIT:39,127 are unamended AND MASTER:112 (§5.6) says the action is still open -> self-contradiction + false done-claim (the "is LANDED not merely asserted" phrasing deliberately exits the target bucket so the BUILD-TARGET banner doesn't rescue it). FIX: land the amendment or reword :226 to "must be stated in ... (target, §5.6)".
- B2 MAJOR: spec.md:32 (scenario 1) + FR-003/004 + SC-002 still assert local sensing "cannot be spoofed", negating §6.4's locked reframe (drop that; sensor-fed-spoof is now a first-class origin) -> add a §5 item rewriting spec.md scenario1/FR/SC-002 to "strong but spoofable; a lone keyless local reading cannot force sustained preemption".
- B3 MAJOR: the ENTIRE 05-supervision layer + report .tex still headline the OLD thesis (PROJECT-STATUS-AND-PLAN, AKIN-survey, FEEDBACK-MATRIX, lee-progress-update, report.tex all frame "SLM vs coordination decided by Exp1" / "coordinated emergency handling the core"); "detectability envelope" survives in PROJECT-STATUS:108, LEE-business-spec:39, AKIN-survey:47; §5 enumerates only spec.md/TEST-AUDIT so D1 has NO passable target or scope-exclusion for them -> add the supervision docs to the §5 manifest with scheduled rewrites, or add an explicit D1 scope-exclusion for dated supervisor snapshots with a stated reason.
- B4 MAJOR: spec.md:10 names PROJECT-PROPOSAL.md "canonical" but PROJECT-PROPOSAL:57,82 still frames the novelty as "an empirically measured detectability envelope" (own-claim, purged term, residual-axis) -> neutralise those lines to the coupling/physical-impact axis, or drop the "canonical" designation.
- B5 MINOR: SSOT "attribution to a KEY" is loose (origin values system-classifier/detector/legitimate are not keys) -> "classifies origin; where the origin is a signing party it names a key, never a person". B6 MINOR: SSOT freezes 31.1/4.63 with CIs while the seeds row says exp1 n=1/exp2 n=3 pilots -> label the row "(StubAgent powered demo, deterministic gate)".
- VERDICT: NO, not only-minor; but note the ONLY fatal is a false done-claim (B1) + 3 UNSCHEDULED doc rewrites (B2/B3/B4) - all no-regret mechanical fixes, none touch a locked decision.

---

## ROUND 5 TOTAL: FATAL 24, MAJOR 36, MINOR 5. (Round 4 was 17/31 -> the battery is finding MORE, not converging.)

## META-CONCLUSION (the loop's "bulletproof/zero-fatal" target is NOT reachable by continued spec-refinement):
Split the 24 fatals into two buckets.
- BUCKET A - NO-REGRET, fixable by spec/build edits (the majority): build FATAL-1..5 (pin the PRODUCER not just the shape; lift the no-edit rule for decide(); capture the bus sender_signature; tamper-test isolating identity.verify; anti-starvation on MaxPressureController + committed fixture); consistency B1-B4 (false done-claim + 3 unscheduled doc rewrites); methods F1/F2/F4 + M1-M10 (sever scoring label from engine input; pin class prior + per-triad MDE; pre-register the full attack grid incl collusion-degree + witness-density; pin OOD metric/FP-budget/dominance/FCR/kappa); legal M2/M3/M4/m1/m2 (off-chain personal data + named controller + Poole + degrade secondary sources); security MAJOR-1..6 (quorum/gossip anchor, pre-anchor window bound, measured position source, GDPR preimage minimisation, TTL/nonce/HWM). -> apply all in v4.
- BUCKET B - NOT fixable by spec-writing; only by CHANGING THE CLAIM (the convergent core, 3 agents independently): the accountability MECHANISM is textbook Certificate Transparency + accountability (RFC6962 / CONIKS / A2M / TrInc / PeerReview / Kusters) applied to a new domain (novelty FATAL-3); the coupling is a definitional identity not an emergent property (novelty FATAL-1); and the UNCONDITIONAL "no-free-lie" claim is PROVABLY FALSE against the colluding-key / operator-omission / circular-enrolment insider the project exists to defend against (novelty FATAL-2/4, security FATAL-1/2/3, methods F3). NO spec edit makes the mechanism novel or the unconditional claim true. The agents' OWN prescribed resolution (all three converge): state no-free-lie as an EXPLICITLY CONDITIONAL theorem (holds vs a single-key non-operator adversary) + MEASURE the residual under collusion/operator-omission as first-class attack axes + CONCEDE §6.7 is CT + relocate the novelty to the CHARACTERISATION (the first quantified map of what this coordination layer can and cannot resist on a real London corridor) + make the SLM load-bearing or honestly demote it.

## DECISION FOR THE USER (do not resolve unilaterally - touches the locked novel-object + is the 3rd reframe):
This is an MSc dissertation. The MSc-distinction bar (and exactly what Lee + Akin asked for: formalise, threat model, derive the gap, evaluate the SLM, methodology, limitations) is rigorous engineering + honest measurement + honest boundaries - NOT a novel cryptographic mechanism. The sadist PhD-examiner persona applies a PhD-novel-mechanism bar that overshoots the actual bar. The honest, BULLETPROOF framing concedes what the agents attack (it's CT; collusion breaks the strong claim) and claims only what survives (conditional guarantee + measured failure boundary + domain-first characterisation). Recommend: adopt the honest conditional/characterisation framing (keeps the coupling as a CONDITIONAL result - a tightening, not a 4th lateral pivot), apply all Bucket-A fixes into v4, then run ONE final verification battery rather than infinite rounds. -> ask the user.



