# Master-spec hardening log (working file; findings accumulate here, synthesised into MASTER-SPEC each round)

## Round 3 (adversarial battery vs MASTER-SPEC.md)

### Novelty agent (a76324cb) — verdict: NOT distinction-grade as written; recoverable
Ranked findings + fixes:
1. **The "detectability envelope" is a re-labelled detector ROC.** Prior art owns it: Neyman-Pearson (1933) likelihood-ratio OC; Page (1954) CUSUM average-run-length; CPS stealthy-attack characterisation (Teixeira/Sandberg/Johansson Automatica 2015; Mo-Sinopoli 2010; Liu-Ning-Reiter 2011 undetectable FDI; Cardenas-Amin-Sastry 2008). Our own ADVERSARIAL-FAILURE-SYNTHESIS S.4 (Keijzer) names the stealthy-residual limit. Plotting recall vs lie-magnitude = tracing our own threshold. NOT novel as-is.
2. **Envelope is tautological if lie-magnitude is on the residual axis** (residual monotone in m -> "recall rises with m" is by construction; the knee is just where we set the threshold). FIX: define lie-magnitude on the PHYSICAL-IMPACT axis (seconds of green stolen, EV delay induced), and construct conservation-consistent lies that are physically large yet residual-invisible. Promote today's "bounding zeros" (collusion/spillback) to the MAIN measured object.
3. **Commodity composition** = exactly the synthesis's own Stage 1-5 recommended default (Ed25519 + Haber-Stornetta/Schneier-Kelsey hash-chain + conservation if-statement + summariser). Each leg individually unpublishable. FIX: make the thesis the DUALITY (emergent property): real-time-undetectable BUT forensically-attributable. Characterise the stealthy class the gate misses in real time, then prove the log attributes exactly that class post-hoc to a key with cited law. This is the one non-obvious claim the composition owns.
4. **SLM Job A is decorative unless it beats a deterministic rule-to-text TEMPLATE on held-out NOVEL fact-combinations** (open-world statute mapping a finite rule table can't enumerate), scored on faithfulness + coverage. FIX: add the template baseline + novel-combination held-out set; SLM win condition = strictly dominate the template there. Elevate the faithfulness "rule oracle" into the contribution: a formal ENTAILMENT CHECKER over a codified UK-law fragment that REFUSES to ship any evidence pack whose cited rules don't entail its attribution (the verifier + shipping guarantee is the novelty, not the generation). Prior art to cite/beat: Lewis RAG 2020; Dahl/Magesh/Suzgun/Ho "Large Legal Fictions" Stanford 2024; Winfield-Jirotka ethical black box.
5. **Traffic-R1 (arXiv:2508.02344) guts the edge/small/frozen angle** (3B production TSC model, beats GPT-4o). "Frozen = no poisoning surface" is true of any non-continual deployment = not a contribution. FIX: either drop edge/small/frozen as novelty and reposition SLM as a TRANSFER question (can a general SLM given only cited policy match a bespoke production model on narrow disambiguation with zero task-specific training?), OR MEASURE "frozen is safer" by running an actual data-poisoning attack vs a continual-learning baseline and reporting the attack-surface delta.

**Single highest-value change (agent's pick):** redefine the envelope around conservation-consistent stealthy collusive lies on the physical-impact axis, paired with the forensic-attributability duality. Makes the envelope non-tautological, turns composition into an emergent property, gives the log+SLM a unique job, reuses everything built.

DECISION FLAG for user: the duality reframe repositions the novelty claim (additive, consistent with locked constraints). Surface at synthesis.

### Methods agent (a40c1c0d) — §8 still not a valid experiment
- K1 circularity INTACT in design: generate_dataset labels are monotone-threshold functions of the same features the rule reads; the "novel" split has NO region a threshold fails. FIX: label must be non-recoverable from observable features by any monotone threshold; documented class-overlap region, Bayes-error floor >0, report label<->feature mutual information; SUMO injector sets the latent real/phantom flag, a noisy sensing process generates overlapping observables.
- K2 prompt STILL leaks "1.0 = the limit" (slm_agent.py) = the novel-split boundary. FIX: strip it; add an assertion that fails the run if the prompt carries any numeric evidence threshold.
- K3 tuned AND tested on the same 90 cases (no dev/test split in code). FIX: disjoint dev/test seeds in code; evaluate once on test; forbid evaluate over tuning cases.
- K4 real_model=true does not exist; parse-failure -> REJECT so a dead model scores maximally safe (fake-green via the error path). FIX: pin+assert model hash/quant/Foundry version, abort on mismatch; parse_failures a separate exclusion, never a safe default; abort if > threshold.
- K5 evidence-pack faithfulness circular (graded vs a rule = rule-mimicry); citation-correctness is an echo test (RAG injects the same KB); Job A not built (max_tokens=16, no-CoT stance). FIX: grade faithfulness vs STATUTE TEXT by an independent adjudicator; citation vs held-out applicable-rule labels produced without showing the annotator the retrieved set.
- K6 human gold standard hand-waved/infeasible (no annotator, qualification, frame; kappa needs >=2 raters). FIX: name qualified raters + sampling frame + 2-rater protocol + adjudication + report kappa BEFORE comparing the SLM; else drop the kappa claim.
- K7 MDE is words not a computation. FIX: real power calc (alpha, power=0.8, piloted SD, Holm-adjusted alpha, derived n) + a TOST equivalence test + "CI half-width < MDE" as the acceptance gate for declaring a null (else = underpowered, not null).
- K8 coordination contrast confounded (defended - maxpressure_preempt mixes gate+SLM+shield+coord). FIX: toggle ONLY the coordination term with all else fixed; rename the current delta "combined defended-stack effect."
- K9 "demand calibrated where possible" = unfalsifiable escape hatch. FIX: name the count source (TfL/DfT AADT), make calibration a startup gate that aborts or flags "uncalibrated synthetic"; delete "where possible."

### Legal agent (a25128598) — §7 rename is cosmetic; core defect survives
- A1 confidence-scored candidate_attribution + cited statutes = machine adjudication of the ultimate issue; post-Bates(Horizon) unreliable-presumption + no admissibility route; the disclaimer is a label not a control. FIX: REMOVE candidate_attribution / confidence / fault_weight from the emitted EVIDENTIAL object.
- A2 auto "attacker_key" = machine-published criminal accusation -> Defamation Act 2013 + UK GDPR Art 10 (criminal-offence data) + Art 22 (automated decision, SCHUFA C-634/21). FIX: ban attacker/culprit language; neutral provenance only ("claim from key X (entity E) failed corroboration checks P3/P5; uncorroborated"); add Art 10 condition + Art 22 handling; restricted/privileged distribution.
- A3 GDPR Art 17 overclaimed (salted hash re-linkable, Breyer). FIX: anchor the Merkle ROOT ONLY (no per-record personal-data hash on chain); per-record secret salt destroyed on erasure; retain under Art 17(3)(b)/(e) exemption + documented balancing; name the Art 6(1)(e) public-task basis.
- A4 SLM legal prose inadmissible + disclosable weapon + induces automation bias. FIX: class the SLM trace non-evidential/internal, firewalled from the pack AND from the reviewer's FIRST look.
- A5 conflicting-green=misfeasance baked as settled + confidence on unsettled law. FIX: emit factual detection only; legal characterisation NOT_ASSESSED (Bird vs Gorringe/Stovin/Poole unsettled); confidence attaches to detection, never to law.
- A6 HITL does not discharge vs rubber-stamping. FIX: define meaningful review (competent+empowered, sees raw provenance FIRST, records independent reasons, logged override-rate KPI).
- A7 KB mixes primary/secondary + SI-numbering caveats; "hard_law" strips the s.38(7) evidential-only nuance + goes stale. FIX: every emitted citation carries source-provenance (primary/secondary) + as-at date + the KB caveat verbatim; never assert hard_law as a conclusion ("MUST-rule; evidential weight only per RTA s.38(7)"); pinned legal-currency review + fail-loud staleness gate.
- VERDICT: single biggest = STOP emitting machine attribution. Split: (1) evidential asset = reproducible provenance record (which key drove which decision, sig/chain verification, neutral rule text; NO attacker/fault/confidence/legal-conclusion); (2) SLM prose = non-evidential, restricted, firewalled triage note.

### Security agent (a13300f7) — §6/§7 fixes are LARGELY ABSENT in the code, plus a design inversion
- E1 fault_report reads a sig_valid bool and verifies NO crypto (no verify_chain/verify anywhere); _audit_bundle still "comment then return" (never verifies). TEST-AUDIT §1.1 STILL defines sig_valid:bool (spec never propagated its own §6.1 fix). FIX: fault_report takes the AuditLog + registry pubkeys, verify_chain once + identity.verify per causal record, hard-refuse (UNKNOWN + abort) on any failure.
- E2 DESIGN INVERSION: an authentic-but-lying insider is attributed to the AI (SYSTEM_CLASSIFIER), NOT the key, contradicting the spec's own worked example T3 (ATTACKER(J1)). Crypto-verify does NOT fix it (message is genuinely signed; the content is the lie). FIX: a validly-signed uncorroborated claim that caused harm -> ATTACKER(sender_key); SYSTEM_CLASSIFIER only with proof the classifier erred on a WELL-EVIDENCED case vs ground truth. THIS ties to the novelty duality: the authentic lie the gate cannot catch in real time is exactly what the log must attribute to the key.
- E3 attacker picks the verdict branch via self-asserted content (msg=None -> AI; in-band+corr>0 -> collusion; out-of-band -> AI); no profitable branch names the attacker. FIX: verdict inputs ONLY from cryptographically-verified provenance; corroboration RECOMPUTED by the engine from independently-signed sighting records, never read from a scalar the attacker set.
- E4 SENSOR_FED_SPOOF split does not exist; ORIGIN rename never happened (HUMAN_VS_AI still hardcoded); local sensing admitted unconditionally and declared "cannot be spoofed", contradicting §6.4. FIX: add ORIGIN incl SENSOR_FED_SPOOF; require a 2nd independent signal before a lone local sighting forces sustained green; STATE the sensor-spoof-vs-detector-error label is a post-hoc, ground-truth-dependent forensic call, not a runtime attribution.
- E5 no conflicting_green outcome type -> authority/AI fault can NEVER be attributed (falls to UNKNOWN); §11 scopes conflicting-green to log-only. FIX: add a conflicting_green outcome + SYSTEM_MISFEASANCE verdict (low-confidence, Bird-v-Pearce, advisory), wired to the live detector, anchored BEFORE actionable so the authority cannot delete its own liability record.
- E6 indirect prompt injection via attacker-authored audit fields (decision reason, ev_id, payload) steers the evidence pack (a real rule id passes citation-correctness). FIX: segregate untrusted audit/message content as clearly-delimited non-instruction context; add a provenance check to the faithfulness oracle (every asserted fact must trace to a verified audit field).
- E7 single-domain operator can rewrite/rollback/selectively-disclose; verify_chain only proves self-consistency; inclusion proof proves presence NOT completeness; §11 uses a MOCK anchor so current runs have ZERO external binding; §1/§13 say "externally-anchored" without the caveat inline. FIX: scope every headline/viva sentence inline ("tamper-evident vs third parties, NOT the operator/admin-key tier"); gate any "externally-anchored" claim on a REAL anchor write (abort/label if mock); add a signed monotonic append-only completeness commitment (anchored (seq_high, root) per batch) so omission/rollback is externally detectable.
- E8 ROOT: trust root is an UNSIGNED, operator-forgeable registry; replay guard in-memory (cross-restart replay works); no nonce/TTL/freshness/HWM/payload-bound; corroboration = a single signed assertion. FIX: sign+anchor registry events; persist per-sender replay HWM; bound payload size/field/depth at publish; TTL-expire sightings; per-CLAIM nonce; require route/time plausibility before a sighting counts as corroboration.
- SINGLE BIGGEST HOLE: non-repudiation rests on the unsigned-registry + mocked-anchor = exactly the out-of-scope operator/admin-key tier; until the registry is signed-and-anchored and the anchor is real, "non-repudiation to a key" is scoped to an adversary strictly WEAKER than the operator the log is meant to hold accountable.

### CROSS-CUTTING (all four so far)
1. The spec is a BUILD TARGET whose fixes are NOT in the code yet; it must say so explicitly and the acceptance gates must be the mechanism that proves each fix landed (not prose claims).
2. CONVERGENT DESIGN DECISION for the user: stop emitting a machine fault verdict/attribution; SPLIT into (a) a pure, reproducible, cryptographically-verified PROVENANCE record (the evidential asset) and (b) a firewalled, non-evidential SLM triage note. Attribution, where made at all, is to a VERIFIED KEY, computed only from verified provenance, and a validly-signed uncorroborated harmful claim goes to the KEY not the AI.
3. The novelty DUALITY (undetectable-in-real-time but attributable-post-hoc) + E2 (attribute the authentic lie to the key) + the split artifact all point the SAME way = the coherent, defensible thesis.

### Consistency agent (a14287e1) — 4 FATAL contradictions INSIDE the master spec (conforming other docs cannot fix these)
- C1 §5.4 "No SLM decision is a defect for lacking policies" vs §10/§12 "zero decision records with empty policies_applied -> ABORT". Direct self-contradiction. FIX: either every decision (SLM incl.) carries the rule policies that fired, OR exempt SLM Job-A records from the empty-policy gate explicitly + amend GROUND-RULES §F + TEST-SPEC.
- C2 §2 ORIGIN enum {attacker-key/system-classifier/system-detector/sensor-fed-spoof/legitimate/unknown} DROPS OUT_OF_SCOPE_COLLUSION which T10 (TEST-AUDIT §2.4) + §7.4 + fault_attribution.py REQUIRE. FIX: add colluding-keys origin OR map collusion to unknown+key-list and amend T10.
- C3 §1 "externally-anchored" retracted by §6.8 (operator controls anchor), §11 (mock anchor), §13 (silently drops it). FIX: strike "externally-anchored" from §1; "hash-chained, optional operator-controlled anchor (single trust domain)".
- C4 §2 "Single Source of Truth" is INCOMPLETE: no Substrate/map row (Euston cutover unlisted) and omits the 4.63s mean-harm figure it calls canonical in §5.2. FIX: add Substrate row (Euston A501; synthetic=fixture; Lambeth dropped) + Harm-avoided-mean row 4.63 [3.25,5.99].
- Quarantine is a LANDMINE: D1 grep passes stale STSD "blockchain/EU-AI-Act-mandate/60ms/optimization" claims merely because a banner sits above them. FIX: excise/neutralise the in-body claims OR exclude the quarantined file from the D1 grep scope; a banner alone must NOT satisfy D1.
- §6.1 sig_valid ban NOT propagated: TEST-AUDIT §1.1 still defines sig_valid:bool. Aspirational-as-settled: §1(b)/(c),§3,§8 read settled but are unbuilt (§13 admits "not yet met"). Two "novel object" defs (§1 pair vs §8 Exp3-alone). slm_agent still cites the DELETED PROJECT-DECISION-BRIEF + keeps no-CoT + MaxPressure prompt. SC-001 still says "coordinated".
- VERDICT: 4 fatal internal inconsistencies survive even if all other docs conform. The rewrite tightened prose but froze a thesis it admits is unbuilt, on a substrate that does not exist, governed by a table that omits the substrate.

### Build agent (a7681af7) — NOT buildable overnight as written
Missing on disk: euston.net.xml, edge_ids.json, ground_rules.yaml, run_emergency.py, attacks_live.py, run_exploit_defend.py, the real_model pin. foundry probe may hang (~120s).
- F1 (fatal) Exp1 label circularity + its ONLY non-circular source (SUMO injection) is phased out -> no honest green tonight. FIX: declare Exp1-with-real-labels OUT of overnight subset; overnight ship only the rule harness + a Foundry determinism probe, labelled "pilot, synthetic, non-circular labels pending".
- F2 (fatal) §10 global startup gates abort to NOTHING when Euston/Foundry/corpus absent. FIX: make each gate SCOPE-LOCAL to the artifact it protects; add a sanctioned SKIP-not-abort contract (doc/corridor work must not be gated on Foundry/Euston).
- F3 (fatal) Euston OSM recipe under-specified + needs live network + edge_map(mode=chain) HARD-ERRORS on real topology. FIX: pre-build and COMMIT euston.net.xml + edge_ids.json by hand (exact bbox, full netconvert flags, expected TLS/in-edge count); the overnight builder must NEVER call osmGet; else stay on corridor.
- F4 (severe) fault_attribution/AuditLog schema mismatch: AuditLog signature = ISSUER's over the entry hash, NOT the SENDER's message signature §6.1 wants; no kind="message" record is ever emitted; _audit_bundle cross-check is a comment then return. FIX: pin the message-record JSON schema + ORIGIN enum + flattening-adapter signature VERBATIM; add a test running fault_report over a REAL AuditLog that refuses attribution on verify failure.
- F5 (severe) slm_agent contradicts spec both ways (no-CoT + max_tokens=16 + threshold-leaking prompt); Job A evidence-pack + ground_rules KB unbuilt. FIX: build+commit ground_rules.yaml (P1-P8 index cross-linked to uk-traffic-law statute ids) + a citation oracle BEFORE the run; else Job A / Exp4 OUT of subset.
- F6 (severe) consistency remediation is 30-40 files not the ~12 §5 names (blockchain 37, liability 34, verdict 29, decentralis(z)ed ~20, optimization 18, Iroha 18, EU-AI-Act 17). FIX: generate the full `grep -rl` manifest + one canonical banner string; run D1 grep LAST as the gate; allow terms inside quotations of prior work.
- F7 (moderate) T10/T11 both REQUIRED (§11 step7 "A1-A5 + T1-T11") and phased-out (documented-xfail). FIX: state T1-T9 pass-required, T10/T11 documented-xfail, in one place.
- F8 (moderate) exploit-defend runner + NaiveCooperativeController don't exist -> D8 + exploit-defend OUT of overnight subset.
- CORRECTED overnight subset (buildable, no Foundry/Euston/OSM dep): (1) doc remediation over the full manifest, D1 grep last; (2) message-record schema + kind="message" emission in the live seam; (3) rewire fault_attribution to the real AuditLog: rename assessment/ORIGIN, add SENSOR_FED_SPOOF, flattening adapter, enforce identity.verify + verify_chain before attributing, wire the dead _audit_bundle cross-check; (4) fix slm_agent prompt as a STATIC edit (strip threshold + few-shots, raise max_tokens, drop no-CoT); (5) anti-starvation shield invariant + forced-skip unit test; (6) Foundry determinism probe as SKIP-not-abort. OUT: Euston, ground_rules citations, Job A/Exp4, SUMO-injection-labelled Exp1, exploit-defend/D8, 30-seed powered runs.
- Single biggest feasibility risk: F1 (Exp1 label circularity) is a DESIGN dependency, not a missing file; the SLM's only scorable task needs a non-circular labelled set whose sole valid source (SUMO injections) is out of the overnight subset, so no honest headline green tonight.

## ROUND 3 SYNTHESIS -> v2 spec changes (pending user decision on attribution model)
Mechanical v2 fixes (no user decision needed): fix C1-C4 internal contradictions; propagate sig_valid ban to TEST-AUDIT §1.1; add SENSOR_FED_SPOOF + keep collusion origin; mark spec explicitly BUILD-TARGET-not-settled (per-section) and gate "settled" on the acceptance test; make §10 gates scope-local + SKIP-not-abort; replace §11 overnight subset with the build agent's realistic 6-item core + a "prerequisite artifacts to pre-build, never osmGet-at-night" phase; T1-T9 required / T10-T11 xfail; full D1 grep manifest + canonical banner, run last, neutralise STSD in-body claims; pin the message-record schema + adapter verbatim; strip the leaked threshold from slm_agent + methods §8 (Bayes-error-floor>0 label decorrelation, dev/test split in code, real power-calc + TOST + CI-half-width<MDE null gate, single-term coordination toggle, statute-text faithfulness oracle, 2-rater kappa protocol, named demand source); adopt the NOVELTY DUALITY as the lead novel object (physical-impact axis, characterise the stealthy conservation-consistent class the gate cannot catch in real time, prove the log attributes it post-hoc to a verified key).
USER DECISION REQUIRED (restructures §1/§3/§7): the fault-output model (machine attribution/verdict vs pure-provenance-evidence-pack split). Asked -> RESOLVED: user chose SPLIT (pure evidence pack + firewalled note). Also RESOLVED: user chose to adopt the DUALITY as the lead novel object.

## Round 4 (adversarial battery vs MASTER-SPEC v2)

### Legal agent (aee40882) — split helped (A5,A7 cured; A3 mostly; A1 mitigated) but 3 FATAL remain
- FATAL-1 defamation-by-innuendo + UK GDPR Art 10: the "pure" pack juxtaposes "corroboration checks P3/P5 FAILED" + curated criminal-statute text (RTA s.36 = criminal) + a key the registry resolves to an entity -> a reasonable reader takes "entity E committed a criminal offence"; a key is a pseudonym = still personal data (Breyer), so "attribution to a key not a person" is legally hollow; the header disclaimer does not change the meaning taken. FIX: strip ALL curated law + the "which-checks-failed" characterisation from the SHAREABLE pack; ship only independently re-verifiable RAW crypto (signed bytes, pubkeys, tooling), assert nothing; OR confine the pack to a closed forensic/legal channel (qualified privilege).
- FATAL-2 the internal note is DISCLOSABLE anyway: an auto-generated operational triage note produced every incident fails the litigation-privilege dominant-purpose test (Waugh, SFO v ENRC) and is not legal-advice privileged; disclosable in litigation + on a DSAR (Art 15). The "firewall" controls internal sequencing, not discoverability. FIX: do NOT persist an auto attribution conclusion at all, or generate it only under genuine litigation instruction via counsel; drop the criminal-implying attacker-key enum from the note.
- FATAL-3 (TOUCHES LOCKED DECISIONS - CONSULT USER) the DUALITY relocates adjudication under a new name: §1/§13 claim the log "attributes the RESPONSIBLE signing key" and E2 hardcodes signed-uncorroborated-harm -> attacker-key = a machine causal-fault determination (SCHUFA: a preparatory automated step IS the Art 22 decision). FIX proposed: change the thesis verb from "attributes the responsible key" to "produces a tamper-evident, re-verifiable record FROM WHICH a human inquiry MAY attribute"; the system links inputs to decisions, it does not name a culprit. NOTE: this softens the attribution the user just locked (duality + split); the security agent pulls the OPPOSITE way (attribute-to-key). RECONCILE at synthesis + put to the user.
- MAJOR A1-residual: machine "verification result / which checks failed" asserted as reliable fact with no admissibility route (post-Bates). FIX: ship raw bytes + pubkeys + tooling so any party re-computes; the pack asserts nothing (provenance = reproducibility, not conclusion).
- MAJOR A6: meaningful-review is aspirational + the reviewer works from a machine-selected record set / machine-chosen checks / machine-curated law = decision frame pre-set. FIX: reviewer assembles their own record set; note withheld unless requested; override-rate externally audited, no target; no consequential action traceable to the note.
- MAJOR "curation IS adjudication": choosing which rules are applicable + reporting which checks failed ARE the adjudicative acts the split claims to remove; RTA s.36 text in a "pure" pack is the tell. FIX: separate a neutral UNSELECTED rule library from the record; the human picks applicable law.
- MINOR A3-residual: Art 6(1)(e) public-task basis fits TfL but NOT a private operator (needs Art 6(1)(f)+balancing); Merkle-leaf erasure vs committed root needs high-entropy secret salt truly destroyed. MINOR A5-label: keep detection physical ("simultaneous green on conflicting links per getControlledLinks"), not "conflicting-green/misfeasance".
- COUNTS: FATAL 3, MAJOR 3, MINOR 2. Verdict: NOT legally survivable as written for an auto-emitted shareable/court-facing artifact. Becomes survivable with: (a) strip curated law + characterisation from the pack -> raw re-verifiable crypto only; (b) don't persist an auto attribution conclusion (or only under privilege), closed channel; (c) reframe thesis "attributes" -> "produces a re-verifiable record enabling a human inquiry".

### Methods agent (ab0ed3a0) — §8/§3 v2 NOT yet valid; the DUALITY reintroduces the tautology (FATAL 5, MAJOR 10)
- FATAL-1 (TOUCHES LOCKED DUALITY - CONSULT) Experiment D "gate recall COLLAPSES on the physical-impact axis" is tautological: the gate detects on conservation residual; you DEFINE the lie family as residual~0; so "recall floored at residual~0" is by construction, no collapse to measure. It is also the prior-art-owned stealthy-detectability half (Teixeira/Sandberg 2015, Mo-Sinopoli 2010, Liu-Ning-Reiter FDI; our own SYNTHESIS S.4). FIX: stop claiming "recall collapses"; state "there exists a lie family whose residual stays below the INDEPENDENTLY-CALIBRATED detection threshold (report residual distribution vs that threshold + the FP budget that sets it) yet whose physical impact is unbounded"; LEAD with the attribution half; detectability half = setup, cite+beat the prior art.
- FATAL-2 (TOUCHES LOCKED DUALITY - CONSULT) D6 "attribution rate to the correct KEY" measures the TRIVIAL part (signature lookup ~100% by construction, not reasoning). FIX: D6 primary metric = ORIGIN-classification accuracy vs the injected latent cause, FULL CONFUSION MATRIX over the ontology, foreground the hard confusions (authentic-lie vs honest sensor/detector error); report crypto attribute-to-key separately as a ~100% verification check, explicitly NOT the contribution. [Converges with the legal agent: the real object is the origin classification enabling a human inquiry, not a machine key-verdict.]
- FATAL-3 Experiment 1 de-circularisation rigs generator+split for the SLM: (a) "label non-recoverable by any MONOTONE threshold" + a non-monotone SLM pre-ordains the SLM win; Bayes-floor/MI do not bound the monotone-vs-nonlinear gap; (b) the novel split is authored "where a reasoner could plausibly generalise" = SLM-favourable, assigned by the experimenter's own rule; (c) "monotone threshold" is the WRONG baseline class - rule_disambiguator is an axis-aligned CONJUNCTION (box), strictly stronger. FIX: report the accuracy ceiling of (i) the baseline's exact class (axis-aligned conjunctions, dev-tuned) and (ii) the unrestricted Bayes-optimal NONLINEAR classifier; judge the SLM vs the nonlinear ceiling (margin is then visibly a fraction of a built-in ceiling); define the novel split by a classifier-INDEPENDENT OOD criterion (distance from dev support), pre-registered; pre-register noise params before any SLM result.
- FATAL-4 UNFALSIFIABLE: §7/§12 make every outcome a contribution (SLM wins -> SLM headline; null -> coordination headline); no result retires the SLM. FIX: pre-commit an SLM KILL criterion (e.g. if the SLM neither beats the template on Job-A faithfulness+coverage on settled-law novel cases NOR shows below-MDE equivalence at an acceptable false-citation rate, the SLM is cut). State before the run. [NOTE: "cut the SLM" collides with SLM-mandatory lock; the kill criterion instead demotes the SLM's ROLE/claim, not its presence - CONSULT user on wording.]
- FATAL-5 Job-A template comparison guaranteed by construction (table template has zero coverage on truly-novel combos -> SLM "dominates" for free) + coverage-as-presence lets a confident WRONG citation win (Dahl et al 2024 legal hallucination). FIX: score coverage by citation CORRECTNESS not presence; give the template a graceful "nearest rule + no exact match" fallback; report the SLM false-citation rate on novel combos as a PRIMARY number (a distinction result may be "honest template abstention beats confident SLM hallucination").
- MAJOR (10): M1 no-threshold assertion catches numerals only, labelled few-shots + comparative bands still leak the boundary -> ban labelled few-shots, independent reviewer confirms no leak. M2 "evaluate once" unenforceable -> git-hash-seal test labels, log+flag every test eval, cap dev prompt revisions. M3 parse-failure exclusion biases up + always-reject games false-preemption -> count parse-failure as classification failure (primary), report FP jointly with recall. M4 kappa/faithfulness infeasible as written + ban LLM-as-judge for legal faithfulness (circular) -> name qualified human raters + frame + adjudication, restrict to settled-law, gate on kappa>=0.6 else NOT_ASSESSED. M5 no numeric MDE/TOST margin pre-registered + pilot SD from n<=3 -> pre-register numeric MDE (seconds EV delay / FP%) + conservative SD. M6 coordination toggle on an INERT term = structural zero, not an empirical null -> say "no active term to measure", measuring it needs a live term = a different system than 31/16/8. M7 AADT is a daily total not a time-resolved profile -> name the hourly/turning/mix sources + method; hard gate for inferential claims. M8 Exp-D region size is a free knob (threshold + impact-axis max are experimenter-set) -> fix threshold at its calibrated FP budget, bound impact axis by physical max, report region as a FRACTION of the physically-realizable attack space. M9 Exp-D conflates the conservation detector with the FULL gate (conservation+CUSUM+corroboration) -> a conservation-consistent lie may still fail corroboration; specify which component each lie defeats, prove the UNION of all live checks has recall below a stated bound (a lie caught by corroboration is NOT in the undetectable class). M10 memoisation launders nondeterminism -> memoise only at 100% agreement; below that report accuracy as a distribution.
- COUNTS: FATAL 5, MAJOR 10. Verdict: §8/§3 NOT valid as written; v2 retired the crudest K1-K9 forms but Exp D reintroduces the tautology + leads with the prior-art-owned/trivial halves, and Exp1/Job-A embed generator+split rigging that pre-ordain the SLM. Fix the 5 FATALs (reframe Exp D around origin-classification with a calibrated threshold; report the nonlinear Bayes ceiling + a classifier-independent novel split; score Job-A on citation correctness with a pre-committed SLM-kill/demote criterion) -> defensible.

### Novelty/examiner agent (a1e59b4f) — the DUALITY is not distinction-grade as written (FATAL 3, MAJOR 3)
- FATAL-1 (TOUCHES LOCKED DUALITY - CONSULT) The attribution half is TRIVIAL non-repudiation dressed as forensics. Attributing a signed message to its key is what a signature IS; every logged message is signed, so D6 attribution-rate ~1.0 by construction. The non-trivial act (establishing the signed claim was a LIE) is done by external ground truth (§6.4/E4 concedes this). The two halves share no mechanism (lie invisible because residual~0; attribution works because Ed25519 signed it) = orthogonal facts stapled, not an emergent property. Prior art: ISO 13888 non-repudiation; accountability formalised by Kusters-Truderung-Vogt CCS 2010, PeerReview (Haeberlen SOSP 2007). FIX: reframe half-B as a NON-EQUIVOCATION / COMPLETENESS guarantee, and make the emergent claim the COUPLING: the same corroborating sightings the operator must SUPPRESS to hide the lie are the physical signals that could have caught it LIVE. That coupling is the one non-obvious link the composition can own.
- FATAL-2 (TOUCHES LOCKED DUALITY - CONSULT) "real-time-undetectable but post-hoc-attributable" is textbook accountability + FDI, not a new object. Prior art: Haber-Stornetta 1991 hash-chain; Schneier-Kelsey 1999 audit logs surviving compromise; Kusters accountability; PeerReview; stealthy half = Liu-Ning-Reiter 2011, Teixeira-Sandberg 2015, Mo-Sinopoli 2010 (our own S.3/S.4 already name these). The ONE defensible increment: not the mechanism but the DOMAIN INSTANTIATION + quantified map = first characterisation/measurement of the physically-large-but-conservation-invisible insider-preemption class on a real signalised corridor tied to cited UK statute. Genuine wrinkle vs PeerReview: the lie is protocol-VALID (conservation-consistent) so accountability-by-replay returns "no fault" (which is exactly why attribution needs external ground truth). FIX: demote "duality" from novel MECHANISM to novel DOMAIN-MAP; stop calling it "the lead novel object", call it "first measured instance of X in domain Y".
- FATAL-3 THREAT-MODEL MISMATCH: the attribution guarantee EXCLUDES the very adversary the gap names. Synthesis motivates an authenticated-but-lying INSIDER / trusted coordination peer (S.1/S.3), but §2/§6.10/E7 scope tamper-evidence to third parties only, NOT the operator/admin-key tier, and §11 keeps the anchor MOCK for all runs. So every actual run has zero external binding; the guarantee holds only against an adversary strictly weaker than the operator. FIX: ship a REAL anchor (not mock) + bind the operator via non-equivocation, OR restrict the threat model to a non-operator insider stated inline in §1/§13.
- MAJOR: (M4) mandatory SLM is NOT load-bearing to the headline (gate does real-time defence; attribution recomputed by engine; pack has no SLM output; Job-A firewalled/non-evidential) -> give it an evidential role that survives the firewall, OR run data-poisoning vs a continual-learning baseline to MEASURE "frozen is safer" (currently asserted, absent from §8), OR honestly downgrade. Prior art: Traffic-R1 arXiv 2508.02344, Dahl et al Stanford 2024. (M5) the split heals the legal defect by SEVERING the value chain: pack = consequential but standard primitives; note = novel-ish but non-evidential; the firewall that saves the legal case prevents the two halves combining -> locate ONE property both novel AND evidential, put it in the pack (the non-equivocation coupling is the only candidate). (M6) nothing distinction-critical is built/green: Exp D, Exp 1, Exp 2, Job-A citation all OUT of overnight subset; a spec is not a result.
- SINGLE CHANGE THAT MOST RAISES THE CEILING (examiner's own words): replace "attributes to a verified signing key" with a REAL-ANCHORED NON-EQUIVOCATION / COMPLETENESS guarantee that BINDS THE OPERATOR, and make the emergent claim the coupling (suppress-to-hide == could-have-caught-live). This one move (a) converts the trivial attribution half into the genuinely hard property, (b) closes the biggest security hole E7/E8, (c) aligns the guarantee with the operator-adjacent insider the synthesis motivates, (d) gives a non-obvious emergent link, (e) puts a novel-AND-evidential property into the pure pack (heals the split). Requires a REAL external anchor instead of the mock. That scope decision is the true gate on distinction. COUNTS: FATAL 3, MAJOR 3.

### Security agent (a3548fd7) — attribution-to-key does NOT survive as the headline (FATAL 2, MAJOR 5, MINOR 1)
- FATAL-1 (TOUCHES LOCKED DUALITY - CONSULT) SELF-CORROBORATING sensor-spoof defeats key-attribution of the headline stealthy lie: attacker signs an in-band conservation-consistent claim AND spoofs the local sensor the gate treats as "independent" corroboration (§6.4 only needs "a second independent signal"); gate passes; post-hoc the corroboration source is a KEYLESS sensor reading so it attributes to no key. §6.4 itself concedes sensor-fed-spoof is a post-hoc ground-truth call. So the corroborating half of the very class the duality headlines has no key. FIX: restrict the attributability claim to lies whose CORROBORATION is entirely key-bound; require any signal that forces sustained preemption to be an attributable signed record; mark keyless-corroborated preemptions "corroboration not attributable".
- FATAL-2 (wholly UNADDRESSED in v2 - CONSULT) FRAME AN HONEST KEY via operator-controlled re-registration: registry.register permits re-registration after revoke with a NEW key (registry.py:72-73) and registry events are admin-key-signed only (single trust domain). An operator-adjacent attacker binds an attacker-held key under honest junction J's id, signs the lie; post-hoc attribution + the pack's key->entity map (§7.2) resolve to honest J while verify_chain/verify_signatures/completeness all stay green. Conflates key-that-signed with entity-that-controls-key. FIX: enrolment ceremony where each junction self-signs its key; rotation/re-registration requires the incumbent key's counter-signature (or a quorum external to the operator); the pack carries enrolment provenance not just current registry state.
- MAJOR: (S3) headline §1/§13 "attributes the responsible key" lacks the §6.10 operator/selective-disclosure caveat inline -> propagate verbatim. (S4) evidence-pack citation-SELECTION is a covert origin label: failed-checks set + cited-rule selection is a near-deterministic function of candidate_origin, so a reviewer reconstructs origin from the "pure" pack, defeating the automation-bias control (D2) -> cite mechanically & origin-independently (every rule touching any present field) or prove citation set is not a function of origin. (S5) E1 fix NOT self-verifiable: §6.1 stores signed_bytes_HASH but §6.3 verify needs the signed PREIMAGE; record lists neither t nor payload -> store canonical signable content (or t+payload) so signed_bytes is recomputable. (S6) corroboration-recompute (§6.6) has no schema to walk: §6.2 pins only kind:message/decision, not a signed per-sighting record with its origin key -> add signed kind:"sighting" record. (S7) prompt-injection oracle checks PROVENANCE not TRUTH: an attacker's own authentically-signed free-text reason/ev_id IS a "verified field" whose content is a lie -> distinguish "field authentically logged" from "content independently corroborated"; forbid attacker-authored free-text as a citation's sole support.
- MINOR: (S8) piggyback corroboration on a real EV's ev_id -> bind nonce to (ev_id, approach, time-window, claimer) + route-consistency. Verdict: "attributed post-hoc to a verified key" survives ONLY inside the explicitly-scoped adversary-weaker-than-operator sub-model AND only for lies whose corroboration is entirely key-bound (spec does not restrict to that). Two things break it within intent: keyless self-corroboration + operator-forgeable key->entity binding. Biggest residual hole = the TRUST ROOT (admin-key-signed registry permitting unilateral re-registration; anchor still mock).

### Build-feasibility agent (a76be160) — full §11 subset NOT unattended-safe tonight (FATAL 3, MAJOR 7)
- FATAL-1 message/decision record schema NOT pinned & self-contradictory: §6.1 says {sender, sender_pubkey_fpr, signed_bytes_hash, sender_signature}; TEST-AUDIT §1.1 (still canonical, line 18) says {channel, payload_hash, sig_valid:bool, ...} (banned bool + different names); §5.4 admits propagation is TODO. FIX: freeze ONE verbatim JSON schema (names/types/enums), delete sig_valid from §1.1 in the same edit, reconcile sender_pubkey_fpr vs key_fingerprint.
- FATAL-2 flattening-adapter signature + real-log link semantics undefined; adapter absent & dead-tested: real AuditLog nests payload under `event`, but fault_attribution reads flat top-level e["kind"]/e["seq"] -> over a real log every read is None -> everything degrades to UNKNOWN; tests use FLAT hand dicts so the real-log path is never exercised (silent fake-green in the accountability layer). FIX: pin flatten(entries)->list[dict] mapping event.* up + preserving envelope seq; define driving_input_seq over a real log; require the item-3 test to run fault_report over a CONSTRUCTED real AuditLog and refuse on a tampered signature.
- FATAL-3 §3 E2 rule CONTRADICTS §6.4/_catchable_phantom for the zero-corroboration case: §3 says signed+uncorroborated+harmful -> attacker-key; §6.4 + code say signed+zero-corroboration+in-band -> system-classifier (catchable phantom). Same input, two origins. FIX: state which wins (recommend signed+uncorroborated+harmful -> attacker-key; system-classifier only when AI erred on a WELL-EVIDENCED case vs ground truth) and rewrite _catchable_phantom so D-sec and the T3 worked example agree.
- MAJOR: (B1) ORIGIN enum string form ambiguous + sensor-fed-spoof has no constant/trigger/test -> pin exact string values + a sensor-fed-spoof predicate + the ground_truth field. (B2) item-2 live seam cannot populate driving_input_seq without editing the forbidden controller -> log each bus msg as kind:"message" in _drain_audit + stamp driving seq, or lift the no-edit rule for this hook. (B3) anti-starvation invariant/counter/test unpinned + no mechanism exists (only min_green) yet D7 asserts anti_starvation_violations==0 -> pin the counter, max-skip bound, hook point, forced-skip test. (B4) item-4 conflates safe static prompt edit with Job-A output needing the absent corpus + "no numeric threshold" assertion will false-fire on legit evidence numbers + few-shot residuals (0.40/1.80) still leak a soft threshold -> split the item, assert "no threshold-DEFINING constant" not "no numeric", strip/qualitativise few-shot numbers. (B5) D-sec statute citations need ground_rules.yaml (out of subset) + §6.7 completeness commitment is a NEW crypto scheme not a wire-up -> mark citation half deferred, scope §6.7 separately. (B6) prerequisite-artifacts phase under-specified: §9 says "pin bbox/netconvert flags/TLS count" but gives NONE; ground_rules.yaml schema never written; injector noise model/feature schema/label format/Bayes target absent -> fill in literal values before the phase runs. (B7) sig_valid ban not yet landed in code/tests/doc -> item 3 must rewrite module+tests+doc atomically. Single biggest risk: record + flattening shapes specified in conflicting prose and never pinned as one verbatim schema -> unattended model emits records fault_report cannot read over a real AuditLog and proves them green on synthetic dicts.

### Consistency agent (a643444c) — round-3 C1-C4 all fixed; v2 introduced N1 (FATAL 1, MAJOR 3, MINOR 2)
- All four round-3 FATALs C1-C4 RESOLVED in v2 text; remaining round-3 items (sig_valid, PROJECT-DECISION-BRIEF, SC-001) honestly reframed as BUILD-TARGET remediation, not settled.
- N1 FATAL: §3 Job-A candidate_origin enum lists only 6 values - `legitimate` is MISSING - while SSOT (:46) says the 7-value ontology "lives in the internal note". Mirror of the fatal C2. Without it the note cannot express "flagged event was actually legitimate" and mis-routes to unknown. FIX: add `legitimate` to the §3 enum.
- N2 MAJOR: §12 Policy-KPI rule states the Job-A exemption is "Stated in GROUND-RULES §F + TEST-SPEC" (present tense) but GROUND-RULES-POLICY.md:92 still says flatly a decision citing no policy is a defect "(rule or SLM)" and §5 never schedules the amendment -> add the amendment to §5, or reword §12 to "must be stated in" (target not fact).
- N3 MAJOR: the detectability->physical-impact reframe is NOT propagated: spec.md:85 SC-005 still says "detectability envelope"; TEST-AUDIT §3.3(c) still sweeps "lie size as multiples of the band, the detectability envelope" (the tautological residual axis). Neither term is in the D1 forbidden-term manifest, no gate catches it -> add SC-005 + TEST-AUDIT §3.3 to §5 (physical-impact axis) or add "detectability envelope/ROC" to the D1 manifest.
- N4 MAJOR: spec.md:20 still leads "the headline value is coordinated emergency response", contradicting §1/§13 "coordination is not my claim; the duality is"; the D1 one-thesis-sentence test would fail but §5 gives it no target edit -> add a §5 item rewriting spec.md §1 overview + SC-005 to the duality thesis.
- N5/N6 MINOR: TEST-AUDIT Part 2 still uses old enum constants + no sensor-fed-spoof (extend §5.4); STSD SSOT row over-states "neutralised" as done while coursework.md still carries live 60ms/blockchain/EU-AI-Act/decentralised-optimisation text -> reword SSOT cell to target-state.

## ROUND 4 TOTAL: FATAL 17, MAJOR 31, MINOR 5. NOT bulletproof.
## CONVERGENCE (4 of 6 agents - novelty, methods, legal, security - independently hit the SAME wound):
The duality's second half "attributes to a verified KEY" is (novelty) trivial 1980s non-repudiation / prior-art accountability, (methods) a trivial signature lookup ~1.0 by construction not the hard origin classification, (legal) machine adjudication that should soften to "a re-verifiable record a human may attribute from", (security) broken by keyless self-corroboration AND operator key-re-registration against the exact operator-adjacent insider the threat model names. ALL converge on ONE keystone reframe: replace "attribute to a key" with a REAL-ANCHORED NON-EQUIVOCATION / COMPLETENESS guarantee that BINDS THE OPERATOR, whose emergent claim is the COUPLING (the corroborating sightings the operator must suppress to hide the lie are the same physical signals that could have caught it live). This ONE move collapses novelty-F1/F2/F3 + methods-F1/F2 + legal-F3 + security-F1(partial) at once and is the true gate on distinction-vs-merit. It REQUIRES a real external anchor (not the mock Besu) = a scope decision, and it repositions the locked duality wording = both are USER-LOCKED decisions -> CONSULT before applying. Everything else in round 4 is a no-regret correctness fix (schema pinning, enum values, tie-break rules, citation-correctness scoring, nonlinear ceiling + OOD split, SLM kill-criterion-as-demote (SLM stays mandatory, only its CLAIM is gated), sighting-record schema, signature preimage, operator caveat propagation, detectability-envelope purge from spec.md/TEST-AUDIT) -> apply autonomously in v3.

---

# ROUND 5 (vs MASTER-SPEC v3, 2026-07-21) — battery attacking the keystone reframe

### Legal agent (aed84484) — v3 NOT legally survivable as an auto-emitted court-facing artifact (FATAL 3, MAJOR 5, MINOR 2)
- FATAL-1 (PRODUCT TENSION - CONSULT USER) the "pure" pack STILL accuses by INNUENDO: naming a verified sender key + enrolment provenance (key->entity) + WHICH corroboration checks failed + curated criminal-statute text (RTA s.36) lets a reasonable reader assemble "this entity's key lied/offended". Removing the `attacker` label + origin verdict does not remove the STING; origin-independent citation (S4) defeats reconstruction but not the defamatory meaning; a header disclaimer does not cure under bane-and-antidote. Authority: Defamation Act 2013 s.1(1)/(2); Tolley v Fry [1931] AC 333 (innuendo); Charleston v News Group [1995] 2 AC 65 (disclaimer inadequacy). FIX (agent's): shareable pack ships ONLY independently re-verifiable raw crypto (signed preimage bytes, pubkeys, verification tooling) + an UNSELECTED rule library; it asserts provenance by reproducibility, states NO negative finding about any key. [TENSION: this strips the law-CITATION the user/Lee/Seb explicitly wanted from the shareable pack; law-selection moves to the human/counsel-gated stage. Genuine deliverable question -> ask user how minimal the auto-emitted pack should be.]
- FATAL-2 (TOUCHES SLM OPERATION - CONSULT) the internal note is NOT privileged: an always-on auto-generated note fails BOTH litigation-privilege limbs (litigation not in reasonable contemplation at generation; dominant purpose is operational triage not litigation) and legal-advice privilege (no lawyer-client advice). So the machine `candidate_origin: attacker-key` verdict is DISCOVERABLE + answerable on a DSAR; the firewall controls viewing order + automation bias, NOT discoverability. Authority: Waugh v BRB [1980] AC 521 (dominant purpose); Three Rivers No 6 [2005] UKHL 48; SFO v ENRC [2018] EWCA Civ 2006. FIX (agent's): do NOT auto-persist an attribution conclusion; generate candidate_origin only under genuine counsel instruction once adversarial litigation really contemplated; drop the criminal-implying `attacker-key` enum from any AUTO-generated artifact. [TENSION: changes SLM Job A from run-every-incident-and-persist to counsel-gated; SLM stays mandatory + still does Job A, only WHEN changes -> confirm with user.]
- FATAL-3 (CONSULT) machine-emitted + machine-selected criminal-law record = Art 22 automated decision + unlawful Art 10 processing. Even origin-independent, the machine-curated record set + machine-chosen checks + machine-selected law leaves the human a pre-set frame, so "meaningful human review" not made out (SCHUFA C-634/21: a preparatory automated step relied on downstream IS the Art 22(1) decision). Emitting RTA s.36 against a key that enrolment provenance re-links to an entity = criminal-offence data (Breyer C-582/14: pseudonymous-but-re-linkable = personal). Spec cites Art 6(1)(e) but is silent on the DPA 2018 Sch 1 condition + Appropriate Policy Document that Art 10 requires. FIX: confine the pack to a CLOSED forensic channel, no auto criminal-law selection; add DPA 2018 Sch 1 condition + APD; document Art 22 safeguards with the human assembling their own record set.
- MAJOR: (M1) CURATION IS ADJUDICATION - choosing which rules are applicable + which checks failed ARE the adjudicative acts the split claims to remove; origin-independence cures automation-bias, not adjudication-by-selection -> ship a full UNSELECTED neutral rule library, human picks applicable law. Authority: R v Bates (Post Office Horizon) reliability line. (M2) immutable Merkle-root anchor is in TENSION with Art 17 DURING the retention window: root + retained salt/preimage is re-linkable = personal + Art-10 data committed to a store the operator cannot rewrite; salt-destruction erasure only bites at end-of-retention -> keep ALL personal data + salt strictly off-chain with a bounded retention clock, anchor a root provably non-re-linkable without the off-chain secret, verify the preimage never enters the committed leaf identifiably. (M3) storing preimage {sender,t,payload} PERSISTS new personal data the digest-only design did not (payloads may carry EV routing/location -> maybe Art 9) -> bounded retention + encrypt at rest + Art 9 assessment in the DPIA. (M4) legal basis + controller under-specified for anyone but TfL: Art 6(1)(e) fits TfL (public authority; A501 is TLRN so TfL is the correct signal authority + misfeasance defendant per Bird v Pearce) but NOT a private edge-layer vendor (needs 6(1)(f) + LIA; a public authority cannot use 6(1)(f) for its tasks) -> name the controller explicitly. (M5) (TOUCHES HEADLINE WORDING - CONSULT) the "no-free-LIE" headline PRE-LABELS the traced act a LIE, but the project's own 7-way confusion matrix exists BECAUSE authentic-lie vs honest-sensor-error vs detector-error are confusable; the coupling proves traceability of SUPPRESSION not dishonest intent; calling it a "lie" prejudges the dishonesty/ultimate issue -> reword the headline object from "lie" to "conservation-consistent deviation" / "unattributed-but-traced deviation"; reserve "lie"/dishonesty for a human inquiry's finding. Authority: Defamation Act 2013 s.1; malicious falsehood (1952 s.3).
- MINOR: (m1) rename court-facing "conflicting-green detector" to the physical fact only ("simultaneous green on conflicting controlled links per getControlledLinks"); add Poole BC v GN [2019] UKSC 25 to uk-traffic-law.md. (m2) auto-emitted "neutral cited-rule text" asserts law the KB itself flags uncertain (secondary sources, SI-numbering caveats, authority-liability least-settled) -> degrade any secondary-sourced/low-confidence rule to "reference only, verify against primary" + exclude from shareable emission.
- VERDICT: NOT survivable as an auto-emitted court-facing artifact. Becomes survivable only if: shareable pack = raw re-verifiable crypto + UNSELECTED rule library asserting nothing; NO auto-persisted attribution conclusion (counsel-gated); closed forensic/privileged channel not "shareable"; controller fixed as TfL + DPA Sch 1 + APD (or 6(1)(f)+LIA for a private operator).
- NOTE FOR SYNTHESIS: F1/F2 + M5 touch USER-LOCKED decisions (SPLIT pack purpose = law-cited & shareable; SLM Job A cadence; "no-free-lie" headline wording). The MINIMAL-PACK direction strips the law-citation the user/Lee/Seb wanted from the SHAREABLE artifact. -> put a single scope question to the user at R5 synthesis; do NOT unilaterally gut the pack. M2/M3/M4/m1/m2 are no-regret (apply in v4).

### Novelty agent (acac9346) — v3 caps at MERIT not distinction (FATAL 4, MAJOR 5)
- FATAL-1 the COUPLING is a definitional TAUTOLOGY not an emergent property: §6.6 defines BOTH live-corroboration and post-hoc-corroboration as a function of the SAME signed-sighting object, so "the evidence you must suppress to hide it is the evidence that caught it live" = reading one variable twice, not a property of traffic physics. Prior art shape: PeerReview (Haeberlen SOSP 2007). FIX: either PROVE the live physical corroboration signal and the anchored audit record are physically INDEPENDENT channels (so their coincidence is a theorem not a definition) + measure the residual where they diverge, OR drop "emergent coupling / non-obvious link" (which removes the distinction claim).
- FATAL-2 no-free-lie COLLAPSES against a colluding second key, and the ontology already concedes it: the stealthy class is corroboration-consistent by construction; if the corroborating sightings are signed by a 2nd colluding key (attacker holds 2 keys), the lie passes live, needs ZERO suppression, leaves NO anchored trace = the "third state". §2 lists `colluding-keys` while §8 targets "free-lie region empty" -> contradiction. Prior art: Qu/Tang/Ma colluding-vehicle TSC attack (arXiv 2111.02845); Li/Nejad/Zhang (arXiv 1906.02628) "non-repudiation not truthfulness". FIX: restrict no-free-lie to "lies corroborated only by honest independent keys"; state inline a k-of-n colluding coalition defeats it (external ground truth required).
- FATAL-3 the keystone §6.7 mechanism IS Certificate Transparency relabelled: signed monotonic append-only commitment + (seq_high, merkle_root) to an external witness + suppression externally detectable = Signed Tree Head + consistency proof + split-view detection. Prior art head-on: RFC 6962 (Laurie et al 2013); Crosby-Wallach USENIX Sec 2009; CONIKS (Melara USENIX Sec 2015); A2M (Chun SOSP 2007), TrInc (Levin NSDI 2009); Haber-Stornetta 1991; Schneier-Kelsey TISSEC 1999; Kusters CCS 2010. FIX: CONCEDE §6.7 = CT applied to sightings, cite RFC 6962, relocate novelty off "the mechanism".
- FATAL-4 "no third state" is asserted+gated, never PROVEN: §8 admits a "bounded residual"; the disjunction is exhaustive only IF (corroboration-live == corroboration-post-hoc, i.e. FATAL-1) AND (anchor uncompromised, an assumption). FIX: state as a CONDITIONAL THEOREM (holds iff single-sourced-honest-keys AND non-equivocating-anchor), report measured collusion residual as the honest bound = defensible MERIT result; unconditional headline is false.
- MAJOR: (1) threat-model mismatch persists - single-node anchor at demo scale is exactly what an operator-adjacent adversary can reach; CT needed MULTIPLE logs + auditors + gossip (Chuat CNS 2015) -> require quorum/gossip (>=2 witnesses) or restrict thesis threat model to non-operator insider inline. (2) confusion matrix scores the SLM vs INJECTED ground truth; the hard confusion (real EV vs collusively-fabricated) is SET by the injector -> state it measures SLM reasoning CONDITIONAL on externally-supplied ground truth, separate from any claim the SYSTEM discriminates real from fabricated (it does not). (3) frozen-is-safer is a strawman (a no-learning model can't be poisoned = true by construction; Biggio-Roli 2018, Goldblum TPAMI 2022) -> compare vs a HARDENED continual-learner (TRIM Jagielski S&P 2018; Steinhardt-Koh-Liang NeurIPS 2017) or measure the ADAPTIVITY COST of freezing. (4) mandatory SLM still not load-bearing (headline coupling is pure crypto/log, SLM firewalled out) -> give it an evidential role or honestly demote. (5) fallback "first quantified map" is an application/measurement contribution + the target result merely CONFIRMS a known guarantee -> lead with a SURPRISING measured finding (collusion residual larger than FDI lit predicts on real turning-count geometry, or anchored-detection-latency phase transition vs demand).
- VERDICT: MERIT not distinction as written. Distinction reachable ONLY via (1) no-free-lie as explicitly conditional theorem bounded by measured collusion residual, (2) concede §6.7 is CT + move novelty to a surprising empirical finding on the real corridor, (3) make SLM load-bearing or demote honestly.

### Methods agent (a91184c5) — §8/§3 still NOT valid (FATAL 6, MAJOR 10, MINOR 1)
- F1 the 7-way confusion matrix is scored against a `ground_truth` field §11 says "the fault engine READS" -> either the classifier reads the answer key (100% by construction) or the hard classes are analytically unresolvable from provenance (§6.4 concedes) and undefined. FIX: SEVER the scoring label from any field the engine/SLM sees; forbid ground_truth from the prompt+engine input; enumerate the verified-provenance-only feature set each origin decision may use; pre-declare provably-non-separable pairs -> force to `unknown` not scored as wins.
- F2 easy classes (attacker-key, legitimate, unknown, colluding-keys) dominate; aggregate accuracy set by the experimenter-chosen unpinned class PRIOR; D6 has no failing condition. FIX: pin the injected class prior; pre-register per-class P/R + a SEPARATE accuracy on the {authentic-lie, honest-sensor-error, detector-error} triad each with its own MDE + a minimum that can FAIL D6.
- F3 the coupling premise ("every stealthy lie needs suppression of an honest sighting") is FALSE on (a) colluding-keys (ADD a sighting, no suppression) and (b) low-impact/no-witness lies (nothing to suppress); region (c) is populated exactly by these and they're EXCLUDED from the declared attack axes; the union-recall "bound" is an asserted knob (depends on empirical witness density). FIX: state the coupling as a THEOREM with explicit assumptions (witness-density>0, no colluding sighter key) then EMPIRICALLY vary collusion-degree + witness-density as first-class attack axes.
- F4 the free-lie "fraction of attack space" has no pinned sampling MEASURE + the 2-D axes exclude collusion-degree/witness-density -> the fraction is whatever the sampling makes it. FIX: pre-register the full attack-parameter grid incl those two dims + a stated sampling distribution; report region (c) per cell.
- F5 the SLM kill-criterion is unfalsifiable: a 3-way OR whose arm (iii) frozen-is-safer is guaranteed true by construction -> criterion never fires (reintroduces round-4 methods-F4). FIX: arm (iii) must beat a FAIR defended continual baseline net of adaptivity loss; multiplicity correction; CLAIM survives only by winning the SPECIFIC job the thesis leans on.
- F6 the frozen-is-safer experiment (the load-bearing arm per F5) has NO protocol (no threat model/channel/budget/objective/baseline-defenses/n/MDE/CI/pre-registration) + strawman baseline. FIX: fully specify threat model, same model/task both arms, defended continual baseline, measure robustness gain AND adaptivity loss, same rigor as Exp1.
- MAJOR (10): M1 "distance from dev support" is a family not a criterion (metric/standardization/support-def/threshold all unpinned; Mahalanobis still favors the SLM) -> pre-register exact formula + report accuracy as a CONTINUOUS curve vs distance so no threshold DOF. M2 nonlinear Bayes ceiling estimable ONLY from the known injector model + least estimable on the low-density novel tail + SLM can't beat it by construction -> compute analytically from the injector model, restrict to density>floor, propagate MC error, pre-register an MDE for the ceiling-minus-SLM GAP, stop calling low-density-in-distribution "OOD". M3 parse-failure-as-failure conflates reasoning with format (template never parse-fails) -> decompose (reasoning acc on parseable + end-to-end incl parse-failure as a separate cost). M4 "no threshold-defining constant" policed by a human reviewer -> add a BEHAVIORAL leak test (SLM implied dev boundary coincides with the rule's box boundary = leak). M5 git-hash seals labels not the whole protocol -> pre-register the ENTIRE analysis bundle under one hash + global test-eval counter cap=1. M6 TOST margin == experimenter-chosen MDE + a Job-B equivalence null is a SUCCESS (kill-arm ii) = incentive to inflate -> justify MDE from an external operational-harm threshold + pin the SD rule. M7 the CUSUM threshold rests on an unpinned FP budget over possibly-uncalibrated synthetic honest traffic -> pin the FP budget numerically + bind Exp-D threshold to the same real-calibration gate. M8 Job-A citation-correctness gold labels have NO kappa gate (only faithfulness does) -> apply 2-rater kappa>=0.6-or-NOT_ASSESSED to citation gold too. M9 faithfulness measured only on settled-law, excluding the novel combos where hallucination risk is highest -> report a faithfulness proxy on novel too or NOT_ASSESSED-with-reason. M10 "strictly dominates" + "acceptable false-citation rate" unpinned (kill-arm i) -> pin the numeric FCR ceiling + exact dominance rule before the run.
- MINOR: m1 coordination toggle is honestly a structural zero but still framed as an experiment -> remove from experimental claims.
- VERDICT: §8/§3 NOT valid; v3 closed the crudest round-4 forms but 3 headline defects survive under new names: confusion matrix scored vs a ground_truth field the engine reads (F1), no-free-lie residual is a knob because the premise is asserted-not-proven + the attack-space measure excludes the dims that populate the residual (F3/F4), SLM kill-criterion unfalsifiable via a construction-guaranteed arm (F5/F6).

### Security agent (a7c795b0) — no-free-lie does NOT survive as headline (FATAL 5, MAJOR 7)
- HEADLINE: v3 is a build target; code implements ~NONE of the repairs (grep: zero signed_payload/sender_pubkey_fpr/kind:sighting/completeness/INCOMPLETE_DISCLOSURE/self_sig/incumbent_countersig/enrol in src/). Per the fail-loud golden rule every no-free-lie claim must currently report RED. AND even if built as written the design still leaks:
- FATAL-1 root-per-batch anchor does NOT bind completeness; operator equivocates WITHOUT touching the anchor: the incriminating decision record is simply NEVER appended (append is operator-side), the chain over what WAS appended is contiguous, verify_chain=True, operator anchors merkle_root over the sanitized set. Inclusion proofs prove PRESENCE never ABSENCE/completeness. FIX: completeness requires the witness to receive a first-party per-signer COUNT ("junction J asserts it emitted N msgs", signed by J, gossiped outside the operator) reconciled against disclosed records, OR every record independently mirrored to the witness. State plainly: intra-batch + creation-time omission survive.
- FATAL-2 enrolment bootstrap is CIRCULAR; operator forges the genesis key->junction binding with NO anchor compromise: "self-sign" proves key-controls-key never key-belongs-to-physical-J; operator enrols an attacker key AS J (self-signed, trivially valid), anchors it, all subsequent counter-signed rotations inherit the forged genesis. FIX: an identity root EXTERNAL to the operator (manufacturer attestation key / CA / physically-witnessed ceremony) must sign the first key->entity binding, else "attribution to a key" conflates key-that-signed with entity-that-controls-key (round-4 finding UNFIXED).
- FATAL-3 colluding keys defeat key-bound corroboration for the exact headline class (multi-junction corridor = operator holds all keys): J2 signs the claim, J3 signs a sighting for the same ev_id from a self-asserted plausible position, _corroborated returns True (junction != claimer/tl), lie passes live, corroboration attributes to the attacker's OWN 2nd key = colluding-keys, runtime-undetectable. F1 relabels the hole (1 key -> 2 keys), does not close it. FIX: none at demo scale under a single admin trust domain; honest framing = coupling holds only vs an adversary controlling ONE corridor key, stated inline in §1/§13 (contradicts the current headline).
- FATAL-4 no external anchor exists (merkle_root is pure in-memory; Besu anchors registry events not audit batches) -> per §10 + D-anchor the claim must be labelled "not externally anchored", not green.
- FATAL-5 the repaired fault engine is entirely absent: fault_report does NO verify_chain, NO identity.verify, NO completeness check; reads msg.get("sig_valid") (banned bool the author controls) + a self-asserted corroboration_count scalar; E2 tie-break not implemented; old HUMAN_VS_AI/ATTACKER enum. Attacker shapes the verdict by choosing content. -> item-3 rewire is unbuilt; no attribution sound until it lands + a test runs over a constructed real AuditLog refusing on a tampered signature.
- MAJOR: (1) counter-signed rotation BRICKS a lost key (availability) or its unspecified "external quorum" escape hatch reopens F2 -> specify the quorum concretely + prove external to operator, or accept the availability cost. (2) pre-anchor rollback/fork window (operator chooses WHEN to anchor) -> bound max unanchored window, treat gaps as INCOMPLETE_DISCLOSURE (still can't catch creation-time omission). (3) route-consistency is a plausibility filter not an independence proof (position self-asserted, never read) -> needs a measured non-attacker-authored position source. (4) S5 preimage persistence vs GDPR Art 17 (recomputable-forever XOR erase-on-request; the real personal data is the off-chain preimage) -> minimise payload to non-personal fields before signing, or defend indefinite retention under Art 17(3) for a NAMED controller (TfL public-task; private operator lacks 6(1)(e)). (5) S7 oracle defeated by colluding keys (provenance-to-a-key != truth). (6) no TTL/nonce/persisted replay HWM (unbuilt). (7) the stated residual adversary (§6.11 "admin key AND anchor") UNDERSTATES the true out-of-scope set: FATAL-1/2/3 each reach a free-lie region WITHOUT that conjunction -> widen the stated residual to include operator-alone omission, genesis forgery, any multi-key insider.
- VERDICT: no-free-lie/operator-binding does NOT survive as the §1/§13 headline. FATAL-1 (completeness != integrity) + FATAL-2 (circular enrolment) each break operator-binding without the anchor compromise the spec names as the sole residual; FATAL-3 leaves the headline class's corroboration in the attacker's own 2nd key.

### Build-feasibility agent (a32eedab) — Fable CANNOT build the §11 subset unattended-safe tonight (FATAL 5, MAJOR 6) [ALL NO-REGRET build-target pins]
- FATAL-1 pinned decision schema matches NO real producer: coordinated_controller emits {tls,tick,slm_phase,...} not {junction,t,classification,policies_applied,driving_input_seq}; the false_preemption/missed_ev decisions live in EmergencyController.ev_events (a different list on a class IntegratedSystem never instantiates + _drain_audit never mirrors); flatten "map event.* up" lifts wrong keys (tls!=junction, tick!=t) -> every causal read None -> UNKNOWN. FIX: pin the schema to what a producer will ACTUALLY emit + schedule the producer edit; require the item-3 test to consume IntegratedSystem.run()'s log not a hand-built dict; pin the field remap in flatten.
- FATAL-2 driving_input_seq cannot be populated from the _drain_audit seam: drain runs AFTER decide() consumed+marked-delivered the messages; only digested event["received"] remains (signature/pubkey/payload discarded). FIX: LIFT the no-edit rule for CoordinatedController.decide/EmergencyController.decide specifically (append the message record + capture its seq at consumption, stamp into the decision), or add a public ctrl.last_driving_seq. Pin which.
- FATAL-3 signed messages never reach the log with a verifiable sender_signature (NeighborMessage.signature dies at inbox; received has none) -> Fable synthesizes a message record with no real signature OR re-signs with the issuer (conflating issuer-sig layer with sender-sig layer = round-3 F4 error). FIX: capture NeighborMessage at consumption, store sender_signature=msg.signature.hex() + signed_payload; assert it verifies under the registry DER key in the same test.
- FATAL-4 "refuse on a tampered signature" is passable by verify_chain ALONE (sender_signature is inside the event body which the chain hashes) -> Fable never calls identity.verify + ships green. FIX: pin a test that swaps sender_signature for a valid-form signature from the WRONG key while re-chaining so verify_chain passes; only identity.verify can reject.
- FATAL-5 anti-starvation: named hook `ShieldController` does NOT exist (base is MaxPressureController); no mechanism, no counter; D7 anti_starvation_violations==0 passes trivially on a never-incremented counter; forced-skip test unpinned = tautological. FIX: correct hook to MaxPressureController.step; pin per-approach skip count + force-serve at max_skip + increment site; pin a committed traffic fixture where one approach genuinely never wins argmax, asserting it IS served within max_skip AND the counter EMERGES 0 (never set by the test).
- MAJOR: (6) sender_pubkey_fpr is a FINGERPRINT, verify needs the 44-byte DER key (only in the live Registry, and re-registration changes it); persisted jsonl has no DER -> store the DER pubkey (or enrolment epoch) in the record + land §6.5 enrolment self-sig. (7) E2 tie-break has no committed ground-truth fixture (code+test can agree either way; T3 is about the UNDEFENDED system so pins nothing) -> commit a frozen worked-example vector (input records -> expected origin string) read-only, authored pre-overnight. (8) sensor-fed-spoof vs system-detector is a ground_truth ECHO (engine reads the same field the test sets) = tautological -> pin the EVIDENCE-DERIVED trigger predicate (lone keyless local sighting, no 2nd independent signed sighting), ground_truth SCORING-ONLY. (9) D1 grep gameable by unverified prose scope-exclusion -> pin an exact exclude ALLOWLIST + assert excluded subset of allowlist + all live docs grep clean. (10) canonical_bytes(signed_payload) recipe not executable (takes 3 positional args not a dict; hex-vs-base64 unpinned) -> pin canonical_bytes(sp["sender"],sp["t"],sp["payload"]) + sender_signature=lowercase hex. (11) real-anchor has no concrete acceptance (endpoint/protocol/write/verify/offline all unpinned) -> pin a concrete witness (Sigstore/Rekor or a named single-node ledger RPC) + submit/verify calls + fail-loud offline, in a prerequisite doc like euston-build.md.
- 13 THINGS THE SPEC MUST PIN (see agent output): decision-record PRODUCER; flatten field remap; which list feeds preemption outcomes; the decision->driving-message link + explicit no-edit-rule lift; sender_signature provenance + DER storage; a tamper test isolating identity.verify; item-3 gate binds to the LIVE-produced log; frozen worked-example fixture for E2; evidence-derived sensor-spoof predicate; anti-starvation hook/mechanism/counter/fixture; D1 exclude-allowlist; literal recompute call; real-anchor contract.
- FATAL 1-3 are ONE wound: v3 pinned the record SHAPE + adapter but not the PRODUCER, so the fake-green vector round-4 named survives intact.

### Consistency agent (a07d1aee) — v3 NOT only-minor; the reframe is coherent INSIDE the master but a fresh FATAL + 3 unscheduled doc contradictions (FATAL 1, MAJOR 3, MINOR 2) [ALL NO-REGRET]
- GOOD: the coupling + real-anchor reframe is coherently propagated inside MASTER §0/§1/§2/§8/§13 (no surviving "attribute-to-key as the novel object"); N1 + N6 genuinely fixed; the anchor golden-rule trap is HANDLED (mandatory-in-design + unbuilt + D-anchor-gated is consistent).
- B1 FATAL: MASTER:226 falsely claims the N2 exemption "is LANDED in GROUND-RULES §F + TEST-SPEC" but GROUND-RULES:92 + TEST-AUDIT:39,127 are unamended AND MASTER:112 (§5.6) says the action is still open -> self-contradiction + false done-claim (the "is LANDED not merely asserted" phrasing deliberately exits the target bucket so the BUILD-TARGET banner doesn't rescue it). FIX: land the amendment or reword :226 to "must be stated in ... (target, §5.6)".
- B2 MAJOR: spec.md:32 (scenario 1) + FR-003/004 + SC-002 still assert local sensing "cannot be spoofed", negating §6.4's locked reframe (drop that; sensor-fed-spoof is now a first-class origin) -> add a §5 item rewriting spec.md scenario1/FR/SC-002 to "strong but spoofable; a lone keyless local reading cannot force sustained preemption".
- B3 MAJOR: the ENTIRE 05-supervision layer + report .tex still headline the OLD thesis (PROJECT-STATUS-AND-PLAN, AKIN-survey, FEEDBACK-MATRIX, lee-progress-update, report.tex all frame "SLM vs coordination decided by Exp1" / "coordinated emergency handling the core"); "detectability envelope" survives in PROJECT-STATUS:108, LEE-business-spec:39, AKIN-survey:47; §5 enumerates only spec.md/TEST-AUDIT so D1 has NO passable target or scope-exclusion for them -> add the supervision docs to the §5 manifest with scheduled rewrites, or add an explicit D1 scope-exclusion for dated supervisor snapshots with a stated reason.
- B4 MAJOR: spec.md:10 names PROJECT-PROPOSAL.md "canonical" but PROJECT-PROPOSAL:57,82 still frames the novelty as "an empirically measured detectability envelope" (own-claim, purged term, residual-axis) -> neutralise those lines to the coupling/physical-impact axis, or drop the "canonical" designation.
- B5 MINOR: SSOT "attribution to a KEY" is loose (origin values system-classifier/detector/legitimate are not keys) -> "classifies origin; where the origin is a signing party it names a key, never a person". B6 MINOR: SSOT freezes 31.1/4.63 with CIs while the seeds row says exp1 n=1/exp2 n=3 pilots -> label the row "(StubAgent powered demo, deterministic gate)".
- VERDICT: NO, not only-minor; but note the ONLY fatal is a false done-claim (B1) + 3 UNSCHEDULED doc rewrites (B2/B3/B4) - all no-regret mechanical fixes, none touch a locked decision.

---

## ROUND 5 TOTAL: FATAL 24, MAJOR 36, MINOR 5. (Round 4 was 17/31 -> the battery is finding MORE, not converging.)

## META-CONCLUSION (the loop's "bulletproof/zero-fatal" target is NOT reachable by continued spec-refinement):
Split the 24 fatals into two buckets.
- BUCKET A - NO-REGRET, fixable by spec/build edits (the majority): build FATAL-1..5 (pin the PRODUCER not just the shape; lift the no-edit rule for decide(); capture the bus sender_signature; tamper-test isolating identity.verify; anti-starvation on MaxPressureController + committed fixture); consistency B1-B4 (false done-claim + 3 unscheduled doc rewrites); methods F1/F2/F4 + M1-M10 (sever scoring label from engine input; pin class prior + per-triad MDE; pre-register the full attack grid incl collusion-degree + witness-density; pin OOD metric/FP-budget/dominance/FCR/kappa); legal M2/M3/M4/m1/m2 (off-chain personal data + named controller + Poole + degrade secondary sources); security MAJOR-1..6 (quorum/gossip anchor, pre-anchor window bound, measured position source, GDPR preimage minimisation, TTL/nonce/HWM). -> apply all in v4.
- BUCKET B - NOT fixable by spec-writing; only by CHANGING THE CLAIM (the convergent core, 3 agents independently): the accountability MECHANISM is textbook Certificate Transparency + accountability (RFC6962 / CONIKS / A2M / TrInc / PeerReview / Kusters) applied to a new domain (novelty FATAL-3); the coupling is a definitional identity not an emergent property (novelty FATAL-1); and the UNCONDITIONAL "no-free-lie" claim is PROVABLY FALSE against the colluding-key / operator-omission / circular-enrolment insider the project exists to defend against (novelty FATAL-2/4, security FATAL-1/2/3, methods F3). NO spec edit makes the mechanism novel or the unconditional claim true. The agents' OWN prescribed resolution (all three converge): state no-free-lie as an EXPLICITLY CONDITIONAL theorem (holds vs a single-key non-operator adversary) + MEASURE the residual under collusion/operator-omission as first-class attack axes + CONCEDE §6.7 is CT + relocate the novelty to the CHARACTERISATION (the first quantified map of what this coordination layer can and cannot resist on a real London corridor) + make the SLM load-bearing or honestly demote it.

## DECISION FOR THE USER (do not resolve unilaterally - touches the locked novel-object + is the 3rd reframe):
This is an MSc dissertation. The MSc-distinction bar (and exactly what Lee + Akin asked for: formalise, threat model, derive the gap, evaluate the SLM, methodology, limitations) is rigorous engineering + honest measurement + honest boundaries - NOT a novel cryptographic mechanism. The sadist PhD-examiner persona applies a PhD-novel-mechanism bar that overshoots the actual bar. The honest, BULLETPROOF framing concedes what the agents attack (it's CT; collusion breaks the strong claim) and claims only what survives (conditional guarantee + measured failure boundary + domain-first characterisation). Recommend: adopt the honest conditional/characterisation framing (keeps the coupling as a CONDITIONAL result - a tightening, not a 4th lateral pivot), apply all Bucket-A fixes into v4, then run ONE final verification battery rather than infinite rounds. -> ask the user.

## USER DECISION (2026-07-21): "Honest conditional + characterisation" framing ADOPTED + "keep looping indefinitely" (loop recalibrated to the MSc-distinction bar). -> v4 committed (5d28649). See memory [[honest-conditional-characterisation-framing]].

---

# ROUND 6 (vs MASTER-SPEC v4, 2026-07-21) — battery recalibrated to the MSc-distinction bar (CT-mechanism + conditionality are CONCEDED, out of scope as attacks)

### Novelty agent (a7e40d82) — v4 CLEARS the methodology/threat-model/limitations bar; the FINDING is under-defined (FATAL 2, MAJOR 5) [ALL NO-REGRET methodological sharpenings within the chosen framing]
- F1 FATAL: the boundary map as specified is a CONFIRMATORY table not a finding - each axis has a foregone answer (collusion k>=2 breaks it by construction; operator omission = "inclusion proof proves presence never absence"; witness-density->0 trivially -> everything). "Report free-region fraction per cell" = corridor calibration numbers, not a credited finding. Prior art owns topology-dependent unobservability: Kosut-Jia-Thomas-Tong (IEEE TSG 2011, unobservable attack subspace vs measurement placement); Hendrickx-Johansson-Jungers-Sandberg-Vetterli (IEEE TAC 2014, security index is topology-dependent); Liu-Ning-Reiter; Teixeira-Sandberg. FIX: pre-register ONE falsifiable non-obvious quantitative hypothesis about the boundary's SHAPE that only the real Euston turning-count geometry can confirm/refute and the well-mixed model does NOT predict.
- F2 FATAL (real theorem BUG): the "conditional coupling" is not yet a theorem - the sufficient condition "witness-density > 0" is FALSE (one distant witness never covering the affected approach = density>0 with zero coverage = direct counterexample), and "honest independent keys" has no operational/measurable predicate (§6.6 concedes independence needs a measured position source, absent which route-consistency "adds no security"). FIX: (i) requantify condition (a) as a per-deviation COVERAGE condition: >=1 honest independent sighting whose spatial-temporal coverage intersects the deviation's physical footprint within the window, i.e. witness-density > d_crit(footprint); (ii) give independence a testable predicate over the measured position source + state plainly where absent it is ASSUMED not verified; (iii) state the theorem structure explicitly: sufficiency proven ANALYTICALLY, necessity shown EMPIRICALLY by the sweep (not an implicit iff). Prior art to match for assumption-explicit accountability: Kusters CCS2010, PeerReview SOSP2007; CT gossip: Crosby-Wallach USENIX Sec2009, Chuat CNS2015.
- F3 MAJOR: delta over FDI+CT risks reading as routine application; "first quantified map" is weak (examiners give no credit for novelty-by-absence). FIX: drop "first" as load-bearing; reposition as "abstract security-index/unobservable-subspace results assume a WELL-MIXED measurement model; on a real signalised corridor the sighting-independence geometry is HETEROGENEOUS, and we measure how that changes d_crit / the free-region shape" -> extends Kosut/Hendrickx rather than re-running it. (+ Cardenas-Amin-Sastry HotSec2008.)
- F4 MAJOR: mandatory SLM is coherent but PERIPHERAL - firewalled out of the headline it justifies; the boundary map is pure crypto/log/geometry + §8 concedes the confusion matrix measures SLM reasoning CONDITIONAL on external ground truth, the system doesn't discriminate real from fabricated. FIX: designate the SLM explicitly as the REASONER-OF-RECORD that produces the characterisation's per-incident origin labels (make Job A the INSTRUMENT of the map, not a sidecar) + lead the SLM story with Job C's measured robustness/adaptivity tradeoff (TRIM Jagielski S&P2018; Steinhardt-Koh-Liang NeurIPS2017). Confront Traffic-R1 on ROLE.
- F5 MAJOR: the 7-way confusion matrix risks collapsing to trivially-separable classes - the hard triad {authentic-deviation, honest-sensor-error, detector-error} is exactly where separability is doubtful; if most hard cases -> forced `unknown`, aggregate accuracy is dominated by easy classes + the injected prior. FIX: pre-register (from the injector forward model, BEFORE any SLM run) the expected fraction of the hard triad that is provenance-SEPARABLE + set D6 to FAIL if that separable fraction is below a stated floor.
- F6 MAJOR: the "deviation" rename (legally correct) risks under-claiming into a generic anomaly characterisation. FIX: keep the ADVERSARIAL framing explicit - the characterised deviations are the ones a rational compromised insider would select (max physical impact at min residual + min traceability), so the object stays "the insider-exploitable attack surface", measured, not "generic anomalies".
- F7 MAJOR/edge: v4 is one step from a "no-contribution" reading; the defence is identical to F1 (fixing F1 closes it). No separate action.
- CREDIT (load-bearing for the verdict): BUILD-TARGET honesty, fail-loud rule, severing the scoring label, the pre-registered attack grid incl collusion+witness-density, the quorum/gossip anchor, the SLM kill vs a HARDENED baseline net of adaptivity loss, the closed-forensic-channel split pack, the concrete legal threat model (Waugh/SFO v ENRC/SCHUFA/Breyer/DPA Sch1+APD) are all distinction-shaped. Rigour + honesty ARE there; the gap is that the FINDING is under-defined.
- VERDICT: clears the honest-methodology/threat-model/limitations bar comfortably; does NOT yet clear distinction because the sole headline object (the measured boundary) is a confirmatory table of foregone per-axis fractions. SINGLE HIGHEST-LEVERAGE CHANGE: convert "witness-density>0" into a measured CRITICAL COVERAGE-DENSITY d_crit + pre-register the falsifiable claim that real Euston turning-count geometry moves d_crit (or creates local heterogeneity / a collusion×density interaction) away from the well-mixed FDI/CT prediction. This one move fixes F2, turns F1 confirmatory->genuine geometry-dependent finding, supplies F3's delta, closes F7 - all at once. [ALL of R6-novelty is no-regret: apply in v5, no user decision needed.]

### Methods agent (a8fc613c) — §8/§3 NOT valid as written; a real v4 self-contradiction (FATAL 2, MAJOR 10) [ALL NO-REGRET; the fix strengthens the thesis]
- F1 FATAL (v4 self-contradiction I introduced): the "hard-triad accuracy" over {authentic-deviation, honest-sensor-error, detector-error} is broken - those labels are NOT in the 7-way ontology {attacker-key, system-classifier, system-detector, sensor-fed-spoof, colluding-keys, legitimate, unknown} (no class for an HONEST non-malicious sensor fault distinct from the malicious sensor-fed-spoof), AND the triad is a PHYSICAL-VERACITY question that crypto provenance + statute are silent on, so once ground_truth is honestly severed all three legs collapse to `unknown`, directly contradicting v4's own "force non-separable pairs to unknown". So the metric is either degenerate (all unknown, D6 always fails) or nonzero only by covertly reading the answer key (the exact leak the severing was meant to close). The declared "hard measured object" has no well-defined instrument. FIX (distinction-viable, matches our framing): reframe the hard object FROM triad-accuracy TO (a) a proven THEOREM that the physical-veracity triad is provenance-INDISTINGUISHABLE (this IS the characterisation, matches novelty MAJOR-2 + F5), (b) pre-registered forcing of those cells to `unknown`, (c) an accuracy metric ONLY over origins that ARE provenance-decidable (keyless-lone-sighting vs key-attributed-corroboration vs uncorroborated-signed) with an MDE + failing threshold.
- F2 FATAL: §6.4's system-detector/system-classifier trigger is defined "vs ground truth", re-importing the severed field into the ENGINE decision -> either the class is structurally un-emittable (collapses to unknown) or it fires and the 100%-by-construction leak returns. FIX: rewrite the trigger as a purely PROVENANCE-derived predicate (e.g. "a corroboration check the detector logged as failed is contradicted by an independently-signed sighting record present in the verified window"), zero ground_truth; if no provenance-only predicate exists, pre-declare the class non-separable -> `unknown` (and then it cannot be a scored leg, per F1).
- MAJOR (all no-regret): M1 the analytic Bayes ceiling is restricted to density>floor but the SLM's claimed value is on the NOVEL/low-density tail where the ceiling is absent -> state no optimal reference exists on the tail (cannot claim near-optimality there), move the headline judgment to the in-distribution region or extend the model so p(x|y) is tail-integrable with bounded error. M2 §9 gives a schema-of-a-spec not an injector MODEL; "analytic" ceiling needs an explicit parametric generative p(x|y) with closed-form/bounded-error likelihood -> §9 must REQUIRE that + pin the density floor + integration method numerically. M3 the free-region "fraction" is well-defined per cell but not as an aggregate (axis extents k_max/witness-density-range unpinned; on a 3-4 signal corridor the operator holds all keys so k_max isn't physically bounded; "fraction over the grid" vs "as they vary" unreconciled) -> commit that the reported object is the per-cell SURFACE with NO aggregate scalar + numerically pin each axis extent with physical-realizability justification (k_max tied to corridor key count + the single-key threat model) + impact denominator = real-geometry physical max. M4 "net of adaptivity loss" = two incomparable numbers (robustness gain vs clean-error-under-shift, different distributions/units) -> convert both to the same external operational-harm currency (sec EV delay / FP%) + pin the poisoning-vs-shift mixing weights in pre-registration + define "net" as expected harm under that mixture. M5 the kill-criterion's OR-vs-AND structure is unpinned (as an OR a Job-B equivalence null passes = unfalsifiable, round-5 F5 returns) -> NAME the single load-bearing job the headline depends on + tie SLM-claim survival to winning THAT job at the pre-registered MDE; other arms reported but not sufficient. M6 the "hardened continual baseline" is not buildable as named - TRIM (Jagielski, trimmed linear regression) + Steinhardt-Koh-Liang (convex data sanitization) do NOT compose with LLM fine-tuning -> pin the exact baseline (model class, online update rule, poison channel + budget as a fraction of the stream, attacker objective, a defense demonstrably applicable to THAT model class). M7 only Exp1's analysis bundle is git-hash-sealed; Exp D + Exp C grids/FP-budget/mixing-weights are choosable after seeing data -> extend the one-hash seal + eval-counter cap to Exp D + Exp C bundles. M8 every "numerically-pinned failing threshold" (FP budget, MDE, hard-triad minimum, FCR ceiling, dominance rule) is pinned in NAME ONLY - no number is in §8/§12 + the prerequisite docs don't exist -> state the numbers or bind them by name under the M7 seal. M9 FCR-on-novel as "primary" contradicts allowing faithfulness-on-novel NOT_ASSESSED (both need the same annotator-blind gold) -> build the novel-combo gold + report both, or drop "primary". M10 "provably non-separable -> unknown" has no pinned proof standard / pre-registration audit (lets a researcher post-hoc excuse any wrong confusion) -> commit the non-separability set + its proof under the M7 seal, reviewed before any eval.
- STRENGTHS (closes prior findings): the continuous accuracy-vs-distance curve removes the OOD-threshold DOF; git-hash seal + eval-counter=1 (Exp1); parse-failures-as-failures + reasoning-on-parseable decomposition; coordination = structural zero removed from experiments; the CT concession + conditional-theorem framing are honest + correct.
- VERDICT: NOT valid at the distinction bar as written; the 2 fatals are a papered-over contradiction (severing ground_truth - required for validity - makes the declared hard object provenance-non-separable, and system-detector's trigger still reads the severed field). The HONEST fix (reframe the hard object to a proven provenance-INDISTINGUISHABILITY result + accuracy only over provenance-decidable origins) is distinction-viable and matches our framing. The 10 majors are all fixable without changing the claim. [ALL no-regret: apply in v5.]

### Build-feasibility agent (aa5480f4) — Fable still CANNOT build the §11 subset unattended tonight (FATAL 6, MAJOR 8) [ALL NO-REGRET build pins]
- FATAL-1 (NEW v4 contradiction I introduced): payload_min makes the pinned verify UNSATISFIABLE - the bus signs canonical_bytes over the FULL payload, so verify recomputes only if payload_min == the full signed payload byte-for-byte; any actual minimisation (the GDPR goal) changes the bytes -> verify FAILS. FIX: DROP minimisation from the SIGNABLE object - the signed payload must contain ONLY non-personal coordination fields needed to reason (so payload_min == the actual signed payload, no separate minimisation step), personal data kept out of the signed message at source + handled by off-chain retention; OR authorize editing message_bus.canonical_bytes/publish to sign payload_min with signer+verifier using the identical function. Pick one + pin it.
- FATAL-2 item-3 "consume IntegratedSystem.run()'s log" cannot exercise the EV attribution path: EmergencyController.ev_events (source of false_preemption/missed_ev) never runs because IntegratedSystem only builds a bare CoordinatedController -> a green item-3 test sees only conservation decisions, never an EV preempt = the fake-green reopens. FIX: add EmergencyController + ev_bus + adjacency + a scripted phantom-claim AND real-EV incident into IntegratedSystem.run() behind a config flag; require item-3 to assert an attacker-key verdict on a false_preemption record from THAT log.
- FATAL-3 "lift the no-edit rule for decide()" is under-specified - emit into WHAT? controllers have no AuditLog handle; if decide() emits AND _drain_audit still mirrors, two conflicting decision producers; driving_input_seq needs the audit seq decide() can't know unless it appends itself. FIX: inject the AuditLog into the controller ctor; decide() appends kind:message at consumption, captures the returned envelope seq as driving_input_seq, then appends kind:decision; DELETE the _drain_audit decision-mirroring (keep registry mirror).
- FATAL-4 the flatten remap is incomplete: the real decision event uses `used` not `executed` (fault_attribution filters on executed), and classification/policies_applied/driving_input_seq/trigger.ev_id are emitted by NO producer -> after the pinned tls->junction/tick->t remap, executed is None + classification None. FIX: make decide() emit classification/policies_applied/driving_input_seq/executed directly matching the schema; then flatten only lifts event.* (no field guessing). If legacy .events kept, pin the FULL remap incl used->executed.
- FATAL-5 message records never enter the log (only ctrl.events + registry appended; no kind:message) -> _messages_by_seq is {} over a real log, every driving_input_seq -> None, every false_preemption with msg None -> SYSTEM_DETECTOR misattribution on the live path while hand-built-dict unit tests stay green. Plus NeighborMessage carries .signature but NOT the DER key (must fetch registry.public_key at capture + persist; re-registration mutates it). FIX: at each bus.inbox/ev_bus.inbox consumption append kind:message with sender_signature=m.signature.hex() + sender_pubkey_der=registry.public_key(sender) + signed_payload; assert it verifies. Pin BOTH seams (the EV seam _ingest_ev_inbox currently DROPS the signature).
- FATAL-6 prerequisite oracles DON'T EXIST (no ground_rules.yaml, euston.net.xml/edge_ids.json/euston-build.md, anchor.md, E2 fixture, forced-skip fixture, assessment.py) - unattended Fable will self-author them (self-authored expected-verdict = tautology) or skip + fake-green. FIX: author + COMMIT the E2 worked-example fixture, the forced-skip traffic fixture, ground_rules.yaml + citation oracle BEFORE the run + a startup assertion they exist git-committed else the dependent items SKIP-with-record.
- MAJOR: (1) anti-starvation counter still tautology-adjacent - MaxPressureController.step has no skip tracking/max_skip/counter + operates on green-phase indices not approaches; two counters conflated (per-approach skip must REACH max_skip>0 to prove the trigger; anti_starvation_violations must stay 0) -> pin both distinctly + fixture guarantees the starved phase never wins _pressure argmax + pin force-serve through the yellow/min_green machine. (2) evidence-derived sensor-fed-spoof predicate needs kind:sighting records the producer never emits (current _catchable_phantom reads a self-asserted corroboration_count scalar = the E3 attack) -> gate on recomputation from appended sighting records, forbid the scalar. (3) tamper test "over IntegratedSystem.run()'s log" conflicts with "swap wrong-key sig + re-chain" (sig lives in event body which _hash_body hashes, so swapping breaks verify_chain unless the tail is rebuilt) -> pin that the test REBUILDS a new AuditLog by re-appending the run's entries with ONE sig replaced by a valid-form sig from a different JunctionIdentity, asserts verify_chain==True + fault_report refuses via identity.verify (which today it never calls). (4) three clocks (_tick, _ev_tick, sim_time) with no pinned mapping to decision.t -> pin decision.t/message.t/outcome.t all to sim time. (5) _audit_bundle "dead cross-check" conflates issuer-sig layer (verify_signatures) with sender-sig layer (identity.verify) -> name both layers distinctly. (6) slm_agent threshold leak is in the LIVE prompt (SYSTEM_DISAMBIG + _case_evidence "1.0 = the limit"), not just few-shots -> scope the strip to those + assert across the whole assembled prompt + remove the PROJECT-DECISION-BRIEF ref. (7) D1 manifest: lee-progress-update.html is UNTRACKED (git ??) + supervision/PROJECT-PROPOSAL/report.tex carry old thesis + "detectability envelope" -> author the committed exclude allowlist + add lee-progress-update.html to the manifest. (8) sender_pubkey_der persistence + rotation -> bind the DER used at that moment into the record, verify against the record's own DER not a later registry lookup.
- VERDICT: NO. Six fatals cluster into two root wounds: (A) the real producer is still not reached (no EmergencyController in the assembly, no emission sink, incomplete remap, no message records, discarded EV signatures) - restated concretely; (B) two hard blockers - the payload_min/verify contradiction v4 newly introduced (F1) + the absent pre-committed oracles (F6). [ALL no-regret build-target pins: apply in v5 + author the prerequisite fixtures during the hardening phase.]

### Legal agent (a3397213) — v4 CLEARS the dissertation-distinction bar; cheap accuracy/completeness polish only (FATAL 0, MAJOR 5) [ALL NO-REGRET; legal dropped 3 fatal (R5) -> 0 fatal (R6)]
- MAJOR-1 CITATION ERROR: "R v Bates" does not exist - the Horizon reliability line is civil: Bates v Post Office Ltd (No 6: Horizon Issues) [2019] EWHC 3408 (QB) (Fraser J); criminal side = Hamilton v Post Office [2021] EWCA Crim 577; reliability presumption = DPP v McKeown [1997] 1 WLR 295. FIX: replace "R v Bates".
- MAJOR-2 §7.8 under-concedes the surviving DEFAMATION-BY-INNUENDO exposure of the PACK: a closed channel gives only QUALIFIED privilege (defeasible by malice/excess, evaporates on DSAR/litigation disclosure); the innuendo sting (verified key + enrolment provenance + failed checks + RTA s.36 text) survives label removal under bane-and-antidote + is CONTAINED not eliminated. Authority: Defamation Act 2013 s.1; Charleston v News Group [1995] 2 AC 65; Adam v Ward [1917] AC 309. FIX: add a §7.8 clause: closed-channel qualified privilege is conditional + defeasible, residual innuendo persists on disclosure.
- MAJOR-3 CJEU authority cited as if BINDING: SCHUFA C-634/21 (7 Dec 2023) + Breyer C-582/14 are post-transition = PERSUASIVE not binding under EU(W)A 2018. FIX: flag retained-EU persuasive status + cross-check EDPB/ICO Art 22 guidance.
- MAJOR-4 controller/processor step SKIPPED for the private operator: §7.5 jumps to "Art 6(1)(f)+LIA" presupposing the operator is a CONTROLLER; if it processes on TfL's instructions it's a PROCESSOR (Art 28) covered by TfL's 6(1)(e). FIX: resolve controllership FIRST (Art 4(7)/(8)).
- MAJOR-5 the DISSERTATION'S OWN ARTIFACTS aren't expressly safe-harboured: the worked-example fixture + confusion matrix name keys + criminal statutes in the thesis TEXT (not in any closed channel); the reason it's safe = the corridor is SYNTHETIC (no real data subject / no defamable claimant), NOT the §7 deployment controls; also a governmental body cannot sue in defamation (Derbyshire CC v Times [1993] AC 534). FIX: state the thesis artifacts concern synthetic entities so Art 10/defamation bite only on the hypothetical deployment; add Derbyshire.
- MINOR: 6(1)(e) is necessity+legal-basis (DPA s.8) NOT an LIA balancing test; §7.6-vs-§7.1 tension (pack DOES process criminal-offence data via RTA s.36 text -> reconciled by origin-independent selection + the Sch1+APD, not by absence); the attacker-key/colluding-keys ENUM prejudges as hard as "lie" did (defended only by the note+counsel-gate+synthetic substrate - acknowledge in §7.8); disambiguate "misfeasance" (Stovin positive-act sense) from misfeasance-in-public-office (bad faith, Three Rivers No 3 [2003] 2 AC 1); update uk-traffic-law.md "Lambeth" -> Euston A501/TLRN (strengthens TfL-as-controller); verify "Steyn LJ in Lavis" (the s.41 point is Lord Hoffmann in Gorringe) + Three Rivers No 6 is [2004] UKHL 48 not [2005].
- VERDICT: FATAL 0, MAJOR 5. v4's legal treatment is substantively sound + honest on the big concessions; falls short of a CLEAN distinction only on completeness + one case-name error, all cheap. Clears the bar; close M1-M5 to make the legal chapter unimpeachable. [ALL no-regret: apply in v5.]

### Security agent (ac6d0d54) — within scope NEARLY sound but the boundary is UNDERSTATED (FATAL 2, MAJOR 6) [ALL NO-REGRET honesty/correctness fixes; convergent with novelty F2 + methods F1]
- FATAL-1 (in-scope free deviation the boundary doesn't list): a single non-operator key PIGGYBACKS on a legitimately-corroborated real EV - signs an extra conservation-consistent claim that INFLATES the preemption (extra sustained green / extension toward a conflicting approach); live the gate sees a genuinely-corroborated EV so it passes; post-hoc the decision record carries ONE driving_input_seq pointing at the REAL EV sighting, so the attacker's marginal contribution is attributed to the honest driver or to nothing = undetectable live AND untraceable after, strictly IN SCOPE. FIX: make the decision record carry the SET of all materially-influencing inputs (`driving_input_seqs: [...]`), bind preemption MAGNITUDE/DURATION to the corroborated need, and state any input not individually captured is in the free region.
- FATAL-2 (in-scope free deviation + §6.4/§6.6 internal contradiction): a keyless physical sensor-spoof (decoy vehicle / RF / camera) by ONE non-operator actor needing ZERO keys - §6.4's sensor-fed-spoof predicate says this "forced sustained green" while §6.6 says a keyless reading "may NOT force sustained green; otherwise the pack MARKS it 'corroboration not key-attributable'" (a LABEL is not traceability, and "otherwise marks it" concedes the preemption still happens). Undetectable live (looks like a real local detection) + untraceable after (no key) = free, in scope; the very existence of the sensor-fed-spoof origin admits this region is reachable yet §6.11/§13 omit it. FIX: resolve the contradiction - either the gate HARD-REFUSES any keyless-forced sustained green (then sensor-fed-spoof fires only on a build defect, say so), OR admit keyless local spoof as an explicit in-scope boundary item in §6.11/§13.
- MAJOR: (3) the first-party emission-count is INERT in scope (honest operator discloses all, so counts add no in-scope security; their job is the out-of-scope operator boundary) AND lets one key FRAME the honest operator: over-report (claim N+1, emit N) -> reconciliation sees claimed>disclosed = the signature of operator suppression -> INCOMPLETE_DISCLOSURE raised against the honest operator = single-key framing/DoS; under-report mislabels a lying signer as a disclosure gap -> state counts instrument only the out-of-scope operator boundary + define each mismatch DIRECTION's semantics + a lone signer's claimed>disclosed cannot attribute suppression to the operator. (4) "non-equivocating quorum" is ASSUMED not delivered - the concrete anchor (Rekor + a single-node ledger RPC) has NO cross-witness gossip/auditor specified, so writing the same root to 2 witnesses only detects equivocation if something cross-checks them; a partitioning attacker presents different counts to each -> specify the gossip/cross-audit step (who reads both, how disagreement surfaces) + make it a D-anchor condition, else state non-equivocation is ASSUMED not enforced (Chuat CNS2015). (5) external identity root relocates trust to an unspecified CA - conceded in §6.11 but DROPPED from the §13 viva sentence + never stated that every key->entity attribution now rests on an external party whose mis-enrolment is undetectable in-band + who certifies/rotates the root's own key -> add identity-root compromise to §13 + state it's an unverified trust assumption. (6) cold-start corroboration state + non-detector warmup unspecified (only the replay HWM persists; _sightings set empty after restart; §6.8 names only DETECTOR warmup) -> specify the corroboration gate cold-start as FAIL-CLOSED (refuse sustained preemption until the corroboration set is warm) + TTL affects only the live working set never the anchored record. (7) [=novelty F2 + methods, THEOREM BUG] §8's stated assumption conjunction (witness-density>0 ∧ honest-independent-keys ∧ non-equivocating-anchor) does NOT entail "free region empty" - operator creation-time omission is excluded by NONE of the three, so under the literally-stated hypotheses the free region is NOT empty = the theorem as written is FALSE -> add an explicit 4th hypothesis "complete first-party disclosure (no creation-time omission)". (8) the in-scope guarantee is near-VACUOUS + not stated: since corroboration needs >=2 keys, a single key can't manufacture it, so almost every single-key deviation is rejected LIVE (the gate working, not a coupling); the one non-trivial in-scope case is piggyback (Finding 1) which escapes -> state plainly the in-scope undetectable-live effective-deviation set is essentially {piggyback on a real corroborated event} + the substantive content is the MEASURED out-of-scope boundary.
- VERDICT: within scope the coupling is NEARLY sound + far more honest than v3, but NOT complete: 2 in-scope free-deviation classes survive unlisted (piggyback via single driving_input_seq; keyless sensor-spoof), the §8 theorem hypotheses don't entail the conclusion without a no-operator-omission premise, and the count/gossip machinery is inert-or-unspecified in scope. [ALL no-regret: apply in v5.]

### Consistency agent (a2d3cc05) — reframe COHERENT inside MASTER but the round-5 FATAL was NOT actually fixed (FATAL 1, MAJOR 2, MINOR 3) [ALL NO-REGRET]
- Q1 PASS: every load-bearing section (§0/§1/§2/§8/§13) tells ONE story (conditional coupling + characterisation + CT conceded + "deviation" + quorum); no stranded v3 section. Q4 PASS: SSOT rows agree. Q5 PASS: quorum-mandatory-for-the-claim + anchor-out-of-overnight-subset + D-anchor-refuses-mock is NOT a golden-rule violation.
- B-1 FATAL (I FAILED to actually fix v3's B1): §12 STILL reads "The exemption IS LANDED in GROUND-RULES §F + TEST-SPEC (not merely asserted)" while GROUND-RULES:92 + TEST-AUDIT:39/127 are UNAMENDED and MASTER's own §5.7 says "§12 must not claim this is LANDED until it is" + §11 step-1 schedules the amendment as pending = triple self-contradiction + false done-claim (the "is LANDED not merely asserted" phrasing deliberately exits the BUILD-TARGET bucket). FIX: LAND the amendment (add the Job-A/non-evidential exemption to GROUND-RULES §F + TEST-AUDIT:39/127 - doc-only, feasible in the overnight subset) OR reword §12 to "must be STATED IN ... (target, §5.7)" + drop "(resolved)" from §5.3's C1.
- B-2 MAJOR: LEE-business-specification.md:39 carries "detectability envelope" (a manifest-forbidden term) with NO superseded banner + NOT in §5.5 + not allowlisted -> D1 unpassable; add it to §5.5 or the D1 allowlist.
- B-3 MAJOR: PIVOT-SIGNOFF-BRIEF.md:1 uses a BANNER-ONLY over live "blockchain"/old-thesis text (the exact thing §5.2 says does NOT satisfy D1) + not scheduled/allowlisted -> allowlist (dated historical reason) or neutralise in-body.
- MINOR: B-4 dual "canonical SSOT" governance (PROJECT-PROPOSAL:2 self-declares canonical-for-novelty vs MASTER §2) -> demote PROPOSAL's designation or state MASTER precedence; B-5 §5.5 under-enumerates the PROPOSAL purge (envelope also at :160/:186, old thesis at :19/:33/:45); B-6 report.tex path is 05-supervision/report/ not report/.
- VERDICT: NOT only-minor - the reframe is coherent inside MASTER but v4 FAILED to fix the one round-5 FATAL it was written to fix (the "is LANDED" false done-claim) + 2 supervision docs D1 can't pass. [ALL no-regret: apply in v5.]

## ROUND 6 TOTAL: FATAL 13, MAJOR 36, MINOR 3. (Round 5 was 24/36 -> fatals HALVED; legal 3->0.) CRITICAL: every R6 finding is NO-REGRET within the chosen framing - NONE needs a user decision. The loop is converging.
## V5 PLAN (all no-regret, convergent): (1) FINDING = measured critical coverage-density d_crit(footprint) as a function of real Euston geometry + a pre-registered falsifiable hypothesis that geometry moves d_crit off the well-mixed FDI/CT prediction (novelty F1/F2/F3). (2) THEOREM = explicit SUFFICIENT hypotheses [coverage>d_crit(footprint)] ∧ [honest independent keys] ∧ [non-equivocating quorum] ∧ [no operator creation-time omission]; sufficiency proven analytically, necessity shown empirically by the sweep (novelty F2 + methods + security F7). (3) HARD OBJECT = a proven provenance-INDISTINGUISHABILITY theorem for the veracity triad + accuracy ONLY over provenance-decidable origins {keyless-lone-sighting, key-attributed-corroboration, uncorroborated-signed}; DROP the veracity-triad accuracy; system-detector trigger rewritten provenance-only (methods F1/F2). (4) BOUNDARY HONESTY = add the 2 in-scope free-deviation classes (piggyback-inflation; keyless sensor-spoof) to §6.11/§13; driving_input_seq -> driving_input_seqs SET; bind preemption magnitude to corroborated need; state the in-scope vacuity honestly (security F1/F2/F8). (5) SLM = reasoner-of-record producing the map's per-incident labels + lead with Job C; name the single load-bearing job for the kill-criterion; pin a buildable hardened continual baseline; pin numbers under a seal extended to Exp D + Exp C (novelty F4 + methods M4/M5/M6/M7/M8). (6) BUILD = reach the real producer (inject AuditLog into controller ctor, wire EmergencyController into IntegratedSystem behind a flag + phantom+real-EV incident, emit records inline, capture DER+sig at BOTH inbox seams, DELETE _drain_audit decision-mirror, one clock=sim time, distinguish issuer-sig vs sender-sig layers, scope the slm threshold strip to SYSTEM_DISAMBIG+_case_evidence); FIX the payload_min contradiction (sign+store the full NON-personal coordination payload; personal data kept out of the signed message); commit the prerequisite fixtures (E2, forced-skip, ground_rules.yaml) + a startup existence gate (build all). (7) LEGAL = R v Bates->Bates v Post Office (No 6) + Hamilton; CJEU persuasive-not-binding; controller-vs-processor first; Derbyshire + synthetic-substrate safe-harbour; misfeasance disambiguation; 6(1)(e) necessity-not-LIA; Three Rivers No 6 [2004] not [2005] (legal M1-M5+minors). (8) CONSISTENCY = LAND the GROUND-RULES §F + TEST-AUDIT exemption + reword §12 (kill the false "is LANDED"); add LEE-business-spec + PIVOT-SIGNOFF-BRIEF to §5.5/allowlist; demote PROPOSAL's canonical designation; fix report.tex path (consistency B-1/B-2/B-3+minors).

---

# ROUND 7 (vs MASTER-SPEC v5, 2026-07-21) — battery at the MSc-distinction bar

### Novelty agent (a5551923) — v5 clears methodology/threat-model/limitations; the headline is not yet a SEVERE test (FATAL 2, MAJOR 3) [ALL NO-REGRET; converges on ONE distinction-lifting move]
- F1 FATAL: the pre-registered hypothesis is a HEDGED 3-way DISJUNCTION ("real geometry moves d_crit OR creates heterogeneity OR a collusion×density interaction away from well-mixed") - cannot fail (any non-uniform corridor confirms >=1 disjunct; "differs from well-mixed" is foregone since well-mixed is an idealisation nobody expects). A test that cannot fail is not a finding (Mayo 2018, severity). Sole headline object -> caps at merit. FIX: replace with ONE directional POINT prediction with a pre-registered SIGN + MAGNITUDE + a genuine NULL BAND (e.g. "d_crit is >= X% HIGHER than well-mixed because phase-gated coverage creates periodic deserts at approach A; if measured d_crit is within [null band] the hypothesis is REFUTED"); commit the null outcome as publishable.
- F2 FATAL: "real geometry" is UNEARNED - turning counts give FLOW not sensor/witness COVERAGE; the turning-counts->honest-independent-sighting-coverage map is a modelling layer never grounded in data, so "real geometry moves d_crit" is really "my witness MODEL moves d_crit" + Euston counts are decorative; compounded by §6.6 conceding independence needs a measured position source "absent which route-consistency adds no security" (so the very axis the finding measures is often ASSUMED). FIX: derive the witness/sighting process from an EMPIRICAL source (documented A501 detector/loop placement, or a cited probe-vehicle penetration rate applied to the flows) + pre-register it under the seal; state per-cell where independence is MEASURED vs ASSUMED + exclude assumed-independence cells from the headline.
- M3 MAJOR (the real increment): the delta over Kosut/Hendrickx is THIN as framed - "approach-weighted spatial non-uniformity" is INSIDE their placement-dependent-unobservability result; a routine sim reproduces spatial heterogeneity for free. The genuinely ABSENT thing is signal PHASING: green/red windows make coverage periodically TIME-VARYING, so observability is spatio-TEMPORAL not just spatial (not in Kosut TSG2011, Hendrickx TAC2014, Liu-Ning-Reiter, Teixeira-Sandberg). FIX: make the PHASE-GATED TEMPORAL coverage condition the explicit novel object (footprint must be covered by an independent sighting WITHIN ITS LIVE PHASE WINDOW) + show a corridor result the static model cannot produce (a demand/phase regime where average coverage is high but a phase-locked pocket keeps d_crit un-crossed).
- M4 MAJOR: the SLM is STILL separable from the boundary result - the methods reframe made the scored origins mechanical/provenance-decidable, so a DETERMINISTIC function decides the labels load-bearing to d_crit = the SLM is redundant FOR THOSE; "reasoner-of-record producing the map's labels" (§1/§3) is an overclaim. FIX: stop asserting the SLM is the map's instrument; present it as a SECOND self-contained contribution (citation-faithful legal reasoning on novel statute combos - Dahl et al Stanford 2024; + Job C adaptivity tradeoff) with its own kill-criterion, decoupled from the headline. [SLM stays mandatory + a genuine pillar - honesty fix, not a demotion.]
- M5 MAJOR: d_crit's crossing LEVEL is sealed but externally-unanchored (an arbitrary-but-sealed level yields an arbitrary-but-honest number) -> anchor the crossing level to an EXTERNAL operational-harm threshold (sec EV delay / FP% budget), same discipline as the Exp-1 MDE, so d_crit denotes a coverage below which harm exceeds a stated bound.
- CREDIT: the 4-hypothesis conditional theorem (the added no-operator-omission premise fixes the R5 bug), provenance-indistinguishability, severed scoring label, the pre-registered grid, the sealed bundle, the honest in-scope free-deviation list, the concrete legal treatment - all distinction-shaped; rigour + honesty ARE present.
- VERDICT: high merit / borderline distinction; the sole headline prediction is not yet severely falsifiable + its "real geometry" rests on an ungrounded witness model. SINGLE HIGHEST-LEVERAGE CHANGE (collapses F1+F2+M3): replace the disjunction with ONE directional magnitude-pinned prediction tied to the SIGNAL-PHASE-COUPLED TEMPORAL coverage mechanism (the genuine gap in Kosut/Hendrickx's static model), driven by a DATA-GROUNDED sighting process with a real null region -> converts the calibration table into a riskable geometry-specific finding + lands a clean distinction. [ALL no-regret: apply in v6.]

### Security agent (a429aeca) — within scope the coupling is essentially sound; the boundary is NOT yet honestly complete (FATAL 1, MAJOR 4) [ALL NO-REGRET]
- F-1 FATAL (boundary understatement + §6.4 overclaim): COMPOUNDED KEYLESS TRANSIENTS are unlisted - the adversary needs ZERO keys, each spoof induces a transient preemption below the sustained-refusal threshold so §6.4 refuses none individually, but fired once per cycle at an approach the aggregate is a persistent green-time bias = SUSTAINED physical impact on the exact axis the thesis measures; conservation-consistent (a real decoy briefly present) so CUSUM/§6.8 doesn't fire, keyless so no trace; NONE of the four theorem hypotheses exclude it (coverage/keys/quorum/omission are all irrelevant to a keyless local reading). §6.4/§6.11/§13 present the keyless class as impact-bounded - false for the repeated case. FIX: add a CUMULATIVE keyless-driven-preemption BUDGET per approach per window (rate-limit transient keyless effects) OR list the compounded-transient class explicitly + state §6.4's refusal bounds per-EVENT not aggregate impact; bound the measured physical impact of the keyless class in the boundary map.
- MAJOR: (F-2) "preemption magnitude bound to corroborated NEED" has NO defined need functional + no code referent; if need is computed from attacker-influenceable sighting content, an added sighting inflates the computed need (in scope it's key-attributed = traceable, so not a free break, but the bound is asserted not proven) -> pin the need functional + require its inputs provenance-verified/measured not self-asserted + prove magnitude monotone only in measured quantities. (F-3) §6.6's "measured non-attacker-authored position source" CONTRADICTS §6.4's spoofable local sensing - if the position source is a physical sensor of the same class the "non-attacker-authored" qualifier is false in scope (the >=2-key rule prevents a soundness break, but it's an unflagged tension + overclaim) -> require the position source provably OUTSIDE the keyless-spoof surface, or state the independence check inherits the keyless weakness. (F-4) the driving_input_seqs "materially-influencing" filter is DISCRETIONARY + set by the attacked path - a signed contribution crafted to be individually sub-material is dropped from the set (why piggyback is "free"); the free class is partly SELF-INFLICTED -> log the FULL set of consumed inputs with NO materiality filter (materiality is a display concern not a logging one); state any filtered input is in the free region. (F-5) the cross-auditor RELOCATES equivocation to "who audits the auditor" - §6.7 requires it be NAMED not INDEPENDENT-of-the-operator (an operator-colluding auditor silently reinstates equivocation) + a SINGLE auditor contradicts the cited CT gossip requirement (Chuat CNS2015 needs multiple); moot in scope but under-surfaced -> add cross-auditor compromise to §13 alongside identity-root + state a single auditor is a weaker instantiation than the cited multi-auditor gossip + auditor honesty/independence is an unverified assumption.
- CODE NOTE (pre-teardown, not weighted): emergency_controller.py:296,344 still auto-trusts local_sensing as "cannot be spoofed"; fault_attribution.py:6,68,165 still reads a singular driving_input_seq + self-asserted corroboration_count; none of the v5 constructs (need functional, transient/sustained split, driving_input_seqs SET) exist yet.
- VERDICT: within scope the coupling is essentially SOUND (the >=2-key rule rejects single-key phantoms live; v5 genuinely closed the round-6 findings) but the boundary is NOT honestly complete - the compounded keyless-transient attack yields sustained impact no hypothesis excludes + no mechanism bounds, while §6.4/§6.11/§13 present the keyless class as impact-bounded. Fix F-1 (cumulative keyless budget + honest listing) -> boundary complete; the 4 MAJORs are overclaim/understatement tightenings that don't falsify the in-scope coupling. [ALL no-regret: apply in v6.]

### Methods agent (abd836e7) — §8/§3 NOT valid; v5's two "analytic" objects lack the model they need (FATAL 4, MAJOR 5) [ALL NO-REGRET]
- F1 FATAL (converges with novelty M4): the provenance-decidable confusion matrix is TRIVIAL by construction - the 5 scored origins are separated by exactly 2 mechanical record features (signing-key count 0/1/>=2 + recomputed-corroboration presence), so the engine label is a deterministic partition f(record) and accuracy = whether a 5-line if/else parses fields correctly = ~100% by construction, a unit test not a measurement. The one genuine ambiguity (honest-independent vs colluding corroboration) collapses either way: if the measured position source exists independence is mechanical (trivial); if not, colluding-keys is non-separable -> forced to unknown (per the spec's own F1/F2), leaving pure key-counting (still trivial). D6's separable-fraction floor is INVERTED - it fails when too FEW are separable (empty), but vacuity lives at the HIGH-separability end (fraction ~1.0 maximally PASSES the floor). FIX: concede origin-classification over provenance-decidable origins is a deterministic VERIFICATION CHECK (~100%, near-zero info) NOT a measurement, DEMOTE from primary; relocate the PRIMARY SLM metric to citation-correctness + FCR on novel legal combos (Job A, where genuine judgment exists); redefine D6 as a TWO-SIDED gate (fail if separable fraction below a floor OR if per-cell accuracy is not statistically below 1.0 on cases engineered to require a non-trivial corroboration-validity judgment - stale/out-of-window/route-inconsistent sightings - i.e. force a decision the deterministic partition cannot make, or drop "accuracy" as a claimed finding).
- F2 FATAL: d_crit is an unpinned LEVEL-CROSSING of a measure-dependent curve with NO estimator + NO error model (needs: a free-region-fraction estimator per density, a functional form to locate the crossing, a CI method - Fieller/profile-likelihood/curve-bootstrap; spec pins none) AND it RESURRECTS the aggregate scalar the R6-M3 fix banned ("per-cell SURFACE, no aggregate scalar") - d_crit(footprint) IS a scalar from that surface; the crossing LEVEL L is a free DOF pinned to nothing, so the "gap vs well-mixed" is tunable through L. FIX: either (i) DROP the d_crit scalar + report the full coverage-vs-escape SURFACE with per-cell MC proportion CIs under a pinned sampling distribution, framing the finding as the surface SHAPE (heterogeneity, collusion×density interaction) tested vs the well-mixed surface by a pre-registered functional CONTRAST; OR (ii) keep d_crit but pin the estimator + sampling measure + crossing functional form + level L (from an external operational threshold) + a named CI method, with the geometry-moves-d_crit hypothesis as a CI on the gap that can exclude zero. [Synthesise with novelty F1's severity: a pre-registered directional functional contrast on the surface shape - e.g. a phase-locked pocket keeps escape high where well-mixed predicts low - IS both a severe test AND avoids the unpinned scalar.]
- F3 FATAL: the "provenance-indistinguishability THEOREM" has no proof object (no formal observable space Phi, no state->observable map, no indistinguishability notion; "committed under the seal" is process not proof). Two readings both kill the "theorem" label: (a) if Phi includes any physical signal surviving into records, veracity-separability is an EMPIRICAL property of the injector (a realistic injector where honest-sensor-error tends keyless + authentic-deviation signed makes the triad PARTIALLY separable -> the "theorem" is FALSE + forcing-to-unknown discards real signal); (b) if Phi excludes all physical signal, indistinguishability is the TAUTOLOGY "a log that never recorded ground truth can't recover it" (data-processing inequality over a channel that never carried the bit) = a correct LIMITATION (MSc-fine) not a proven hard object. v5 promoted the round-6 admission to "a PROVEN theorem" = inflation. FIX: drop "theorem" unless you supply the proof object (define Phi = the exact verified-provenance feature set, define the injector state->observable map, prove veracity-invariance / Phi is a sufficient statistic discarding veracity); ABSENT that, restate honestly as a design LIMITATION with a one-line DPI argument ("the audit channel carries no physical-veracity signal, so the system never adjudicates it") - defensible, needs no theorem.
- F4 FATAL: the analytic nonlinear Bayes ceiling is UNDERIVABLE - §9 lists the FIELDS an injector model must have (feature schema, noise params, Bayes floor, density floor, integration method) with NO distribution family, NO parameters, NO likelihood expression, NO floor number = a schema-of-a-spec (round-6 M2 persists; confirmed on disk: no injector artifact exists, experiment-1 doc never mentions p(x|y)/likelihood/Bayes/parametric). No model -> no ceiling -> the pre-registered "MDE on ceiling-minus-SLM gap" (D4/D5) is undefined; F3's proof + F4's ceiling share the SAME missing root. Secondary: a Bayes ceiling is model-relative; unless the injector reproduces real SUMO observables, "SLM near ceiling" measures agreement with an experimenter model not near-optimality. FIX: before any seal, WRITE the actual p(x|y) (named family per class, explicit params, closed-form/bounded-error likelihood, numeric density floor, integration method) in the §9 artifact + a model-adequacy check vs real-substrate observable moments (else label the ceiling "model-relative"); if unwriteable, remove the analytic-ceiling claim + report without an optimality reference.
- MAJOR: (M1) the single-load-bearing-job kill-criterion is structurally falsifiable but the bar height (MDE, FCR ceiling, dominance rule) is name-only + the template's ABSTENTION-scoring is an open DOF (score abstention as neutral -> SLM clears it by emitting anything sometimes-right = round-4 F5 reopened) -> pin numeric MDE + FCR ceiling from an external legal-harm threshold under the seal + pin the abstention-scoring rule (abstention = correct-refusal where no rule applies, incorrect where one does). (M2) Job C's hardened-baseline STRENGTH + mixing WEIGHT are win-deciding DOFs ("plausible sanitiser" = strawman; the weight decides the sign of net harm) + gate/report ambiguity (§3 says reported-not-sufficient, D-SLM-kill lists it as a bullet reading as a gate) -> pin the baseline to a SPECIFIC published LLM-continual defense with demonstrated applicability + pin the weight to an external threat-likelihood estimate + state explicitly Job C is reported-only not a survival gate. (M3) the independence predicate is "assumed not verified" (no non-attacker-authored position source specified for Euston; turning counts are volumes not per-sighting position truth) = a load-bearing theorem hypothesis that's unfalsifiable + collapses colluding-keys vs legitimate (feeds F1) -> name the concrete measured position source + its test, or state plainly the honest-independent-keys hypothesis is UNVERIFIED in this substrate so the coupling is conditional on an unchecked assumption. (M4) TWO conflicting "primary" designations - §3 "origin-classification accuracy is PRIMARY" vs the kill-criterion "citation-correctness is the single load-bearing job"; F1 shows origin-accuracy is vacuous -> designate citation-correctness+FCR the SOLE primary, reclassify origin-classification as a verification check. (M5) the seal binds ZERO values - every "numerically-pinned" threshold is name-only + the prerequisite artifacts (ground_rules.yaml, §9 injector model, anchor.md, euston-build.md) are ABSENT on disk, so a one-hash seal over an empty set is process theater -> author+commit all pinned numbers + artifacts, hash, THEN permit runs; until then §12 must label the seal "target, not yet in force" NOT a passed control.
- VERDICT: NOT valid at the distinction bar as written; v5 is markedly more honest + clears methodology/threat-model/limitations, but the two "analytic" objects it introduced (the indistinguishability THEOREM + the analytic Bayes CEILING) both lack the explicit generative/observable MODEL they require (which doesn't exist), the headline d_crit is an unpinned level-crossing with no estimator/CI, and the PRIMARY metric is a deterministic field-parse ~100% by construction with D6's failing condition pointed the wrong way. Fixable by: writing the generative model, pinning d_crit's estimator+CI+level (or reporting the surface + a functional contrast), demoting the trivial matrix for citation-correctness, before anything is sealed/run. [ALL no-regret: apply in v6.]

## ROUND 7 PARTIAL (novelty + security + methods in; build a5620130 + legal a50431046 + consistency a9559eea PENDING at session limit): FATAL 7, MAJOR 12 so far. Trend still converging vs R6 per-agent (novelty 2->2, security 2->1, methods 2->4 [methods went UP - it dug into the "analytic" claims v5 newly added]). ALL findings no-regret; NONE need a user decision.
## V6 SYNTHESIS PLAN (so far, all no-regret; finalise after build/legal/consistency land):
- HEADLINE FINDING: replace the 3-way disjunction with ONE pre-registered directional functional CONTRAST on the coverage-vs-escape SURFACE, tied to the PHASE-COUPLED TEMPORAL coverage mechanism (footprint must be covered by an independent sighting WITHIN its live phase window - the genuine gap in Kosut/Hendrickx's STATIC model), on a DATA-GROUNDED witness process (A501 detector placement or a cited probe-vehicle penetration rate, NOT a stipulated model), with a real NULL region + per-cell MC CIs (novelty F1/F2/M3/M5 + methods F2). Report the SURFACE not an unpinned d_crit scalar; if d_crit kept, pin estimator+CI+level-from-external-harm-threshold.
- HARD OBJECT: DEMOTE origin-classification to a deterministic VERIFICATION CHECK (~100%, not a finding); make citation-correctness + FCR on novel legal combos the SOLE primary SLM metric (methods F1/M4, novelty M4). Restate provenance-indistinguishability as a design LIMITATION with a one-line data-processing-inequality argument, NOT a "theorem" (methods F3). Two-sided D6 gate.
- INJECTOR MODEL: write the actual p(x|y) (named family, params, likelihood, floors, integration) as a prerequisite artifact + a model-adequacy check, else drop the analytic-ceiling claim (methods F4/M5).
- SLM: present as its OWN second contribution (citation-faithful legal reasoning + Job C adaptivity tradeoff), NOT the instrument of the boundary map; SLM stays mandatory (novelty M4). Pin the kill-criterion numbers + abstention-scoring + Job-C baseline/weight + reported-not-gate (methods M1/M2).
- BOUNDARY: add the COMPOUNDED-KEYLESS-TRANSIENT class + a cumulative keyless-preemption budget per approach per window; §6.4 refusal bounds per-event not aggregate (security F-1). Log the FULL consumed-input set (no materiality filter) (security F-4). Add cross-auditor-compromise + the single-auditor-vs-gossip weakness to §13 (security F-5). Pin the "need" functional over measured inputs (security F-2). Flag §6.6-vs-§6.4 position-source tension (security F-3).
- SEAL: label "target, not in force" until the numbers + artifacts exist + are hashed (methods M5).

### Build agent (a5620130) — Fable STILL can't build the §11 subset unattended tonight (FATAL 6, MAJOR 8) [NO-REGRET; but mostly "author the artifacts/wire the code", NOT spec edits]
- v5 fixed the PROSE (schema/seams/tamper-rebuild/payload_min) but reopened an equal set at the CODE/SUBSTRATE level: the pinned producer FIELDS need VALUES the live controllers + FakeConn can't supply tonight.
- F1: the real-EV incident CRASHES on CI substrate - EmergencyController._sense_local_ev does getattr(self.c.vehicle,...) but FakeConn has NO .vehicle attr -> AttributeError; AND the phantom claim in DEFENDED mode = withheld = NO false_preemption record, so "assert attacker-key on a false_preemption" has nothing to assert (only NAIVE mode corroboration_required=False produces it, never pinned). FIX: pin the wired incident runs the naive victim + a committed FakeConn vehicle-API stub + pinned attacker/victim/real-EV junction ids.
- F2: policies_applied can't be honestly populated overnight (no producer emits P-ids; ground_rules.yaml is OUT of subset) + §12 aborts empty policies_applied -> every run aborts OR Fable fabricates ids = NEW v5 contradiction (step-2 "emit policies_applied inline" vs prereqs-out vs §12 abort). FIX: define a tiny committed LOCAL check-id set (corroboration/conservation_band/replay/membership) decoupled from the corpus, OR exempt the overnight-wired records from the abort like the Job-A note.
- F3: classification computed by NO producer (EmergencyController emits admissible/corroborated bools; CoordinatedController emits detections; neither emits a classification string; conflated with the post-hoc ORIGIN enum). FIX: pin classification := LEGITIMATE if admissible else SPOOFED_OR_FAULTY (EV) / from detections[].flagged (coord); state decision.classification (runtime) is distinct from the engine ORIGIN.
- F4: "one clock = sim time" is INCOMPATIBLE with the signed tick + replay guard - message.t is inside canonical_bytes(sender,t,payload) AND the replay key (recipient,sender,t); forcing sim time breaks the signature + collides the replay guard; on the fake path FakeConn has no simulation.getTime so decision.t==0. FIX: keep the signed per-message LOGICAL tick as message.t; pin ONLY decision.t + outcome.t to sim time; give FakeConn a monotonic getTime; state message.t and decision.t are different clocks by design.
- F5: driving_input_seqs SET has NO population rule + no seq captured on the EV path (_ingest_ev_inbox discards signatures + captures no seq; _sightings stores no seq) + fault_report resolves ONE seq->ONE culprit (no SET->culprit rule). FIX: define material = every signed input appended this decision that fed the used phase or the corroborated need; store (seq,sighter_key) in _sightings; pin the marginal-attribution rule (inflation claim whose need exceeds corroborated magnitude -> attacker-key).
- F6: prerequisite oracles ABSENT on disk (ground_rules.yaml, assessment.py, euston*, E2 fixture, forced-skip fixture, anchor.md) -> the §10 gate SKIPs the headline accountability items = ship untested, OR Fable self-authors the expected-verdict (tautology). FIX: author + git-commit E2/forced-skip/ground_rules during hardening BEFORE the run + a reviewed non-self-satisfiable existence gate.
- MAJOR (8): ctor AuditLog-injection param name/position unpinned (CoordinatedController.__init__ has 18 params + no audit param; EmergencyController takes *args/**kwargs); the config flag unnamed/untyped vs the frozen SystemConfig; system.py still builds only a bare CoordinatedController (no EmergencyController/ev_bus/inject_phantom anywhere); _audit_bundle's decision_entries=n-registry_entries mislabels once decide() appends kind:message; EmergencyController.decide has NO self.events.append site for the preempt (writes ev_events only) = a NEW emission point not a mirror; MaxPressureController.step has no skip counter/max_skip/counter + operates on green-phase indices not "approaches" (unit mismatch) + max_skip pinned only ">0" no number; the round-6 "forbid corroboration_count scalar" fix has no emitted substitute (_catchable_phantom still reads it, no kind:sighting w/ seq produced); assessment.py is a full REWRITE of fault_attribution.py not a rewire.
- VERDICT: NO. v5 closed the text-level fatals but the pinned producer fields require values the live controllers + FakeConn can't supply tonight; 6 fatals are code/substrate-level, not prose. [NO-REGRET but the fix is AUTHOR THE ARTIFACTS + WIRE THE CODE during the prerequisite/hardening phase - most cannot be closed by editing MASTER-SPEC.]

### Consistency agent (a9559eea) — round-6 B-1/B-2/B-3 RESOLVED; 2 one-line MAJOR survive (FATAL 0, MAJOR 2, MINOR 7) [ALL NO-REGRET]
- Round-6 B-1 (false "is LANDED") genuinely converted to honest TARGET-STATE (verified GROUND-RULES:92 + TEST-AUDIT:39/127 still unamended so "not landed" is honest); B-2 (LEE-business-spec) + B-3 (PIVOT-brief) RESOLVED (scheduled + allowlisted); B-4/B-5/B-6 resolved-or-scheduled. No stranded v4 framing survives (grep for "three-hypothesis"/"veracity-triad accuracy"/"witness-density>0"/"no-free-lie"/"attribute-to-key"/"detectability ROC" found only negations/changelog/manifest). Q1 one-story PASSES.
- #1 MAJOR: §6.5:124 orders "list identity-root compromise in §6.11 AND §13" but §13:228 OMITS it (§6.11 has it) -> add "an external identity-root whose mis-enrolment is undetectable in-band" to the §13 out-of-scope list.
- #2 MAJOR: the SCORED-ORIGINS set is TRIPLE-VALUED - §2:55 "ONLY {5 origins}" (system-classifier/detector excluded, forced unknown) vs §3:84 "system-classifier/system-detector trigger via a provenance-derived predicate" (scorable) vs §11:194 "EACH enum origin has a trigger + ground_truth SCORING ONLY" (all 7 scored); D6 scores "ONLY provenance-decidable origins" so the set must be single-valued. FIX: reconcile - either §2 drops "ONLY" + admits system-classifier/detector as conditionally-scored-if-a-provenance-predicate-exists, OR §3/§11 state they're non-separable->unknown + never scored. [Convergent with methods F1 + minor #4: resolve by making citation-correctness the primary + origin-classification a verification check, and pin the scored set to exactly the deterministic partition.]
- MINOR (7): §5.3 "(resolved)" label imprecise; free-class count varies 4(§1/§12/§13) vs 5(§0) vs 6(§6.11) -> label as "4 free classes + hypothesis-failure axes"; triad names {authentic-deviation, honest-sensor-error, detector-error} don't map to the enum (honest-sensor-error has no enum home; detector-error vs system-detector) -> state the triad<->enum mapping; §5.5 under-enumerates envelope docs (DEMO-REPORT:107, FORMAL-SPECIFICATION:184/240, GLOSSARY:59); §2:55 "is PROVEN" tense vs §8:153 "PROVE" target -> "target: provably..."; spec.md:10 still calls PROPOSAL "canonical" -> add to the §5.5 spec.md edit.
- VERDICT: NOT strictly only-minor - 2 one-line MAJOR internal inconsistencies (identity-root missing from §13; triple-valued scored-origins set); no FATAL; the reframe itself is sound. [ALL no-regret one-line edits: apply in v6.]

### Legal agent (a50431046) — §7 sound + honest; 2 FATAL are UNAPPLIED fixes to the KB, not §7 (FATAL 2, MAJOR 4, MINOR 4) [ALL NO-REGRET]
- §7 genuinely closed round-6 M1-M5 (Bates No6/Hamilton/McKeown, Charleston/Adam v Ward, SCHUFA/Breyer-persuasive, Derbyshire, controller-first, Three Rivers No3/No6[2004], misfeasance disambig, 6(1)(e)-as-necessity all present + correct). Remaining exposure is in the KB + a DP-completeness gap §7 skips.
- F1 FATAL: the fixes never touched 01-research/uk-traffic-law.md (the CITED grounding of record per §0) - it STILL frames everything around "a LAMBETH junction" (lines 3,6,333,335,407,421) + routes TfL-defendant reasoning through Lambeth, while §7.5 says A501/TLRN. An examiner cross-reading §7.5 vs its own cited source finds a different junction/route-class + a different controllership logic. FIX: rewrite uk-traffic-law.md Lambeth->Euston A501/TLRN.
- F2 FATAL: uk-traffic-law.md:275 misattributes "s.41 does not cover signs" to "Steyn LJ in Lavis v Kent CC" - the governing authority on the SCOPE of the s.41 duty (fabric/surface only) is Goodes v East Sussex CC [2000] 1 WLR 1356 (HL) (ABSENT entirely) + the no-duty-to-sign point is Lord Hoffmann in Gorringe; the Steyn/Lavis attribution is wrong. FIX: add Goodes + reattribute.
- MAJOR: (M1) §7.5 presents a controller/processor BINARY - omits JOINT controllership (Art 26; Fashion ID C-40/17, Wirtschaftsakademie C-210/16) where the edge operator co-determines means -> raise + dispose of Art 26. (M2) the external identity-root CA (§6.5) is the actor whose enrolment provenance IS the Breyer re-linkage making a key personal data, yet §7 never classifies the CA as controller/processor, its Art 6 basis, or the pack's carried enrolment-provenance controller -> classify it. (M3) §7.8's synthetic-substrate safe-harbour CONTRADICTS §0/§9 (the corridor is REAL Euston geometry - that reality IS the novelty; the ACTORS/keys/incidents are synthetic, not the corridor); + naming a real corridor + "compromised insider operator" could identify a real private maintenance contractor (Derbyshire shields TfL not a private operator; malicious falsehood Defamation Act 1952 s.3 survives Derbyshire) -> fix the safe-harbour basis to "synthetic actors on a real corridor" + concede the residual. (M4) §7.4/§7.8 under-concede that in deployment the SLM PERSISTS a machine attacker-key conclusion AT GENERATION before counsel/review, so counsel-gating cures neither the Art 22 character nor Art 15 discoverability of the already-created verdict -> name this residual.
- MINOR: add Poole BC v GN to the KB; name the DPA Sch 1 paragraph (para 10 / 33-36) in §7.6; name Art 4(5) pseudonymisation + salt-keeps-preimage-personal in §7.7; KB traffic-law substance otherwise sound (RTA s.36, TSRGD Sch14, Griffin, Keyse, Gorringe, Stovin, Bird, RTRA, RVLR, TMA all check out).
- VERDICT: §7 substantively sound + honest + distinction-shaped; NOT complete - 2 fatals are unapplied round-6 fixes that never reached the cited KB (stale Lambeth; Steyn/Lavis misattribution + Goodes missing) + 4 DP-completeness majors. All cheap, none forces a framing change. [ALL no-regret: apply in v6 + a KB rewrite pass.]

## ROUND 7 TOTAL: FATAL 15, MAJOR 26, MINOR ~11. (R6 was 13/36.) Fatals NOT monotone-decreasing (24->13->15) BUT the character has STABILISED + every finding is NO-REGRET (none needs a user decision).
## KEY META-INSIGHT (surface to user at next checkpoint): the remaining fatals now cluster into "the PREREQUISITE ARTIFACTS + WIRING don't exist yet" (build F1-F6: ground_rules.yaml/injector model/euston files/fixtures/controller wiring; methods F4: the actual p(x|y) model) which NO spec edit can close - they need the artifacts AUTHORED BY HAND + the code WIRED. The spec-level fixes (novelty=1 move, security=1 move, consistency=2 one-liners, legal §7 + a KB rewrite, methods precision + demote-the-trivial-matrix) ARE closable in v6. The spec-hardening loop is near its useful end; the next phase is BUILD the prerequisites, not harden the spec further. User said "keep looping indefinitely" - honour it, but flag this honestly.
## V6 PLAN (finalised, all no-regret): [novelty] one directional functional CONTRAST on the coverage-vs-escape SURFACE tied to PHASE-COUPLED TEMPORAL coverage on a DATA-GROUNDED witness process + real null + per-cell CIs; [methods] DEMOTE origin-classification to a verification check, citation-correctness+FCR = SOLE primary, provenance-indistinguishability = a LIMITATION w/ a 1-line DPI argument NOT a theorem, two-sided D6 gate, write the actual p(x|y) as a prereq artifact (or drop the analytic ceiling), pin kill-criterion numbers + abstention-scoring + Job-C baseline/weight + reported-not-gate, seal="target not in force" until numbers exist; [security] compounded-keyless-transient class + cumulative keyless budget, log FULL consumed-input set, need functional over measured inputs, §6.6-vs-§6.4 tension flagged, cross-auditor-compromise + single-auditor weakness to §13; [consistency] identity-root -> §13, single-valued scored-origins set, + the 7 minors; [legal] rewrite uk-traffic-law.md Lambeth->Euston + Goodes/reattribute + Art 26 joint-control + classify the CA + fix the safe-harbour basis + name the auto-persist residual + Poole/Sch1-para/Art4(5); [build] pin ctor injection param + config flag + incident script/mode + FakeConn vehicle stub + classification predicate + local policy-id vocab + clock split (logical message.t vs sim decision.t) + driving_input_seqs population+seq-capture+SET->culprit + max_skip integer + per-approach-vs-phase-index unit + the EmergencyController.decide append site; AUTHOR the prerequisite artifacts (E2 fixture, forced-skip fixture, ground_rules.yaml, injector model, euston files, anchor.md) during hardening.

---

# ROUND 8 (vs MASTER-SPEC v6, 2026-07-21) — battery at the MSc-distinction bar

### Novelty agent (af2ca5f1) — closest to distinction yet; ONE fatal + a genuinely novel object to CLAIM (FATAL 1, MAJOR 4) [ALL NO-REGRET, strengthening]
- F1 FATAL: the witness process STILL has a modelling escape hatch (round-7 F2 half-closed) - "OR a cited penetration rate applied to flows" reintroduces the ungrounded layer (a penetration scalar gives expected probe COUNT not sighting COVERAGE of a footprint within a phase window; converting needs an unstated observation model), so "real geometry moves the surface" collapses to "my model moves the surface" = model-relative = caps at merit. FIX (single highest-leverage): COMMIT documented A501 detector/loop placement (TfL SCOOT/MOVA loop positions on the TLRN, real + genuinely phase-coupled) as the PRIMARY grounding; if penetration used at all, ground the probe-to-sighting observation model in a cited empirical source + pre-register a sensitivity analysis over its free params + TAG each surface cell measured(loops)-vs-modelled(probe) + EXCLUDE modelled cells from the headline contrast. Secures Q1 severity + closes Q3 + converts Q2's increment from modelled to measured, all at once.
- M2 MAJOR (the REAL increment to CLAIM): the genuine unoccupied object is the SELF-REFERENTIAL COUPLING - "the preemption attack CONTROLS the signal phase, which is ALSO the variable that gates honest-witness coverage, so executing the attack opens the very coverage desert that hides it." NOT in Kosut-Jia-Thomas-Tong (TSG2011), Hendrickx (TAC2014), FDI (Liu-Ning-Reiter, Teixeira-Sandberg), nor the domain-closest signal-attack work (Chen et al I-SIG NDSS2018; Ghena et al Green Lights Forever WOOT2014) - none couple attack-lever to observability-gate. v6 only says "periodically time-varying / spatio-temporal" = the ROUTINE framing (dismissible as re-deriving periodic-system observability, Bittanti-Colaneri Periodic Systems 2009). FIX: state the LOAD-BEARING object as the attack-lever==coverage-gate coupling; cite Bittanti-Colaneri to distinguish from routine time-varying observability; claim the domain-specific coupling not the time index.
- M3 MAJOR: the coupling is STILL called "a THEOREM / prove SUFFICIENCY analytically" + §2 "is PROVEN" while being NEAR-DEFINITIONAL (honest witness covers footprint within its live phase -> witnessed -> traceable; content is in the coverage THRESHOLD not the proof) - the R7-F3 demotion (indistinguishability theorem->limitation) was NOT propagated to this object. FIX: restate as a conditional LEMMA, say plainly the implication is near-immediate given the coverage hypothesis, locate the content in the empirical threshold; fix the §2 "is PROVEN" vs §8 "PROVE" tense same pass.
- M4 MAJOR: two-contribution coherence - the connective argument is asserted + a null SLM reads as STAPLED (mandatory-yet-killable SLM joined only by shared substrate; §13 treats it as a trailing clause). FIX: state the ONE question BOTH halves answer - "in a compromised-insider emergency, what can be held accountable MECHANICALLY, and what remains a human/counsel judgment even an on-device reasoner cannot faithfully automate" - framing the possible SLM null as PART of the thesis; rewrite §13 so the SLM is a CO-EQUAL answer, not a trailing clause. (Dahl et al 2024 is contribution 2's anchor.)
- M5 MAJOR: magnitude thinness guard - the direction is nearly a priori (an average hides a zero, Jensen), so "elevated, CI excludes zero" is THIN; distinction content is in the MAGNITUDE + regime. FIX: pin Δ pre-registration to a magnitude that CHANGES an operational decision (a stated EV-delay / FP-budget threshold) so "confirmed" = "operationally material"; state the regime qualitatively (high average coverage but a phase-locked pocket keeps escape high).
- VERDICT: NOT yet distinction as frozen but the CLOSEST v6 has been; all 5 fixable without a framing change. Commit the witness process to documented A501 detector placement (kills the penetration escape hatch) + NAME the phase-as-attack-lever-and-coverage-gate coupling -> crosses the distinction bar. [ALL no-regret: apply in v7.]

### Legal agent (afd54617) — the KB rewrite is CONFIRMED correct; 2 cheap DP-precision majors (FATAL 0, MAJOR 2, +polish) [ALL NO-REGRET; legal now essentially clean]
- VERIFIED CORRECT: Goodes v East Sussex CC [2000] 1 WLR 1356 IS the leading HL authority on s.41 "maintain" SCOPE (fabric/surface, incl repair); the reattribution away from "Steyn LJ in Lavis" is a genuine improvement; Gorringe/Lord Hoffmann correctly source the no-duty-to-sign proposition; Lavis (1992) 90 LGR 416 properly demoted to earlier CA; Poole BC v GN correct. A501/TLRN = "TfL is BOTH highway + signal authority" is factually + legally CORRECT (GLA Act 1999; Highways Act 1980; RTRA 1984) and is a point in the dissertation's favour. Art 26/Fashion ID substantially correct (operation-specific). No citation is wrong.
- M1 MAJOR (a genuine error I introduced): the identity-root CA is mis-classified as "controller/PROCESSOR ... with its own Art 6 basis" = incoherent - a processor has NO own Art 6 basis (processes under the controller's basis on documented instructions, Arts 28-29); a root CA setting its own validation/cert policy determines purposes+means = an independent (or joint) CONTROLLER. FIX: state it is an independent controller (or joint) with its own Art 6 basis; reserve "processor" for solely-on-instructions.
- M2 MAJOR: the private operator qua JOINT controller has no lawful basis specified - it CANNOT ride TfL's Art 6(1)(e) public-task basis (that's only for the PROCESSOR branch), it needs its OWN basis, realistically Art 6(1)(f) + a balancing test (round-4 A3 closed only for the processor branch). FIX: name Art 6(1)(f) + LIA for the operator's joint-controller processing.
- POLISH (no-regret, fold into v7 + KB pass): (3) state Fashion ID's operation-specific LIMIT explicitly (joint control does NOT extend to operations the operator doesn't co-determine - TfL downstream, the CA's enrolment); (4) add a DUAA 2025 caveat (Data (Use and Access) Act 2025 reforms UK GDPR ADM via new Arts 22A-22D) + as-at date alongside the SCHUFA "persuasive not binding" note; (5) KB Goodes entry OVER-ATTRIBUTES the signs/signals point to Goodes in its first clause (Goodes decided ice/surface; the signs proposition is Gorringe) -> tighten the first clause to "transient surface conditions (ice/snow)" + keep signs under Gorringe; (6) note Goodes' ice holding was legislatively reversed by Railways and Transport Safety Act 2003 s.111 inserting Highways Act 1980 s.41(1A) (the fabric/scope PRINCIPLE survives, so reliance is safe) - one-line footnote for honesty; (7) source the "~6,400 junctions" + "591 sites without detection" figures with a TfL source + as-at date or soften to "~6,000"; (8) optionally add Robinson v CC West Yorkshire [2018] UKSC 4 to the omissions set.
- CORRECTLY HANDLED (distinction-grade, for the record): Three Rivers No 3 misfeasance disambiguation; Derbyshire shields TfL not a private operator + malicious falsehood (1952 s.3) surviving; synthetic-actors-on-a-real-corridor safe harbour + the conceded residual; Bird v Pearce vs Gorringe/Stovin/Poole = NOT_ASSESSED; the always-on note failing Waugh/ENRC; Bates/Hamilton/McKeown on curation-as-adjudication.
- VERDICT: FATAL 0, MAJOR 2. Legal is now sound + honest + very nearly complete; the only substantive blemishes are 2 DP-precision points, neither distinction-blocking for a non-GDPR-core dissertation. Fix M1+M2 + the KB polish -> legal section clean. [ALL no-regret: apply in v7.]

### Methods agent (a6e7b5a7) — more honest + now CAN-fail, but the headline test isn't yet a computable object + the SLM baseline is rigged (FATAL 2, MAJOR 7) [ALL NO-REGRET]
- F1 FATAL: the sole falsifiable headline statistic (the "functional contrast") is STILL not a well-defined statistical object - (a) the contrast functional is name-only (point-difference / region-integral / difference-in-differences all have different sampling distributions); (b) the "well-mixed surface" comparator is itself an unpinned numerical MODEL (Kosut/Hendrickx give an observability condition NOT an escape-proportion-vs-coverage curve, so mapping needs its own parametric assumptions); the seal lists Δ+null+phase-window but NOT the comparator's parameterisation, so Δ=measured-well-mixed is tunable through an unsealed DOF (round-7 F2/F4 relocated from d_crit's level L to the whole comparator surface). FIX: pin under the seal (1) the contrast functional as an explicit estimator over named cells; (2) the well-mixed comparator as a closed-form numerical curve with stated params + variance status (fixed constant vs random-with-CI); (3) the decision rule = "lower one-sided CI bound on the contrast EXCEEDS Δ" (NOT "excludes zero"); (4) add the comparator to the model-adequacy check. Justify the null band as a LIVE possibility not a strawman or severity isn't earned.
- F2 FATAL: making citation-correctness the SOLE primary exposes a RIGGED baseline on the exact cases that define the test - on NOVEL combos the template has no exact match -> fires the fallback -> under the pinned abstention rule ("incorrect where a rule applies") it AUTO-LOSES on every novel-applicable cell = round-4 F5 reopened; the SLM "dominates" structurally not by judgment; since citation-correctness is the single load-bearing metric + the kill-criterion is an AND on it, a rigged baseline makes the SLM's whole contribution unfalsifiable-in-its-favour. FIX: score the template's "nearest applicable rule" for CITATION-CORRECTNESS on the same gold as the SLM (abstention reserved ONLY for genuine no-rule-applies cells); state a template near-miss is a WRONG citation not an abstention, for BOTH arms.
- MAJOR: (M1) MC-only CI + "excludes zero" is SEED-SHRINKABLE (per-cell CI quantifies MC seed noise not corridor uncertainty; at large N any nonzero contrast excludes zero) + the contrast CI method is UNNAMED while Exp-1 names BCa/permutation/Holm/Fieller -> make the operative rule the magnitude-anchored one-sided bound + name the method (curve-bootstrap/Fieller on the surface functional) + fix N under the seal. (M2) GEOMETRY n=1 - one real corridor = geometry-level sample size ONE; all CIs are conditional on that geometry -> state the inferential target is THIS corridor (a quantified case study), report sensitivity to demand/phase-offset draws as the only generalisation axis, drop any "corridors are like this" phrasing. (M3) the two-sided D6 second arm is gameable both ways + doesn't force a genuine measurement (doesn't say whether the partition or the SLM is the classifier of record; uses experimenter-CONSTRUCTED cases not a sealed distribution) -> SEAL the engineered-case generator as a fixed distribution with a pre-registered expected error profile, NAME the SLM (not the partition) as the classifier scored, state plainly the matrix produces NO claimable accuracy finding by design. (M4, new) the one-line DPI argument is OVER-STRONG/false as stated - "the audit channel carries no physical-veracity signal" contradicts §6.6 (signed sightings carry position/timing/route = PARTIAL physical signals, so a realistic injector can make the triad PARTIALLY separable) -> state "no signal SUFFICIENT to adjudicate veracity" + cite the injector model-adequacy check, not a channel-carried-no-bit tautology. (M5) "target not in force" is honest (blocks false credit) but confesses NO pinned value currently exists so nothing in §8/§3 is presently falsifiable; "intend to seal later" is not pre-registration -> deliver the artifacts + hash the numbers; rename the state "pre-registration PROTOCOL, not yet a pre-registration". (M6) per-cell descriptive CIs across the grid have NO multiplicity statement (the single pre-registered contrast controls FWER correctly, but any per-cell claim would be uncontrolled) -> label per-cell CIs descriptive/non-inferential, forbid secondary claims without a correction. (M7, new) the two-metric dominance primary can produce NO winner ("higher correctness but higher FCR") + the AND kill-criterion's behaviour on split-dominance is undefined -> pin under the seal the split-dominance resolution (default to kill/demote UNLESS FCR<=ceiling AND correctness clears the MDE).
- VERDICT: v6 markedly more honest + converted the un-failable disjunction + inverted origin-floor into can-fail structures, BUT the headline's only inferential test is still not a computable statistical object (comparator + estimator unpinned + outside the seal) + the now-sole SLM primary rests on a baseline rigged on the very cases that define it. Recoverable by pinning the contrast functional + comparator + un-rigging the template scoring BEFORE any seal is in force. [ALL no-regret: apply in v7.]

### Security agent (a778f8c6) — coupling sound for the CLASS it resists; the 2 NEW bounding mechanisms v6 added don't bound their residuals (FATAL 2, MAJOR 3) [ALL NO-REGRET; the fix is HONEST CONCESSION, not more mechanism]
- Per-item: §6.1/§6.5/§6.7/§6.9/§6.10 CONFIRM sound (esp §6.5 genuinely closes the round-4 operator re-registration framing hole; §6.7 single-auditor weakness honestly in §13); §6.2 confirm-intent (but widens the internal data-subject surface, M3); §6.3 PARTIAL (SET->culprit half broken, M1); §6.4 REFUTE (F1); §6.6 PARTIAL (F2); §6.11 PARTIAL (contradicts §6.3, M1).
- FATAL-1: the cumulative keyless BUDGET is NOT a real aggregate bound + introduces a NEW DoS - (a) per-approach accounting < per-junction harm unit: spoof sub-threshold transients across all N approaches, each under its own ceiling, delivering N× bias to one victim movement (per-approach budgeting provably doesn't bound the per-junction aggregate); (b) the window RESETS by design -> a permanent bias at rate B/W forever (B,W unpinned, not tied to the harm threshold, so "measured ceiling" is a target not a bound); (c) CHARGING is circular-or-DoS: to charge the RIGHT transients the budget must distinguish attacker spoof from genuine keyless detection = the veracity distinction §8 concedes is provenance-INDISTINGUISHABLE -> either charge ALL keyless transients (a subsequent REAL keyless emergency is refused = golden-rule violation the mechanism itself creates) OR only flaggable ones (the attacker's indistinguishable spoofs never trip it). FIX: pin B, W, tie ΣB/W AGGREGATED AT THE JUNCTION below the external operational-harm threshold; concede it charges ALL keyless transients (accept + bound the emergency-refusal exposure); add budget-exhaustion->refused-real-emergency to §13.
- FATAL-2: the need-functional MAGNITUDE bound is VACUOUS vs a keyless adversary - §6.6's "magnitude monotone only in measured quantities" is offered as a bound, but §6.6 itself concedes the measured position source may share §6.4's keyless-spoofable surface; the concession is applied to the independence/existence check but NOT the magnitude functional -> on a genuine >=2-key-corroborated emergency the attacker keyless-spoofs the need inputs to INFLATE magnitude/duration = invisible-live (rides a corroborated EV), untraceable (no key), not bounded (the >=2-key rule protects preemption EXISTENCE not MAGNITUDE; per-event refusal doesn't fire because the event is genuine). §6.11 lists keyless-transient (forcing a transient) but NOT this magnitude-inflation-of-a-genuine-sustained-event variant. FIX: carry the §6.6 keyless-surface concession into the MAGNITUDE functional; list keyless magnitude-inflation as an in-scope FREE class in §6.11/§13; require the need functional's measured inputs provably OUTSIDE the keyless surface for monotonicity to bound anything.
- MAJOR: (1) the marginal-attribution counterfactual is UNDEFINED (needs a LEAVE-ONE-OUT recompute excluding the suspect sighting; as written it's a circular global comparison - if the attacker's extra sighting is counted INTO corroborated magnitude nothing "exceeds" it) + SUB-MARGIN evasion (keep each marginal contribution below the unpinned detection margin, compound) + §6.3 (piggyback->attacker-key TRACEABLE) CONTRADICTS §1/§6.11/§13 (piggyback listed as a FREE/untraceable class); the only coherent reconciliation (marginal rule catches SUPRA-margin, only SUB-margin is free) is never stated + the margin never pinned. FIX: define the counterfactual as leave-one-out; pin the detection margin; state the free piggyback class is the SUB-margin regime; reconcile §6.3 with §6.11. (2) a SINGLE named cross-auditor is weaker than the cited CT multi-auditor gossip (Chuat CNS2015) + its honesty/independence is assumed -> make the "assumed" trust explicit, on par with the identity root. (3) full unfiltered (seq,sighter_key) logging (the F-4 fix) enumerates every sighter key per decision + combined with the key->entity map + enrolment provenance ENLARGES the internal-record Art-10/innuendo surface -> a §7 completeness item.
- VERDICT: within scope the coupling is essentially SOUND for the class it resists (>=2-key corroborated deviations caught live), but NOT yet sound at the BOUNDARY + the boundary NOT honestly complete - v6's two NEW bounding mechanisms (keyless budget, measured-need magnitude functional) are asserted bounds that remain FREE in scope, and the budget forces a circular-or-DoS dilemma. Close F1/F2 by pinning the budget at junction-aggregate below an external harm threshold + conceding it charges all keyless transients, and by carrying the keyless-surface concession into the magnitude functional + listing keyless magnitude-inflation as free. HONEST CONCESSION not framing change; the agent notes the binding constraint is now the UNBUILT prerequisite artifacts + wiring (per the log's own meta-insight). [ALL no-regret: apply in v7.]

### Build agent (a2fa9e1e) — NOT buildable unattended tonight; confirms diminishing returns (FATAL 5, MAJOR 6) [ALL NO-REGRET; explicitly "build the prerequisites by hand"]
- v6 matched several pins to real code exactly (audit_log=None kw-only ctor, naive-victim mode, admissible/corroborated/detections at emit time). 4 round-7 code/substrate fatals survive + the incident is too low-cardinality to distinguish a correct assessment.py from a STUB.
- F1: SET->culprit marginal rule references a magnitude NO record carries - real code has no need functional, no preemption magnitude (preempt is binary _phase_serving), _corroborated returns a BOOL, EV payload is {ev_id,target,approach_edge}; only the degenerate zero-corroboration form is computable + that's the only thing the single-phantom overnight incident exercises. FIX: for the overnight subset restate the rule as its computable form (culprit=attacker-key iff recomputed independent-corroboration count==0 over driving_input_seqs); explicitly DEFER the "need>corroborated magnitude" form to Experiment D + mark NOT-IMPLEMENTED overnight (so Fable can't invent a placeholder magnitude + green it).
- F2 (genuinely valuable): the §10 prerequisite-artifact gate is SELF-SATISFIABLE - an unattended run that authors + git-commits the fixtures as its FIRST step satisfies "exists+committed at startup"; the anti-tautology guard is itself a tautology. FIX: pin the SHA-256 of each prerequisite artifact INTO THE SPEC (a separate human commit); the gate asserts `git hash-object <file> == <pinned hash>` AND that the containing commit is an ANCESTOR of the run's base ref; absent a matching pinned hash the dependent item SKIPs. Without this the entire accountability headline is ungated.
- F3: the incident is too SMALL to distinguish a correct assessment.py from a stub (1 phantom->1 preempt->1 outcome; any selector greens on a single candidate) + the pinned v6 schema DROPS mp_choice which _causal_decision filters on (EV producer emits mp_used/executed, coord producer emits mp_choice/used). FIX: pin a causal-selection rule over the new schema (walk driving_input_seqs->message->signed_payload.payload.ev_claim.ev_id==outcome.ev_id); require the committed E2 fixture to contain >=2 decisions where the WRONG one is the naive causal pick, so the selector can FAIL.
- F4: the pinned FakeConn stub MIS-ASSIGNS the vehicle API - §6.2 pins vehicle.getLastStepVehicleIDs but real code calls lane.getLastStepVehicleIDs + lane.getLength (emergency_controller.py:118-119); _sense_local_ev wraps in try/except:continue so a spec-built stub SWALLOWS the AttributeError -> real-EV half silently no-ops while the phantom half greens. FIX: correct the pin to lane.getLastStepVehicleIDs/getLength/getLastStepHaltingNumber, vehicle.getVehicleClass/getLanePosition, simulation.getTime (monotonic) + a POSITIVE assertion the scripted real EV IS preempted.
- F5: anti-starvation max_skip STILL "e.g. 3" (an instruction not a value) + unit unresolved (step operates on phase indices not approaches); D7 stays tautological on a never-incremented counter. FIX: commit max_skip=3, pin the unit per-approach (in-edge), pin the increment/force-serve site in MaxPressureController.step through the yellow/min_green machine, commit the forced-skip fixture.
- MAJOR: policies_applied per-decision MAPPING unpinned (a constant 4-id stamp greens the abort meaninglessly) -> pin which ids fire per decision type; decision.t sim field must be ADDED to the coord producer (emits tick not t) + without FakeConn.simulation.getTime _sim_time()==0.0 collapses the causal window; EV inbox seam discards signature + stores no seq (real edit to a no-edit file); no producer emits an outcome record (§1.4) -> the TEST synthesizes it from scripted ground truth (pin the test owns it); classification=LEGITIMATE trap (naive phantom admissible=True->LEGITIMATE; old _classify routes LEGITIMATE+zero-corroboration->SYSTEM_CLASSIFIER = opposite of E2 attacker-key) -> pin assessment.py NEVER reads runtime classification for origin (provenance only); two-signature-layer verify wiring is new but implementable. [+ Q9: pin which record is causal when an EV junction emits BOTH a coord decision (super().decide) AND an EV decision the same tick.]
- VERDICT: NO. FATAL 5, MAJOR 6. Explicitly: "spec-level hardening has hit diminishing returns exactly as the round-7 meta-note predicted - the remaining fatals are 'the wiring + fixtures do not exist, and the pins mis-describe the real code.'" F1/F2/F3/F5 are bounded spec edits; F4 is a one-line pin correction; then BUILD the prerequisites by hand + add a discriminating multi-decision fixture before Fable runs. [ALL no-regret: apply in v7 + author artifacts.]

### Consistency agent (a4d0f5c7) — round-7 items landed; ONE new v6-introduced MAJOR at the rewritten seam (FATAL 0, MAJOR 2, MINOR 2) [ALL NO-REGRET]
- RESOLVED: consistency #1 (identity-root + cross-auditor in §13/§6.11); #2 surface (scored-origins one rule stated) but re-broken in substance (B1); origin-classification-PRIMARY purge; indistinguishability theorem->limitation (no surviving "is PROVEN"); 3-way disjunction->one directional contrast; d_crit demoted; compounded-keyless class + budget; full consumed-input set; the KB rewrite is a GENUINE done-claim (verified: uk-traffic-law.md Euston/A501/TLRN, Goodes + Poole added, s.41 reattributed, zero residual Lambeth/Lavis/Steyn); §5.5 envelope-doc enumeration; §5.3 "(resolved)" label removed.
- B1 MAJOR (NEW in v6, at the seam v6 rewrote): the "5 provenance-decidable origins" set is NOT decidable by the two features §3 names + self-contradicts the indistinguishability limitation - two features (key-count 0/1/>=2 + corroboration-presence) yield at most ~4 cells; key-attributed-corroboration + colluding-keys + legitimate ALL collapse into the SAME (>=2 keys, corroboration-present) cell; separating colluding from honest is EXACTLY the veracity judgment §8 calls provenance-INDISTINGUISHABLE->unknown (round-7 methods F1). v5 scored 3, v6 expanded to 5 to satisfy consistency #2 + over-claimed decidability for the 2 origins needing veracity/independence data. FIX: restrict the scored partition to the genuinely 2-feature-decidable classes {keyless-lone-sighting, uncorroborated-signed, >=2-key-corroborated}; move colluding-keys-vs-legitimate into the independence-conditional/->unknown branch (as §3 already does for system-classifier/detector). [APPLIED as a targeted edit immediately.]
- B2 MAJOR (surviving, pre-existing): "Lambeth dropped" (§2 SSOT) is enforced by NO gate - "Lambeth" is absent from the D1 manifest so the grep never catches it; sibling live docs still say Lambeth (PROJECT-STATUS:29/38/108, AKIN:71, LEE-business-spec:76/103, report.tex:175, PROJECT-PROPOSAL:37/161/174, + code/results/sumo/lambeth/*, presentation, 04-writing, 06-literary-survey) while uk-traffic-law.md now says Euston = split-brain. FIX: add "Lambeth" (+ A23/A3) to the §5.1 D1 forbidden-term manifest, or a substrate-swap remediation line in §5.
- MINOR: B3 free-deviation class list not identical across §0/§1/§6.11/§12/§13 (4 vs 2 vs 3 items; identity-root in §13/§6.11 but not §0/§12) -> state ONE canonical list once + cross-ref; B4 triad<->enum mapping still unstated (honest-sensor-error has no enum home; detector-error vs system-detector) -> state the mapping. + scheduled-but-present target-state (spec.md:10 "canonical", SC-005) correctly flagged not false.
- VERDICT: NOT quite only-minor; the headline story is coherent + round-7 #1/#2 + KB genuinely landed with no new false done-claim, but ONE new MAJOR self-contradiction slipped in at the rewritten seam (B1 scored-origins over-decidability). Fixing that line-pair returns v6 to only-minor. [B1 applied now; B2/B3/B4 in v7.]

## ROUND 8 TOTAL: FATAL 10, MAJOR 24, MINOR ~4. (Trend: R5=24, R6=13, R7=15, R8=10 fatal.) Of the 10 fatal: ~5 are BUILD-PREREQUISITE (artifacts don't exist / pins mis-describe code - NOT spec-closable, need the artifacts authored + code wired), ~5 are genuine spec refinements (methods statistical-object + rigged-baseline; security keyless-boundary honest concession; novelty witness-grounding). Build agent EXPLICITLY confirmed diminishing returns. NONE needs a user decision on FRAMING - but a genuine PROCESS/SCOPE decision is now due: continue spec-hardening rounds (diminishing) vs PIVOT the loop to building the prerequisite artifacts (closes the recurring build/methods fatals). -> ask the user.
## V7 PLAN (all no-regret): [novelty] commit A501 detector-placement as PRIMARY witness grounding (kill penetration escape hatch) + tag measured-vs-modelled cells; NAME the self-referential coupling (attack controls phase == coverage-gate; cite Bittanti-Colaneri); coupling = conditional LEMMA not theorem; ONE question both contributions answer (§1/§13); Δ operationally-material. [methods] pin the functional contrast as a statistical object (estimator + closed-form well-mixed comparator + one-sided-bound>Δ rule + comparator in model-adequacy); UN-RIG the SLM baseline (score template nearest-rule for CORRECTNESS, abstention only for genuine no-rule cells); magnitude-anchored one-sided bound + named CI + fixed N; geometry n=1 case-study framing; seal the D6 engineered-case generator + name SLM as classifier + matrix yields no claimable accuracy; DPI "no signal SUFFICIENT to adjudicate"; seal="pre-registration protocol not yet a pre-registration"; per-cell CIs descriptive-only; split-dominance resolution. [security] keyless budget honest concession (pin B,W at JUNCTION-aggregate below external harm threshold, concede it charges ALL keyless transients + the emergency-refusal exposure in §13, OR present as a bounded-rate free item); need-functional magnitude concession (carry keyless-surface into the magnitude functional, list keyless magnitude-inflation as free); marginal-attribution leave-one-out + supra/sub-margin reconciliation of §6.3-vs-§6.11; single-auditor assumption explicit. [legal] CA = CONTROLLER not "processor with own basis"; private operator qua joint-controller needs Art 6(1)(f)+LIA; Fashion ID operation-specific LIMIT; DUAA 2025 caveat; KB polish (tighten Goodes first clause to ice/surface, s.41(1A) reversal note, source the 6400 figure, optional Robinson). [build] SET->culprit computable zero-corroboration form overnight + defer magnitude to Exp D; §10 gate pins artifact SHA-256 + ancestor-commit (kills the self-satisfiable gate); >=2-decision E2 fixture + causal selector over the new schema; FakeConn API pin CORRECTION (lane.getLastStepVehicleIDs/getLength etc) + positive real-EV-cleared assertion; max_skip=3 + per-approach unit + site; policies_applied per-decision mapping; decision.t sim field on the coord producer; EV seam signature+seq capture; outcome record test-owned; assessment.py ignores runtime classification for origin; EV-both-records causal rule. [consistency] B1 scored-origins (APPLIED now); B2 add Lambeth to D1 manifest; B3 canonical free-class list; B4 triad<->enum mapping.

---

# BUILD PHASE (vs MASTER-SPEC v7; hard requirement: each phase -> alignment battery -> only-minor gate before next). v7 committed 3e8a1dd.

## PHASE 1 — ground_rules.yaml (committed 74bdfe8). Alignment battery (3 agents: schema-vs-spec, legal-fidelity-vs-KB, mirror-vs-master).
Self-caught pre-battery: invalid YAML (`;` inline separators) FIXED; 4 broken statute cross-links (para5(4-6)->(4)-(6), HighwayCode-r219->HC-Rule219, TfL-authority->TfL-London-signals, ContribNegligence->ContribNeg) FIXED + re-verified (yaml parses, all 14 refs resolve).

### Legal-fidelity agent (a6b40589) — law_rules NOT yet faithful (FATAL 1, MAJOR 3, MINOR 6) [fix before Phase 2]
- F1 FATAL: LR-ev-exemption fault_weight:high OVERSTATES + inverts the conditional exemption - TSRGD para5(4)-(6): an EV is NOT at fault merely for crossing red; Griffin 60/40 is AGAINST the non-emergency driver (EV bears the 40% minority); fault_weight tracks strength-of-fault in this file so `high` = "strong fault against the EV" = contradicts statute + the text/note (internal contradiction the engine's scalar resolves the wrong way). FIX: fault_weight medium (or low), fault triggered only by the para5(5) endangerment test not the crossing.
- M1 MAJOR: LR-authority-maintain mis-binds the "does not extend to signs/signals" point to GOODES - the KB splits it (Goodes = fabric/repair SCOPE; the signs/signals point is Lord Hoffmann in GORRINGE). (I fixed this in the KB but reproduced the conflation in the artifact text.) FIX: attribute the signs/signals clause to Gorringe.
- M2 MAJOR: Griffin (the 60/40 calibration anchor) survives only in a note (not a citable law_rule.id) + Keyse absent entirely; Job-A cites law_rule.id verbatim so the engine can't cite the anchor -> add LR-griffin-calibration (statute_ref Griffin-v-Mersey) + LR-keyse (Keyse-v-Commissioner).
- M3 MAJOR: maintenance-no-exemption lives only in a note despite the maintenance entity class + the signal_tampering threat -> add LR-maintenance-no-exemption (statute_ref MaintenanceVehicles-no-exemption, fault_weight high, applies_when a non-emergency works vehicle crosses red).
- MINOR: m1 binding:MUST on pure case-law (Gorringe/Bird/Poole, several NON-liability holdings) mislabels them vs the file's own MUST def -> add a `common_law` binding value; m2 LR-authority-misfeasance fault_weight:medium sits above the KB's "least-settled/low-confidence" framing -> low + low-confidence flag; m3 LR-contrib-negligence applies_when conflates contribution-between-wrongdoers with contributory-negligence (claimant's own share) -> reword; m4 LR-amber medium under-weights the culpable case (KB: high where a safe stop was possible); m5 LR-green-due-regard medium may overstate the green driver (KB: minority share vs Joseph Eva) -> low; m6 MISSING HC-Rule176 (signals dark -> MUST duty falls away, revert to unmarked-junction care) - important for a signal-MALFUNCTION system so the engine doesn't charge a red-offence when the signal was dark -> encode it.
- No broken citations (all 14 refs match a KB id). VERDICT: NO - fix F1 + M1 + M2 + M3 (+ minors) before re-gating.

### Mirror-fidelity agent (ab47f2f4) — NOT a faithful mirror of master §D (FATAL 0, MAJOR 2, MINOR 3) [fix before Phase 2]
- M1 MAJOR: P6 INVENTED classification UNKNOWN - master §D:63 gives P6 NO classification (a pure escalate hand-off like P7); UNKNOWN has a terminal meaning (P8->reject) so it both invents + conflicts with P6's own escalate action. FIX: remove UNKNOWN; give P6 the same non-terminal escalate representation as P7.
- M2 MAJOR: P7 classification:escalate is NOT in the declared enum {LEGITIMATE, SPOOFED_OR_FAULTY, UNKNOWN}; master leaves P7 unclassified (the disambiguator RETURNS a classification). FIX: add an explicit non-terminal ESCALATE sentinel to the enum (or a separate `disposition: escalate` field) + apply to BOTH P6 + P7.
- MINOR: m1 P3 invented SPOOFED_OR_FAULTY - master §D:60 frames it "not-yet-actionable, NOT as true" (withhold), though §E:78-80 example ties a phantom to SPOOFED_OR_FAULTY (master is internally split; the artifact picked the accusatory reading) -> classify P3 as the withhold/not-yet-actionable disposition; m2 B3 "spoofable" (spec-correct per §6.4/§5.6) runs ahead of the UNAMENDED master ("cannot be spoofed remotely") so the header's "mirror section-for-section" is an overclaim on B3 -> land the §5.6 master amendment OR soften the header claim; m3 policy_kpi_rule slm_job_a_note_exempt:true is a LIVE field with target-state only in a COMMENT -> a naive consumer applies the exemption before §F is amended -> gate behind an explicit `status: target_state`/`enabled: false` field the engine honors; + add the master §F KPI second half (classification consistent with cited policies).
- VERIFIED CLEAN: substrate Euston (not Lambeth); enum; P2/P4/P5/P8 match master exactly; P4 correctly B1&B2&B4&B5 (B3 absent) + spec-consistent magnitude-void note; all 11 attacks incl both OUT_OF_SCOPE; entities + B1-B5; keyless-budget + magnitude-void + S1-S4 max_skip=3 track spec; NO false done-claim. (law_rules judged by the legal agent, not this one.)
- VERDICT: NOT clean - fix P6/P7 (2 MAJOR) to a single declared ESCALATE sentinel + realign P3, then zero-fatal/zero-major on the mirror axis.

### Schema-vs-spec agent (aea56186) — DOES NOT ALIGN (FATAL 2, MAJOR 3, MINOR 5) [fix before Phase 2]
- F1 FATAL: P7 classification:escalate is outside the artifact's own enum {LEGITIMATE,SPOOFED_OR_FAULTY,UNKNOWN} + contradicts §6.2 runtime classification (LEGITIMATE if admissible else SPOOFED_OR_FAULTY) -> validator fail-red or lenient fail-green. FIX: a non-enum classification field must not carry escalate.
- F2 FATAL: the §6.2 policies_applied VOCAB {corroboration, conservation_band, replay, membership} is ABSENT + the header conflates it with P1-P8 policy_ids (different objects: P1-P8 = decision policies, the 4-vocab = verification check-stages); the next phase (§6.2 producer) wires against this base + would emit P-ids/empty policies_applied -> fail-green the §12 Policy-KPI gate. FIX: add policies_applied_vocab + the per-decision mapping; correct the header so policies_applied (checks that ran) is DISTINCT from P1-P8.
- M1 MAJOR: cited_rules resolution key ambiguous - header says "cites law_rule.id (LR-*)" but the exampled/corpus form RTA1988-s36 is a statute_ref (== KB id), which resolves against statute_ref ONLY, never against an LR-* id -> oracle fail-red or fuzzy fail-green. FIX: pin statute_ref as the canonical cited_rules key + correct the header.
- M2 MAJOR: literal "Lambeth" (in "NOT Lambeth") trips the D1 forbidden-term grep (Lambeth is in the §5.1 manifest); file not allowlisted -> D1 fails. FIX: drop the token ("NOT the prior synthetic corridor").
- M3 MAJOR: header claims RuleDisambiguator + slm_agent "reason against THIS file" but NEITHER imports yaml/opens it/references any P#/B#/LR- id (RuleDisambiguator decides on hardcoded numeric thresholds, docstring "no file loads"; slm_agent does no RAG) -> any linkage test passes vacuously (fail-green). FIX: build the loader OR restate the header to reality (canonical base for the yet-to-be-built RAG/producer layer; RuleDisambiguator is the decoupled Exp-1 yardstick).
- MINOR: m1 `note` is an 8th field beyond §9's 7 (a whitelist validator drops 7 rules) -> declare note optional in §9; m2 fault_weight domain undocumented + decoupled from binding -> enumerate in §9; m3 replay dedup key inconsistent (attacks.replay (recipient,sender,t) vs B5 (ev_id,approach,window,claimer) vs §6.1 (sender,t)) -> state which key at which layer; m4 header overclaims hash-pinning (no SHA in spec yet) -> "to be hash-pinned"; m5 slm_agent still ships the §5.5/§11-step-4 threshold-leak strings (a later-phase consumer remediation, not this artifact).
- STRONG (survived): all 14 law_rules have the 7 fields; source/binding in-enum; all 14 statute_refs unique + resolve 1:1 to KB ids; YAML valid + deterministic. VERDICT: DOES NOT ALIGN (2F/3M) - fix before proceeding.

## PHASE 1 FIX (applied, all 3 agents' fatal+major + clear minors): EV fault_weight high->medium (endangerment-gated, F1-legal); Goodes/Gorringe split fixed - signs/signals -> Gorringe (M1-legal); added LR-griffin-calibration + LR-keyse + LR-maintenance-no-exemption + LR-dark-signals (HC-Rule176) (M2/M3-legal + m6); binding `common_law` added for case-law rules (m1-legal); LR-authority-misfeasance/green->low, amber->high, contrib-negligence reworded (minors-legal); P3->disposition WITHHOLD (not-yet-actionable, not a spoof verdict), P6/P7->disposition ESCALATE via a new non-terminal `dispositions:[WITHHOLD,ESCALATE]` set (F1-schema + M1/M2-mirror + m1-mirror); added policies_applied_vocab + mapping + header distinction from P1-P8 (F2-schema); pinned statute_ref as the cited_rules key + corrected header (M1-schema); dropped "Lambeth" (M2-schema); restated the header consumption claim to reality (M3-schema); policy_kpi_rule gated enabled:false + status:target_state + added the §F 2nd-half KPI (m3-mirror); replay-key-by-layer note (m3-schema); hash-pin header softened (m4-schema); MASTER-SPEC §9 clarified (note optional, fault_weight domain, binding common_law, cited_rules key=statute_ref). RE-VERIFIED: 18 law_rules, 0 broken cross-links, 0 schema problems, 0 policy verdict-field problems, enabled:false, 0 Lambeth. slm_agent threshold-leak (m5-schema) DEFERRED to its build phase. GROUND-RULES §B3/§F + TEST-AUDIT master amendments DEFERRED to the doc-remediation phase (header now honest about tracking spec ahead of master). -> re-gate battery (3 agents) to confirm zero-fatal/zero-major before Phase 2. (fixes committed 949a69d)

## PHASE 1 RE-GATE (3 agents on the fixed artifact; confirm closure + hunt new drift):
### Legal re-gate (ae359d79) — PASS (FATAL 0, MAJOR 0, MINOR 2). All 4 prior defects (F1 EV-weight, M1 Goodes/Gorringe, M2 Griffin/Keyse, M3 maintenance) genuinely CLOSED + faithful to KB; no new fatal/major; all 18 statute_refs resolve 1:1; Griffin 60/40 direction correct; common_law applied only to case-law; dark-signals correct; no new authority-liability overstatement. MINORs (non-gating, optional cleanup): (1) `binding: MUST` on 4 CIVIL statutes that create no offence (s.41 civil duty, TfL-controllership identification fact, 1945/1978 apportionment Acts) - MUST is the least-wrong bucket (no civil_statute slot); text accurate + fault_weight low/medium so no legal misstatement, only over-labels binding strength -> optionally add a `civil_statute` binding value; (2) LR-authority-nonfeasance text appends "liability only for misfeasance" (a Bird limb) to a Gorringe rule - a correct SYNTHESIS not a false holding -> optionally tighten. VERDICT: PASS - law_rules faithfully encode the KB; Phase 2 may proceed on the legal axis.
### Schema-vs-spec re-gate (a8e39a32) — ALIGNED (FATAL 0, MAJOR 0, MINOR 2). All prior F1/F2/M1/M2/M3 + minors CLOSED: dispositions reframe coherent (every policy carries exactly one of classification-in-enum XOR disposition-in-dispositions); policies_applied_vocab + mapping EXACT match to §6.2; statute_ref pinned as cited_rules key; no Lambeth/A23/A3; header consumption claim accurate vs code (grep ground_rules in src/ = 0 hits); §9 schema edit fully covers the artifact; 18 refs resolve; 0 schema violations. NEW MINORs (non-gating, clean BEFORE the §10 hash-pin so the pinned file survives a mechanical D1 grep): N1 authored comments use manifest words "verdict"/"liability" (D2 scopes the ban to the evidence-pack OUTPUT + MASTER itself uses "verdict"; "liability" is legal-domain/KB-mirror + the statute proper name CivilLiability-1978 - intent-scoped D1 passes, only an over-broad grep trips) -> reword the comments; N2 header "cite policy_id (P1-P8)" but the decision schema has no policy_id field (aspirational narrative) -> drop/reword. VERDICT: ALIGNED - Phase 2 can proceed on the schema axis.
### Mirror re-gate (a0c879a5) — FATAL 0, MAJOR 1, MINOR 4. Prior M1/M2/P3 + honesty-gating CLOSED (dispositions reframe faithfully mirrors master §D; terminal set matches exactly; policy_kpi enabled:false genuinely prevents a false done-claim). But the fix INTRODUCED a MAJOR: my authored comments used "verdict" x5 (D1 forbidden-manifest term, NOT prior-work quotation) -> the to-be-hash-pinned artifact fails D1. MINORs: "liability" x4 (KB-quote + statute name; defensible under §5.1 quotation but a blunt grep trips) -> allowlist; extend the spec-ahead disclosure to name B2/B5/P4 (not just B3/P1/§F); P3 note should cite §E explicitly; LR-dark-signals lacks the "reference only" degrade (§7.3). VERDICT: NOT clean (1 major) - remediate "verdict" before Phase 2.

## PHASE 1 CLEANUP (mirror MAJOR + all re-gate minors): "verdict"x5 -> "classification" (D1 major CLOSED, grep-verified 0); MASTER-SPEC §12 D1 verbatim allowlist entry added for ground_rules.yaml "liability" (KB-quote + statute proper name; file greps clean for every other manifest term); header policy_id narrative corrected (no policy_id field in the decision schema; reasoning-trace names P-ids, policies_applied carries the §6.2 vocab); spec-ahead disclosure extended to B2/B5/P4; P3 note cites §E; LR-dark-signals tagged "reference only, excluded from §7.3 emission". RE-VERIFIED: yaml parses, 18 law_rules, 8 policies, 0 broken cross-links, D1 manifest terms all 0 EXCEPT allowlisted liability=4. All 3 re-gate axes now zero-fatal/zero-major (legal PASS, schema ALIGNED, mirror's sole major=verdict CLOSED by the deterministic D1 grep). -> one consolidated adversarial re-verify (all-axes) to confirm the cleanup introduced no new drift, then Phase 1 PASSES + Phase 2 opens. (cleanup committed 9093b07)

### Consolidated re-verify (acfe3ee1) — PASS (FATAL 0, MAJOR 0, MINOR 1). "verdict" CLOSED (0 occurrences; 5 replacements read correctly, no meaning change); §12 D1 allowlist matches the file exactly (liability only in law_rule.text + the statute proper name; every other manifest term 0); yaml parses, 18 law_rules §9-valid, all 18 cross-links resolve; cleanup touched only comments/5 tokens/2 notes - no schema/cross-link breakage; minor edits (b-f) all accurate (header policy_id reword aligns to the pinned §11 schema; B2/B5/P4 genuinely spec-ahead; §E cross-ref honest; dark-signals §7.3-excludable correct). The 1 MINOR = PRE-EXISTING doc staleness in GROUND-RULES-POLICY.md §E/L6 (still says a decision cites "policy_id" + puts P/B-ids in policies_applied) vs the yaml which is correct per the pinned §11 schema - DISCLOSED inline, non-blocking, in scope for the doc-remediation phase. VERDICT: PASS.

## >>> PHASE 1 DONE (ground_rules.yaml): zero-fatal/zero-major on all 3 axes after 2 full batteries + 1 consolidated re-verify. HASH-PINNED into MASTER-SPEC §10 (git-blob-id 44672cf4 + sha256 4667c591, artifact commit 9093b07, pin in a separate commit). Battery-sizing note: the re-gate cleanup was a wording fix whose sole major ("verdict") is a DETERMINISTIC D1 grep (verified 0), so I used 1 consolidated re-verify agent not a 3rd full 3-agent battery - flagged to the user as a transparent judgment, not a silent override; user may require full 3-agent re-gates going forward. -> PHASE 2 = the E2 worked-example fixture (>=2 decisions where the WRONG one is the naive causal pick, read-only) per §9.

## PHASE 2 — E2 worked-example fixture (committed 6a4b6aa). Alignment battery (3 agents: schema/spec, logic-adversary, rule-correctness).
### Logic-adversary (a4e62d5c) — does NOT genuinely discriminate (FATAL 3, MAJOR 1, MINOR 2). My single-case fixture is OVER-DETERMINED: seq 4 is simultaneously ev_id-match + lowest-seq + earliest-t + t==outcome.t + fewest-inputs, so cheap wrong selectors coincide with the correct pick.
- F1: "always attacker-key" stub PASSES - only ONE expected (seq4->attacker-key); seq 7's corroborated case has no scored outcome, so count==0-vs->=1 is never negatively tested. FIX: add a 2nd scored (outcome,expected) pair -> seq 7, origin corroborated (ev_id EV-REAL).
- F2: naive t-match PASSES - outcome.t=100 uniquely maps to seq4 (t=100) so "decision whose t==outcome.t" lands on seq4 without the ev_id walk. FIX: neutralize t as a match key (both outcomes + decisions share t, or drop t from the match).
- F3: earliest/lowest-seq selector PASSES - seq4 is both the lower seq + earlier t of the only 2 decisions; defeating "latest" auto-makes "earliest" coincide. FIX: add a distractor decision at J1 (diff ev_id, no outcome) at an extreme so the scored decisions aren't the min/max.
- M1: "length==1->attacker-key" + "fewest-driving-inputs" both PASS (seq4 has 1 input). FIX: give the attacker decision >1 driving input, all att-fpr (indep-corrob count still 0).
- MINOR: m1 expected co-located in-file (harness must feed only records+outcome, hold expected out-of-band); m2 inverted-classification stub uncatchable without a 2nd scored case whose classification differs (fixed by F1's 2nd case: both classification LEGITIMATE, different origins).
- CAUGHT (partial credit): counting the claimer's own key as corroboration flips seq4 0->1 -> fails (the 1 real discrimination). VERDICT: NO (3F/1M) - redesign with 2 scored outcomes (distinct origins) + neutralized t + a distractor + multi-input attacker. [DESIGN READY: registry 0-2; msg att EV-PH x2 (seq3,4); D_phantom driving[[3,att],[4,att]] t=150 (seq5); msg hA EV-REAL (seq6)+sighting hB EV-REAL (seq7); D_legit driving[[6,hA],[7,hB]] t=150 (seq8); msg hA EV-OTHER (seq9); D_distractor driving[[9,hA]] t=150 (seq10, latest, no outcome); outcome1 EV-PH t=150->attacker-key seq5; outcome2 EV-REAL t=150->corroborated seq8. Kills F1(2 origins)/F2(shared t)/F3(distractor+2 outcomes defeat every outcome-independent selector)/M1(both scored decisions have 2 inputs).]
### Rule-correctness (a7e23c54) — expected VALUES CORRECT (FATAL 0, MAJOR 0, MINOR 2). Walked §3 E2 rule + §6.3 + §6.6: seq4 attacker-key CORRECT (signed 1-key uncorroborated harmful; NOT sensor-fed-spoof=0-key; NOT system-classifier - its predicate needs a logged-failed check contradicted by an independent signed sighting, absent here since the AI wrongly ADMITTED not rejected); culprit=att-fpr (names a KEY) CORRECT; runtime-LEGITIMATE-ignored CORRECT (the crux); seq7 corroborated CORRECT; independence notion matches §6.6. MINORs: (1) rationale should add "signed key-count==1 not keyless -> count==0 resolves to attacker-key not sensor-fed-spoof" (value already right); (2) note origin is class-independent (J-ATT registry class unspecified only affects the runtime classification layer origin ignores). VERDICT: expected verdict CORRECT under the spec.
### Schema/spec (ab40b7ce) — does NOT align cleanly (FATAL 1, MAJOR 1, MINOR 3). F: "verdict" token in _doc is a D1 forbidden term + file not allowlisted -> fails D1 on freeze -> reword (do NOT expand allowlist, the word is avoidable). M: outcome.t=100 lets a TIME-WINDOWED recency selector pick seq4 correctly (legacy _window_decisions(J1,t=100,horizon=60)=[40,100] EXCLUDES seq7 t=104), so the fixture only defeats a time-BLIND latest-by-seq selector, not the natural time-windowed recency the code implements -> push outcome.t >= 104 (after both decisions) so recency picks the WRONG decision while ev_id-walk stays correct. MINORs: _note/_doc extra fields (document loader ignores _-prefixed); t-clock conflation (msg logical t vs decision sim t share value 100); naive_wrong_pick slash-descriptor not a single enum token. PASSED: all §11 record schemas exact; policies_applied vocab-valid; causal-selector contract verbatim §6.3; expected.origin valid §2 token + correct §3 output; JSON parses + hash-pinnable once "verdict" removed.

## PHASE 2 REDESIGN (all 3 agents' fatal+major + minors): REWROTE the fixture as 2 SCORED outcomes (o1 EV-PH->attacker-key seq5; o2 EV-REAL->corroborated seq8) + a DISTRACTOR decision (seq10, latest, fewest-inputs, no outcome) + a MULTI-INPUT attacker decision (seq5 = 2x att-fpr, indep count still 0); NEUTRALIZED/pushed t (msg logical t 100-104, decision sim t 150, outcome detection t 160 AFTER all decisions); "verdict"->"expected result" (D1 FATAL closed, grep 0); rationale adds the signed-key-count==1-not-keyless clause + origin-class-independence note; _consume documents loader ignores _-prefixed keys + hold `expected` out-of-band; replaced naive_wrong_pick with a _discrimination block enumerating 9 defeated shortcuts. PROGRAMMATICALLY VERIFIED: ev_id selector gives o1->seq5->attacker-key + o2->seq8->corroborated (both = expected); EVERY naive selector (latest/earliest/fewest-inputs/t-window-latest/t-exact) is WRONG on >=1 outcome; D1 terms all 0. Kills logic F1/F2/F3/M1 + schema F(verdict)/M(recency). -> full 3-agent re-gate (material redesign) before hash-pin.
### Re-gate v2 consolidated (a8b18aa8) — 6 prior CLOSED, 4 NEW (FATAL 0, MAJOR 4, MINOR 2). N1 positional-pairing fake-green (outcomes same order as causal decisions, distractor last -> zip/drop-last passes without ev_id walk); N2 SPEC GAP §6.3 selector only covers message->ev_claim, o2's keyless sighting-driven decision unresolvable -> UNKNOWN; N3 SPEC GAP §11 keyless sighting (null key/sig) unsanctioned + collides with missing_metadata; N4 committed _build_e2.py regenerates the "frozen" fixture byte-identically + not hash-pinned + "hand-authored" false; N5 placeholder sigs -> verify-first pipeline UNKNOWN (crypto scope unstated); N6 seq2/seq3 B5-nonce collision (same ev_id/approach/claimer). VERDICT: does not pass (4 major).
### Phase-2 FIX v4 (643e3e8): N1 reorder (distractor seq6 MIDDLE; outcomes listed [o2,o1] REVERSED vs causal seq -> zip mispairs o2:4/o1:6, verified); N2 amended MASTER §6.3 (selector matches ev_claim.ev_id for message OR ev_id for sighting inputs); N3 amended MASTER §11 (keyless sighting null key/sig is valid + NOT missing_metadata -> sensor-fed-spoof); N4 removed _build_e2.py from repo (out-of-tree builder in scratchpad) + reworded _doc (deterministic out-of-tree builder, frozen+hash-pinned) + hash-pin deferred to post-re-gate; N5 added _scope block (crypto verify OUT OF SCOPE for this fixture, post-verification selector+attribution sub-function; tamper fixture covers crypto); N6 2nd attacker input now a SELF-SIGHTING (distinct kind, no nonce collision, tests the independence recompute). RE-VERIFIED: o1 EV-PH->seq4->attacker-key + o2 EV-KL->seq8->sensor-fed-spoof (both in-enum); positional zip mispairs; distractor middle; naive selectors all wrong on >=1 outcome; D1 terms 0; sigs 128. -> consolidated re-gate before hash-pin. NOTE: E2 fixture now at v4 (4 battery/re-gate cycles); each found legitimate defects incl 2 genuine SPEC gaps; if the next re-gate is only-minor, hash-pin + Phase 3.
### Re-gate v3 (a99468ab) — 0F/1M/1m. 6 prior CLOSED; NEW MAJOR: monotone id->causal-seq mapping (o1->seq4-min, o2->seq8-max) -> sort-by-id + map-seq-extremes selector passes without ev_id (2-outcome fixture can't defeat try-both-orderings). MINOR: nonce namespace unstated. -> v5 anti-monotone swap (still had a contrived descending residual) -> v6 DEFINITIVE.
### Phase-2 FIX v6 (d086f62): added a THIRD scored outcome for a NON-MONOTONE id->causal-seq permutation (o1->seq8 sensor-fed-spoof, o2->seq10 attacker-key, o3->seq4 attacker-key; distractor seq6). Brute-force verified NO asc/desc positional map over any 3-of-4 decisions recovers the mapping. §6.6 nonce-namespace note added (minor). o1/o2 are byte-identical decisions differing ONLY in the driving ev_id -> the §6.3 ev_id walk is STRICTLY necessary.
### >>> PHASE 2 DONE (E2 worked-example fixture): FINAL re-gate (aa7cce89) PASS 0 fatal/0 major/0 actionable minor across all 6 axes. HASH-PINNED into MASTER-SPEC §10 (git-blob a7fdb174 + sha256 881aff1e, artifact commit d086f62, pin separate commit). 6 build/re-gate cycles; surfaced 3 genuine SPEC gaps (§6.3 sighting selector, §11 keyless sighting, §6.6 nonce namespace) now fixed. CALIBRATION for later phases: heavier PROGRAMMATIC self-verification BEFORE firing the battery to cut round-trips; accept a brute-force-verified test oracle rather than iterate on contrived residuals.
### >>> PHASE 3 STARTING = the forced-skip traffic fixture (anti-starvation; §6.8/§9/§11-step5): the starved approach provably never wins _pressure argmax, max_skip=3, assert served within max_skip + anti_starvation_violations EMERGES 0 (never set by the test).

## PHASE 3 — forced_skip.json (committed a2997c2; self-verified: phase0 pressure 1<9 never wins, ref-sim serves at interval 3, phase1 dominant 7:2, violations 0, no-override never serves phase0). Alignment battery (2 agents: spec/controller, logic/non-tautology).
### Logic/non-tautology (a12d59b6) — does NOT genuinely catch a broken mechanism (FATAL 2, MAJOR 3, MINOR 3). [predicted]
- F1 FATAL: constant pressure + SINGLE starver -> a real per-phase skip-counter is OBSERVATIONALLY IDENTICAL to a hardcoded `interval % 3` timer (both serve phase0 at 3,6,9); the fake tracks no counter/no per-approach state + generalises to nothing yet passes A/B/D/E. FIX: add a VARYING-starvation scenario (time-varying halting so phase0 legitimately WINS argmax sometimes -> counter must RESET on a pressure-driven win, not only on override) AND/OR two phases that ALTERNATELY starve (a single global/modulo counter serves the wrong phase). Only a real per-phase counter survives both.
- F2 FATAL: assertion C "violations==0 emerges" is NOT enforceable by a frozen data file + NO negative control (no scenario where a broken mechanism drives violations>0), so ==0 is "0 for free". FIX: (a) move C into an explicit TEST CONTRACT (test READS controller.anti_starvation_violations, never assigns it; mechanism derives force-service from the counter, not a timer); (b) add a NEGATIVE-CONTROL/mutation case (override disabled or max_skip exceeded) whose expected is violations>0, proving the counter CAN be nonzero.
- M1: assertion D "strict majority" admits a pressure-ignoring 4-of-9 cheat -> pin integers: phase1_min_service_count, phase0_exact_service_count, phase0_service_intervals [3,6,9], asserted exactly. M2: "decision interval" undefined (seconds vs phase-cycle) -> under per-second reading 9 intervals < one min_green so A+E contradict -> define decision_interval = one decide() call at a phase boundary. M3: A uses "<=3" not "==3" -> a wrong max_skip=2 (over-eager) passes -> assert first-service == max_skip + inter-service gap == max_skip exactly.
- MINOR: m1 assertion B is self-fulfilling (a property of the data, can't fail; a sanity check not a mechanism test); m2 expected block is PROSE not machine-checkable -> typed thresholds; m3 single-TLS/2-phase/out=0 means the per-approach counter claim is asserted not EXERCISED under a genuinely multi-approach phase.
- VERDICT: NO (2F/3M) - proves a no-override controller fails (A has teeth vs the null) but does NOT discriminate a correct mechanism from a hardcoded timer + can't enforce C. Add varying/alternating scenario (F1) + negative control + test-contract (F2) + pin exact counts (M1-M3).
### Spec/controller-alignment (a2fff394) — does NOT align (FATAL 1, MAJOR 1, MINOR 4). [caught a ctor subtlety my self-verify MISSED]
- F1 FATAL: the CTOR serves green[0]=phase0 at init (controllers.py:42,46, cur=0 + setRedYellowGreenState) BEFORE any decide(); my "starved" phase0 thus gets a full min_green window at interval 0 -> a NO-OVERRIDE controller serves phase0 at interval 0 -> assertion A ("served <=3") PASSES for the broken null = the exact tautology the fixture exists to prevent. FIX: starve the phase the ctor does NOT initially serve (starved_phase=1; swap halting LA>LB) OR require recurring-override service + counter-reached-max_skip semantics.
- M1 MAJOR: no §10 pinned hash for forced_skip.json -> D7 SKIPs -> Phase 4 can't proceed hash-pinned -> append after the (fixed) artifact commit.
- MINOR: m1 skip-increment/reset convention + exact service interval only implied; m2 "per-approach (in-edge)" keying never exercised (1:1 phase/lane/edge here - stated assumption); m3 implicit getControlledLinks link-order assumption (Phase-4 net/FakeConn builder must guarantee); m4 yellow state strings/program order unpinned (builder must supply a conforming program). Non-issues confirmed: pressure argmax correct, state-string encoding valid, config matches ctor+spec, override can fire at the t>=min_green decision point, horizon adequate, deterministic/hash-pinnable.

## PHASE 3 FIX v2 (062a382): rebuilt as a MULTI-SCENARIO harness-verified oracle. STARVED phase = phase1 (LA=9>LB=1: phase0 wins argmax + served at ctor-init; phase1 never served w/o override) -> fixes the ctor-init tautology (spec F1). S2 alternating/reset-on-win (pressure flips at interval2: phase1 wins on PRESSURE = reset not override, then phase0 forced) -> a timer/global-counter yields a different schedule (logic F1). negative_control (override-disabled on S1 -> violations>=1) -> counter is a live signal (logic F2). test_contract (counter-not-timer / violations read-not-set / per-phase / safe-transition) + decision_interval defined + EXACT pinned schedules S1[3,7] / S2[0,0,1,1,1,0,1,1,1,0] (logic M1-M3). OUT-OF-TREE HARNESS verified: correct mech reproduces the pinned schedules (viol 0); no_override(viol 7/5)/modulo_timer/always_starved/overeager(ms=2) each DIVERGE on >=1 scenario; neg-control fires (viol=7). D1 clean. -> consolidated re-gate before hash-pin.
### Re-gate v2 (a6a22fe1) — content PASS (FATAL 0, MAJOR 1, MINOR 5). ALL prior CLOSED (spec-F1 ctor-init, logic-F1 timer-indistinguishability, logic-F2 neg-control, M1-M3 pinning). Independently hand-recomputed BOTH schedules on the real controller: S1=[3,7] (NOT [3,6,9] - forced service resets + costs an interval, inter-force gap 4), S2=[0,0,1,1,1,0,1,1,1,0] (reset-on-win at 2 genuine, skip[1]=2<3), violations 0; all 4 cheats diverge as claimed. MAJOR = the missing §10 hash-pin registry entry (a PROCESS precondition, not a content defect). MINORs (non-blocking): force-at-count-3 convention (fixture is frozen-first so it DEFINES not over-constrains); tie-break never exercised (but not over-specified - a valid mechanism with any tie policy passes); hardcoded (scenario,interval)->phase lookup inherent to any fixture (mitigated by review + D7 n=30); position-index harness assumption; override-disable-hook naming. -> added the last 2 as test_contract doc notes (override_disable_hook + position_index_mapping); harness re-verified.
### >>> PHASE 3 DONE (forced_skip.json anti-starvation fixture): re-gate content PASS 0 fatal/0 major; HASH-PINNED into §10 (git-blob d1d3b432 + sha256 6cf250a2). 2 build/re-gate cycles (vs E2's 6 - calibration working: harness self-verification upfront). Surfaced no spec gaps but a real ctor-init tautology + timer-indistinguishability, both closed.
### >>> PHASE 4 STARTING = the Exp-1 injector model (§9): an EXPLICIT parametric generative p(x|y) - named distribution family per class, params, closed-form/bounded-error likelihood, feature-vector schema, noise params, numeric Bayes-error floor, numeric density floor, integration method, + a model-adequacy check. Needed for the analytic nonlinear Bayes ceiling (§8 Exp-1) + the non-circular labelled dataset.

## PHASE 4 — injector_model.py (committed 490769b; self-verified: Bayes error 0.055->ceiling 0.945, box ceiling 0.857, headroom 0.088, non-circular). Battery (2 agents: methods/stats, build/spec).
### Build/spec-alignment (a1594e7e) — NOT §9-complete / NOT harness-compatible (FATAL 1, MAJOR 5, MINOR 4).
- F1 FATAL: sampled records lack event_type + split (both mandatory FlaggedCase fields) -> can't construct a FlaggedCase -> the Exp-1 harness cannot consume the injector at all. FIX: emit event_type (5-enum) + split from the generative story.
- M2: NO OOD/novel mechanism - only the 2 in-distribution classes; §8 needs a classifier-independent distance-from-dev-support OOD metric (pinned formula + dev-frozen standardisation + fixed-percentile threshold) + a novel-tail generator + a continuous accuracy-vs-distance curve; DENSITY_FLOOR is a bare constant not an implemented split. FIX: implement the distance statistic + percentile threshold + novel generator (or scope OOD to the harness + drop the promise).
- M3: TWO conflicting Exp-1 truth sources (ambiguous_decision.generate_dataset curated 90-case WITH event_type+split, consumed by evaluate; vs injector.sample_dataset parametric, wired to nothing) - relationship undefined. FIX (decision, within §8/§9 mandate): injector REPLACES generate_dataset as the §8 non-circular parametric Exp-1 source; generate_dataset demoted to a stdlib unit-test fixture. State it.
- M4: generated dataset NOT cross-interpreter reproducible - rnd.betavariate/normalvariate algorithms aren't in CPython's stability contract + shift the shared stream (so even the stable Poisson counts diverge); pinning the SOURCE is stable but the DATASET (feeds the D4 sealed bundle) is interpreter-dependent + undocumented. FIX: draw all variates from random()/getrandbits via inverse-CDF (version-stable), OR FREEZE + commit the generated dataset JSON as the pinned artifact + pin the interpreter.
- M5: §9 "numeric Bayes-error floor" not FROZEN in the artifact (only a runtime MC in __main__; non-reproducible per M4). FIX: compute once + freeze the Bayes-error value + half-width + seed/n/interpreter as a constant.
- M6: committed (490769b, blob 8a518b52) but NOT in the §10 registry -> D4 SKIPs -> do NOT pin until F1/M2-M5 fixed (pinning now freezes a broken artifact).
- MINOR: m7 axis_aligned_ceiling uses a DIFFERENT grid than the real RuleDisambiguator (_CORR_K_GRID etc, residual_max<=1.5) -> import+reuse the real grid/decider so the "rule ceiling" is the rule's actual function class; m8 ADEQUACY is prose+PENDING not executable -> add a substrate-gated moment-comparator stub; m9 residual round(exp(normal),3) can collapse to 0.0 -> _log_lognormal(0)=-inf -> clamp residual to a small positive floor on sampling; m10 feature schema implicit (subsumed by F1).
- VERDICT: 1F/5M - fix F1 (event_type+split) + M2 (OOD split) + M3 (replace generate_dataset) + M4/M5 (freeze the dataset+Bayes floor as committed JSON, reproducibility) + m7-m9, THEN pin.
### v2 (a3e0a95) closed all 7 build/spec defects (event_type+split; distance-from-support OOD split; injector=sole source; FROZEN JSONs; frozen Bayes floor; real grid; residual clamp). Re-gate (a6d7012a, methods+build fresh - prior methods agent was STOPPED with no output): 0F / 4 METHODS MAJOR / 3m. MAJOR-1 the distance-tail "novel" split is the EASY far-tail not harder (inverts the generalisation story); MAJOR-2 headroom compared ceilings on DIFFERENT populations (0.099 overstated ~25%); MAJOR-3 density_floor DECLARED but never APPLIED (contradicts §8/D4 tail-no-ceiling); MAJOR-4 adequacy PENDING -> ceilings researcher-DOF, can't headline. Minors: m-1 pool-frozen (not dev-frozen) standardizer; m-2 event_type zero-signal; m-3 raw-vs-balanced-accuracy box objective.
### v3 (a7d0fd4) closed the 4 methods majors the way §8 ITSELF prescribes: dropped the "harder novel/OOD" framing -> the split is just the p85 distance-from-DEV-support tail (FlaggedCase enum) + the PRIMARY characterisation is a continuous accuracy-vs-distance CURVE (honestly shows the tail is EASIER 0.875->1.0, no spin) [M1]; both ceilings on the SAME density>floor population, balanced acc -> headroom 0.074 [M2]; density floor APPLIED (595 in-support / 5 tail no-ceiling) [M3]; MODEL_RELATIVE_CAVEAT embedded + every downstream ceiling labelled model-relative pending real-Euston adequacy [M4]; dev-frozen standardizer [m-1]; balanced-acc box [m-3]. Final re-gate (ad9e5e88, proportionality-weighted): PASS 0F/0M/3m (doc/dead-code nits). Key conclusion: a well-specified synthetic Naive-Bayes model CANNOT provide a genuinely-harder novel regime (the far tail is where classes separate = easier); the injector is a valid BAYES-CEILING tool + accuracy-vs-distance characterisation, and the novel-GENERALISATION story is correctly SCOPED to the real substrate (Job B is non-load-bearing per §3). Applied the 3 nits (strike stale MC-half-width promise + dead CEIL_MC_N + relabel typicality).
### >>> PHASE 4 DONE (Exp-1 injector: injector_model.py + exp1_dataset.json + exp1_ceilings.json): final re-gate PASS 0 fatal/0 major; HASH-PINNED all 3 into §10 (injector 58f5eb07 / dataset 6f940d2 / ceilings 2c9714ff). 3 build/re-gate cycles. Bayes ceiling 0.933, box 0.859, headroom 0.074 (balanced acc, density>floor), MODEL-RELATIVE pending adequacy.
### >>> PHASE 5 STARTING = anchor.md (the quorum-anchor contract, §6.7/§9): the concrete >=2 external witnesses (e.g. a Rekor/Sigstore transparency-log entry AND a named single-node ledger RPC) + the NAMED cross-auditor + its disagreement-surfacing behaviour + submit/verify calls + fail-loud offline behaviour. NOTE: euston.net.xml (a later phase) needs SUMO/netconvert tooling -> LIKELY the point to AskUserQuestion about the user's machine.

## PHASE 5 — anchor.md (committed e3a6b5c; self-checked D1 clean + §6.7/§9 coverage). Battery (2 agents: security-soundness, build/spec).
### Security-soundness (a6136370) — NOT sound / NOT honestly complete (FATAL 2, MAJOR 3, MINOR 4). anchor.md OVERCLAIMED detection + CONTRADICTED §6.7's own honest framing.
- F1 FATAL: per-signer reconciliation is OPERATOR-CONTROLLED on both sides -> detects nothing about operator suppression (operator drops 3 of signer X's records, writes per_signer_counts[X]=7 to match the 7 disclosed -> disclosed==claimed, xaudit passes). The "first-party signed assertion" signature is NEITHER anchored in the Commitment NOR verified by xaudit + xaudit has no operator-independent path to X's true signed count. The §1 "per-signer-undercount externally detectable" claim does NOT hold. FIX: anchor SIGNER-SIGNED count assertions (sig IN the Commitment); xaudit verifies each signer's sig over its own count + gets the expected-signer ROSTER from an operator-independent enrolment source (the §6.5 identity root); reconcile signed-count vs disclosed. Without that -> DROP the detection claim to "MEASURED, not detected".
- F2 FATAL: mismatch DIRECTION is BACKWARDS + contradicts §6.7 + FRAMES the honest operator. anchor.md L62 "disclosed<claimed -> operator dropped -> INCOMPLETE_DISCLOSURE" but §6.7 says "a lone signer's claimed>disclosed CANNOT attribute suppression to the operator". A malicious NON-operator signer over-reports emitted_count -> disclosed<claimed -> raises INCOMPLETE_DISCLOSURE -> frames the operator + halts attribution. FIX: align to §6.7 - disclosed!=claimed is INCONCLUSIVE (signer over-claim vs operator drop are provenance-indistinguishable), NEVER operator-attributed; state the framing vector.
- M1 MAJOR: batch-drop/seq-gap selective disclosure CLAIMED detected but NOT caught - monotonicity check asserts seq_high non-DECREASING not CONTIGUOUS, so dropping a whole batch/seq-range at a boundary = a seq_high JUMP that passes; §5 omits this post-emission-drop class. FIX: anchor a per-batch seq_low + contiguity witness + a sound per-signer channel, OR add "post-emission operator drop / seq-gap" to the §5 MEASURED-not-detected boundary + retract the blanket "selective-disclosure detectable".
- M2 MAJOR: GDPR - "Personal data NEVER in the commitment" is FALSE: key_fpr + per_signer_counts + seq_high anchored to the IMMUTABLE PUBLIC Rekor log = pseudonymous personal data (re-linkable via enrolment provenance, Art 4(5)), UN-erasable -> defeats Art 17; contradicts §7.7 "anchor stores ONLY a Merkle root". FIX: qualify to "no message PAYLOAD / no direct identifiers"; concede key_fpr/counts/seqs are pseudonymous personal data on an immutable log, DPIA residual; reconcile with §7.7.
- M3 MAJOR: the §10/D-anchor gate precondition "a reconciled per-signer count" is satisfiable by operator gaming (F1) -> a run labelled "externally anchored" with the coupling ALLOWED green while detection is illusory. FIX: gate must assert SIGNER-SIGNATURE-VERIFIED counts vs an independent roster.
- MINOR: m1 disclosed>claimed also indistinguishable (operator replays a signed sighting to inflate a signer's disclosed count - §6.6 nonce is claims-not-sightings); m2 one-sided batch_id (present on one witness, absent on other) behavior undefined -> pin to EQUIVOCATION/ROLLBACK; m3 single-auditor "gossip logs locally" = gossip-to-oneself, decorative; m4 omitted-signer-ROW leaves nothing to reconcile -> xaudit must enumerate expected signers independently.
- HOLDS (credited): cross-witness split-view equivocation IS caught (given honest xaudit+witnesses); rollback caught by Rekor immutability + monotonicity; trust-relocation honesty stated; CT credit complete; fail-loud coverage correct except the M3 gate gap; creation-time-omission + colluding-keys correctly out of reach (but incomplete for post-emission-drop, M1).
- VERDICT: 2F/3M - the per-signer detection is a tautology under operator control + the direction frames the honest operator + selective-disclosure overclaimed + GDPR "never personal" false. The honest fix mostly ALIGNS anchor.md BACK to §6.7 (per-signer = a MEASURED out-of-scope-operator instrument, NOT an in-scope detection; direction inconclusive) + qualifies GDPR + moves batch-drop to the measured boundary.



### v2 (a432fc0) closed the 2F/3M; re-gate (aa2407b2) 0F/2M NEW: (MAJOR-1) gate condition (ii) turned a NON-attributing per-signer count mismatch into a BLOCKING gate -> a lone lying signer could DoS the operator's true non-equivocation claim + it attributes an inconclusive signal (contradicts §6.7); (MAJOR-2) my GDPR "fix" just RELOCATED key_fpr+counts from immutable W1 to immutable W2 (still a ledger, still un-erasable).
### v3 (6dcfa83) restructured: both witnesses carry ONLY {batch_id,seq_low,seq_high,root}; counts fully OFF-CHAIN/erasable; per-signer surfaces-not-blocks-not-attributes. Re-gate (abcd133169c5321f7) found 1 FATAL I MISSED: removing the explicit key_fpr FIELD did NOT remove the fpr from the Merkle LEAF PREIMAGE (entry_body carries sender_pubkey_fpr, §6.1/§6.2), so the immutable PUBLIC Rekor root STILL commits to pseudonymous personal data via inclusion proofs; §7.7 MANDATES a salt ("provably non-re-linkable without the off-chain secret") which my line 17 explicitly DECLINED. Also MAJOR-1 ("matching §10/§12" over-assertion vs the reinterpreted 3rd conjunct) + MAJOR-2 (equivocation call conflated REACHABLE with CONSISTENT - honest operator false-accused mid-propagation, no finality guard) + 2m.
### v4 (b955602) FATAL fix = ADOPT §7.7 salted root: witnessed `salted_root` = Merkle over HMAC(salt,entry_body), salt off-chain/erasable, crypto-erasure = Art 17; current unsalted AuditLog.merkle_root declared NOT the witnessed root (NEW mechanism, scoped to §11). MAJOR-1 = §6 states the 3 conjuncts, count-conjunct NON-BLOCKING per §6.7, + amended §10/§12 to "reconciled = run-and-logged, non-gating". MAJOR-2 = §4 step 2 gates one-sided-absence equivocation on FINALITY (W1 STH-covered / W2 block-depth>=finality) + absent-witness-caught-up, else INCOMPLETE_DISCLOSURE. Re-gate (same agent) PASS 0F/0M/2m.
### v5 (7c9a5c1) folded the 2 gate-pass minors (empty-window wording -> salted-leaf commitment; §5 presence-claim -> salt-holder qualifier + Art-17 forensic-availability tradeoff). Stress-checked salt restructuring introduced NO new breakage: inclusion-proof PRESENCE preserved (salt-holder-conditional), byte-canonical `agree` unaffected (compares anchored bytes), xaudit's 2 SOUND detections need no salt.
### >>> PHASE 5 DONE (anchor.md): final re-gate PASS 0 fatal/0 major; HASH-PINNED into §10 (blob 9753babb / sha256 5d341aef). 5 build/re-gate cycles. DETECTS equivocation+rollback, SURFACES per-signer mismatch (non-attributing), creation-time omission + colluding keys OUT of reach (§8). GDPR clean via §7.7 salted root. KEY LESSON: the battery caught a FATAL my own self-verification missed twice (personal data survives in the Merkle preimage even after the explicit field is removed) - exactly the point of the hard-requirement build battery.
### >>> PHASE 6 STARTING = FakeConn vehicle-API stub (§6.2): lane.getLastStepVehicleIDs/getLength/getLastStepHaltingNumber, vehicle.getVehicleClass/getLanePosition, monotonic simulation.getTime. NOTE: euston.net.xml (later) needs SUMO/netconvert -> AskUserQuestion about the user's machine when reached.
